====================================================================================================
FINDING 1/25  [Blocker] [contradiction]  (raised by consistency reviewer)
LOCATION: Part C M3-14 (section_3_functional_core.md L389) against Part B WF-38 (L651) and Table A
EXACT TEXT IN THE DOCUMENT:
| M3-14 | Confirmation of the request creates the Sample records in state Expected, or Received where the sample is already in hand, and creates the Test Allocations. | [MUST] |
PROBLEM:
M3-14 creates the Test Allocations when the request is confirmed — i.e. potentially before the silk has arrived. Section 2 creates them only when the sample is accepted: WF-38 [MUST] "On entry to Accepted or Accepted with Reservation the system creates exactly one Allocation per requested test for that sample, and the sub-samples specified by each method version", and Table A's **Expected** row states the system "Does **not** print a label. Does **not** start any clock" while the **Accepted** row "Creates one Allocation per requested test." §7.2 also places allocation creation at step 10, after receipt (step 7) and the acceptance decision (step 8). Two [MUST] requirements demand mutually exclusive behaviour at the exact point where the developer writes the trigger; whichever he implements, the other acceptance test fails, and allocations created for samples that are later rejected would pollute the workload and turnaround figures.
REQUIRED FIX:
Replace the whole M3-14 row (parts/section_3_functional_core.md L389), including the acceptance-check cell, which the reviewer's fix leaves untouched and self-contradictory:

| M3-14 | Confirmation of the request creates the Sample records in state **Expected**, or **Received** where the sample is already in hand (in which case the receipt fields required by WF-32 and the condition checklist of WF-33 are captured at confirmation). Confirmation does **not** create Test Allocations. Test Allocations are created by the system on the sample's entry to **Accepted** or **Accepted with Reservation**, one per requested test, together with the sub-samples each method version requires (WF-38). Until then the **Allocation preview** shows the sample x test grid that *will* be created, and the request's promised date and estimated charges are computed from that preview, without creating any allocation record. | [MUST] | Confirm a request for 5 samples x 3 tests: the 5 samples appear in Expected, the preview shows 15 rows, and a query for allocations against the request returns none. Accept one sample: exactly 3 allocations appear for that sample and none for the other four. Reject a second sample: still no allocations for it (M4-06). |

Two companion edits in the same file so Part C is internally consistent:
- L499, Sample registration field table, "Tests requested (multi-select)" notes column: change "Generates the allocations" to "Determines the allocations that will be created on acceptance".
- L515, M3 worked example, "Test Allocation | 15" detail cell: append "Created in M4 on acceptance, like the sub-samples" so it matches the sub-sample row on L516, which already reads "Created in M4".

No change is needed to WF-38, Table A, Table B or 7.2 step 10 — they are already mutually consistent, so Part C is the side to amend.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
Verified independently, and the contradiction is broader than claimed. Both quotes exist verbatim: M3-14 at parts/section_3_functional_core.md L389 ("...and creates the Test Allocations. | [MUST] | Confirm a request; the sample and allocation grid is created automatically.") and WF-38 at parts/section_2_workflow.md L651. Part B is unanimous that allocation creation is triggered by acceptance, not confirmation: WF-38 [MUST]; Table A Expected row (L577) "Allots the sample number. Does not print a label. Does not start any clock."; Table A Accepted row (L580) "Creates one Allocation per requested test."; Table A Rejected row (L582) "Creates no allocations."; and 7.2 step 10 "Test Allocations created automatically" placed after receipt (7) and the acceptance decision (8). Three further corroborations the reviewer did not cite make this worse, not better. (1) M4-06 [MUST] at section_3_functional_core.md L584 — "No test allocations are created for a rejected sample", acceptance check "Reject a sample; no allocations exist" — so M3-14 contradicts a MUST inside its own Part C, and the two acceptance tests cannot both pass. (2) WF-36 [MUST] (L649) says Doubt Raised "blocks the creation of allocations", which is meaningless if creation already happened at confirmation. (3) Table B (L598) has no pre-Pending allocation state, and entry to Pending "Computes the due date", which per M3-24 (L409) and WF-51 (L664) is measured from the acceptance clock start — an allocation created at confirmation therefore has no computable due date and would appear in the Section Head's allocation queue (M5-09) for silk that has not physically arrived. I searched for a reconciliation and found none: there is no precedence or conflict-resolution clause anywhere in SPEC.md, the appendix contains no note on M3-14 or WF-38, and I searched "allocations are created", "creates the Test Allocation", "Allocation per requested test" and "creates no allocation" across all parts — the only hits are the four lines above. The M3 screens table entry "Allocation preview | Front Desk | Shows the sample x test grid before confirming" (L356) supports the reviewer's fix rather than the requirement. Two caveats that do not defeat the finding: the reviewer's supporting claim that pre-acceptance allocations "break WF-1" is wrong (WF-1 at L251 governs the Test Request state, and confirmation is exactly when the request becomes Accepted); and the proposed fix rewrites only the Requirement cell, leaving the acceptance check "the sample and allocation grid is created automatically" in place, which would leave the table row self-contradictory and the failing test still failing. Blocker severity is correct: two MUST requirements with mutually exclusive triggers, each carrying an acceptance check that the other implementation fails, at the point where the developer writes the trigger.

====================================================================================================
FINDING 2/25  [Major] [contradiction]  (raised by fact-integrity reviewer)
LOCATION: Part C M8-46 (approx. L2491) versus Part D M21-43 (approx. L4425)
EXACT TEXT IN THE DOCUMENT:
| M8-46 | Signing supports three tiers, selected by configuration: (i) a system electronic signature ... (ii) a signature applied with a certificate issued by a licensed certifying authority; (iii) an Aadhaar-based electronic signature service. Tier (i) is the minimum.
PROBLEM:
Both requirements define "three tiers" of signature as a configuration choice, but they are different three-item lists. M8-46's set is {system e-signature, licensed-CA certificate, Aadhaar eSign}. M21-43's set is {printed name with an in-application record, in-application e-signature with re-authentication, cryptographic digital signature on the file}. A developer building a configuration enum cannot satisfy both. Separately, M8-46 offers Aadhaar eSign as a stand-alone configurable tier for report signing, which the research rules out for this site: eSign needs a UIDAI round trip for every signature and "fails entirely when the internet is down, which for Dharmavaram ... is disqualifying for the core report-issue path", contradicting NFR-27/NFR-29 (report issue must work with no internet).
REQUIRED FIX:
Keep the reviewer's direction (M21-43 becomes the single definition; drop Aadhaar from the report-signing ladder) but tighten it.

Replace the M8-46 requirement cell (SPEC.md:2490 / parts/section_3_functional_core.md:1245) with:

"Signing uses the tier ladder defined once in M21-43 and no other set: (a) a printed name and designation with an in-application signature record; (b) an in-application electronic signature with re-authentication at the moment of signing, cryptographically bound to the file's checksum, with a tamper-evident audit record; (c) a cryptographic digital signature applied to the file with a certificate from a Certifying Authority licensed by the Controller of Certifying Authorities. Tier (c) is the design target, tier (b) the minimum acceptable; tier (a) alone is never presented to a customer as a signature, and neither is a pasted signature image. The tier actually used is recorded on every signature record. **No signing tier that requires a call outside the laboratory network may be offered for report issue**, because report issue must complete with the internet disconnected — see NFR-27, NFR-29, NFR-32 and acceptance case AC-33. This rules out an Aadhaar-based electronic signature service on this path, which needs a UIDAI/ESP round trip per signature."

Four corrections to the reviewer's version:

1. Cite NFR-32 and AC-33, not just NFR-27/NFR-29. NFR-32 ("Approving or issuing a report never waits on any network call outside the laboratory") is the requirement that actually forbids the Aadhaar tier; AC-33 is the test that would catch it. The reviewer's fix omits both.

2. Do NOT grant Aadhaar eSign for customer-signed documents inline. The reviewer's clause ("may be offered only as an additional option for customer-signed documents (test requisitions, declarations, undertakings)") creates an orphan permission: "Aadhaar" appears in the document only at SPEC.md:323 (glossary), 2490 (M8-46) and 4864, and SPEC.md:4864 is NFR-60 [MUST] — "Aadhaar numbers are not collected or stored anywhere in the system... Zero Aadhaar fields." Granting the capability in the report-signing block gives it no requirement ID, no acceptance check, and a live tension with NFR-60. Delete the Aadhaar reference from M8-46 outright; if customer-side eSign is wanted, raise it as an OPEN-Q against the requisition module, with the NFR-60 carve-out stated explicitly.

3. Update M8-46's acceptance-check cell, which the reviewer's fix leaves untouched and which still reads "Configure tier (i)". After relabelling to (a)/(b)/(c) it should read: "Configure tier (b); a signature record is created carrying the checksum, the signer, the re-authentication and the tier used. Inspect the tier options offered in configuration; none requires an external network call at the moment of signing."

4. Reconcile §24.4 Technology stack (SPEC.md:5057), which the reviewer did not check. It lists "an electronic-signature service provider" as one of three interchangeable PDF-signing implementations. A blanket ESP ban would strand that option, so either reword it to "a remote signing service whose signing key remains inside the laboratory network" or annotate it as available for non-report documents only.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
Independently confirmed on both counts. (1) Both quotes are verbatim: M8-46 at SPEC.md:2490 (authored in parts/section_3_functional_core.md:1245) and M21-43 at SPEC.md:4424 (parts/section_4_functional_support.md:1499). Both state "three tiers" as a configuration choice, and the sets genuinely differ: M8-46 = {system e-signature, licensed-CA certificate, Aadhaar eSign}; M21-43 = {printed name, in-app e-signature with re-auth, cryptographic digital signature}. I searched for a reconciling mechanism and found none: M8-46 occurs exactly once in the whole document, the M8 block contains no cross-reference to M21.6 or M21-43, there is no Part C/Part D precedence rule in the front matter, and the DB column sys_esign.auth_method (SPEC.md:5244) is an untyped VARCHAR(20) so it does not pin the enum either. The only partial tie-breaker is the configuration default table at SPEC.md:4544 (signature_tier = "In-application electronic signature with re-authentication", cited to M21-43), which fixes the default but leaves the full option set ambiguous — exactly the developer problem the reviewer describes. Note the document actually contains a THIRD triad at SPEC.md:5057 (§24.4 Technology stack): PDF signing behind "an interface with three interchangeable implementations: a local key file (development only), a hardware security module holding an organisational Document Signer certificate, and an electronic-signature service provider." (2) The Aadhaar-eSign-for-report-signing conflict is real and the reviewer actually under-stated it. The decisive requirement is not NFR-27/NFR-29 but NFR-32 (SPEC.md:4812): "Approving or issuing a report never waits on any network call outside the laboratory... Report issue completes in the NFR-16 target with the internet disconnected," reinforced by acceptance case AC-33 (SPEC.md:5603) which requires "report generation, report printing... work with no degradation" during the cable-pull day. Configuring M8-46 tier (iii) fails both [MUST] items. The research backs the mechanism: parts/research_india_commercial.md:409 states for eSign "Works when internet is down: No — requires UIDAI/ESP round trip"; line 445 adds the "disqualifying for the core report-issue path" judgement. That line 445 is tagged (ASSUMPTION) rather than [VERIFIED] does not weaken the finding, because the contradiction is internal to the specification. Also worth noting the research recommended eSign for the CUSTOMER side and explicitly against the lab-issue side; the spec did the opposite. Severity Major is correct, not inflated: two [MUST] requirements define the same named configuration key differently, and one enumerated option would fail a [MUST] acceptance test. The fix direction is right but imprecise in four ways, corrected below.

====================================================================================================
FINDING 3/25  [Major] [buildability]  (raised by fact-integrity reviewer)
LOCATION: Part C M10-17 and OPEN-Q-C11 (approx. L2805, L2828); also M10-20, M10-23
EXACT TEXT IN THE DOCUMENT:
*Recommended default:* seed the configuration with the standard's values (six skeins, two sets of three, the standard's convergence threshold and its set agreement tolerance), mark each value as unconfirmed
PROBLEM:
The document instructs the developer to seed the conditioning configuration with "the standard's values" but never states three of them anywhere in 6,000 lines: the oven temperature (the string "140" does not appear in the document at all), the dry-to-constant-mass convergence threshold, and the two-set moisture agreement tolerance. All three are verified facts in the research brief ([F, S4]: oven at 140 °C with an in-chamber balance reading to 0.1 g; dry 15 min, weigh, then 5-minute intervals until successive loss is at or below 0.25 per cent of the previous weighing — 10 minutes for the first period in Part 6; "If the two results vary by more than 0.5 percent, the test shall be repeated", corroborated by FAO). The 11 per cent regain and the 7 per cent retest rule are both stated explicitly, so the omission is inconsistent as well as blocking. The solo developer cannot seed the table, cannot write M10-27's test vectors, and will either guess or leave the values null — which silently disables the M10-17 and M10-20 gates on the highest-consequence money calculation in the unit.
REQUIRED FIX:
Seed the values, but do not follow the reviewer in reclassifying the apparatus figures.

(a) M10-17 (SPEC.md L2804) — append, in M10-23's existing house style: "Seeded from the governing standard, each value held with an effective date and marked unconfirmed until checked against a real worksheet: oven temperature **140 °C**; first drying period **15 minutes**, then successive periods of **5 minutes**; convergence threshold **0.25 percent of the previous weighing**. The conditioned-size determination of Part 6 uses a **10-minute** first period — hold it as a second configuration row keyed to that method version, never as a branch in code."

(b) M10-20 (L2807) — append: "The tolerance is seeded at **0.5 percent**, the figure the governing standard prescribes ('if the two results vary by more than 0.5 percent, the test shall be repeated'), held as configuration with an effective date." This also removes a present ambiguity, because 0.5 percent already appears illustratively in M6-23's acceptance check (L2141) and in the M14 method-master replicate-agreement example (L3418) with no statement that it is the normative value.

(c) OPEN-Q-C11 (L2824) — replace "the standard's convergence threshold and its set agreement tolerance" with the figures themselves: "six skeins in two sets of three; oven 140 °C; 15 minutes then successive 5-minute periods; convergence 0.25 percent of the previous weighing; set agreement tolerance 0.5 percent". Apply the identical edit to the Appendix B row at L5825, which restates the recommended default verbatim, so the two copies do not diverge.

(d) REJECT the reviewer's last two clauses. Moving the balance capacities and least counts (platform 100 kg / 0.1 kg, skein 1 kg / 0.1 g, in-oven 0.1 g) into this list and declaring "every one of these is configuration with an effective date" contradicts M10-07 (L2789), M11-03 (L2955) and the M11 equipment field table (L3003, "Least count / resolution — mandatory for measuring instruments"), all of which hold measurement range and least count on the per-instrument equipment record, not in method configuration. If the standard's apparatus minima are wanted — and they are worth having — add them against the method version as a required-range attribute so M11-03's existing warn-on-mismatch check has something to compare against, leaving the actual instrument's least count where it already lives. Also drop the reviewer's "six skeins, two sets of three" restatement, which M10-13 and OPEN-Q-C11 already state.

(e) Optional but cheap: extend go-live checklist item 5 (L5508) to name "the conditioning method's numeric constants" alongside the master-data list, so the seeded values are signed for rather than resting on OPEN-Q-C11 alone.

Cite provenance in the edit as the research pack already does: parts\research_silk_domain.md L217, L397, L398, L437, all [F, S4], with FAO corroboration [F, S5] for the 0.5 percent rule.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
CONFIRMED, with two of the reviewer's supporting claims overstated.

1. Quote is verbatim. `D:\Prashant_WorkSpace\TTL Software\SPEC.md` line 2824 (OPEN-Q-C11), restated verbatim in the Appendix B register at line 5825; authored source is `parts\section_3_functional_core.md` line 1579.

2. The three values really are absent. Searched the whole 6,000-line spec: "140" appears once only, as a source-list row number at line 6053 — never as a temperature. "0.25" appears nowhere at all. No drying-period figure (15 / 5 / 10 minutes) appears anywhere near M10; the only "15 minutes" hits are staff-time targets, notification cycles, session timeouts and RPO. Searched synonyms as instructed: "oven temperature", "drying temperature", "convergence", "constant mass", "dry-to-constant-mass", "seed/seeded", "least count", "method constant" — every hit describes the values as configuration without ever stating them (M10-17 L2804, M10-18 L2805, field table L2896 "Configuration-seeded", note 4 at L2916, M16-20 L3695). Partial exception the reviewer missed: 0.5 percent does appear twice, but only illustratively and in other modules — M6-23's acceptance check at L2141 ("differing by 0.7 percent against a 0.5 percent rule") and the M14 method-master field example at L3418 ("e.g. repeat if the two sets differ by more than 0.5 %"). Neither is a normative seed for M10-20, so the tolerance is still unseeded, but it is not literally "nowhere".

3. Real in context, and inconsistent with the document's own practice. M10-23 seeds the regain at "eleven percent" with attribution to the governing standard; WF-74/L1049 states the 7 percent retest rule outright; L784 states 27 ± 2 °C / 65 ± 2 % RH and the 40-sizing-skein plan; OPEN-Q-C11 itself states six skeins and two sets of three. So there is no policy of withholding the standard's numbers, and no copyright or standard-unavailability caveat anywhere (only M14-18's "where the laboratory holds a licensed copy"). More decisively, the front matter's stated contract (L44/L46) is that every OPEN-Q carries "a recommended default so that nothing is blocked while answers are pending" — a default that says "the standard's convergence threshold and its set agreement tolerance" is self-referential and satisfies nothing.

4. Not handled elsewhere. Appendix B (L5825) copies the same non-value. Go-live checklist item 5 (L5508) lists master data — tests, methods, parameters, specifications, grades, rate cards, taxes, holidays, customers, locations — and does not name the conditioning method constants, so nothing downstream forces the seed to exist.

5. Research provenance checks out and the spec does not overstate. `parts\research_silk_domain.md` L217 (apparatus, oven 140 °C, in-chamber balance to 0.1 g), L397 (two-set 0.5 percent repeat rule, corroborated by FAO at S5), L398 (15 min then 5 min, 0.25 percent of previous weighing, Part 6 uses a 10-min first dry), L384 (Part 6 oven-dried at 140 °C) — all marked [F, S4]. L437 is the brief's own recommendation: put the 140 °C and the 0.25 %/0.5 % thresholds in configurable reference data with an effective date, not in code. So there is no verification barrier to stating them.

Two reviewer sub-claims that do not hold, and which is why the rationale needs trimming rather than the finding dropping: (a) "cannot write M10-27's test vectors" is wrong — M10-27, note 8 (L2920) and go-live item 9 all source the vectors from twenty historical manual worksheets with hand-computed weights, not from the standard's constants; (b) "silently disables the M10-17 and M10-20 gates" is overstated — the gate mechanism is fully specified structurally and both acceptance checks are value-independent, and OPEN-Q-C11 does require the scientist to confirm before go-live. The residual harm is real though: an unseeded or guessed default (105 °C is the obvious wrong guess a developer reaches for) feeds the oven-dry mass and therefore the settlement figure on a commercial invoice-weight certificate, and the spec gives the developer nothing to check against. Major stands.

6. The proposed fix is directionally right but introduces one new contradiction — see correctedFix.

====================================================================================================
FINDING 4/25  [Major] [contradiction]  (raised by consistency reviewer)
LOCATION: Part C M7 throughout — screens table (section_3_functional_core.md L1013–L1015), M7-25, M7-26; Part C M5 blinding table header — against Part B §8.3 (L531)
EXACT TEXT IN THE DOCUMENT:
| Authorisation queue | Authorised Signatory | Verified work awaiting signature |
PROBLEM:
Section 2 §8.3 settles the person who signs as "**Approving Authority**" and lists "Signatory (acceptable in speech)" as a word not to be used in the system. Part C never once uses "Approving Authority" — it uses "Authorised Signatory" as the role on the authorisation screens, as the register name ("Authorised Signatory register" in M7-25, M7-26) and as the blinding-table column header ("Unit Incharge / Authorised Signatory"). Part D uses "Approving Authority" 37 times, including as a permission-matrix role. The same person therefore has two names in two adjacent parts, and the screens, the register and the permission matrix will not match.
REQUIRED FIX:
Change the ROLE LABEL only in Part C (parts/section_3_functional_core.md); do NOT touch the register name.

Role-label edits ("Who uses it" column and prose naming the person):
- L1013: "Authorisation queue | Authorised Signatory" -> "Authorisation queue | Approving Authority"
- L1014: "Authorisation and signing | Authorised Signatory" -> "... | Approving Authority"
- L1162: "Report submit for approval | Report Writer | Sends to the authorised signatory" -> "...Sends to the Approving Authority"
- L1163: "Authorisation and issue | Authorised Signatory" -> "... | Approving Authority"
- L1164: "Amendment | Unit Incharge (raise), Signatory (issue)" -> "Unit Incharge (raise), Approving Authority (issue)"  [bare "Signatory" is the most direct §8.3 violation and the reviewer missed it]
- L1523: "Certificate authorisation and issue | Authorised Signatory" -> "... | Approving Authority"
- L745 blinding-table header: "Unit Incharge / Authorised Signatory" -> "Approving Authority (Unit Incharge)", matching Part D M21.3's own gloss "AA Approving Authority (Unit In-Charge)". Do not write "Unit Incharge / Approving Authority" as the reviewer proposes - the matrix already states AA IS the Unit In-Charge, so the slash pair is misleading.
- L742 (M5-23): "the reviewer and the authorising signatory are not blinded" -> "the reviewer and the Approving Authority are not blinded"
- L1090 (M7-35): "no authorised signatory is available" -> "no Approving Authority is available"; "which parameter has no available signatory" -> "...no available Approving Authority"
- L1588: "signed by an authorised signatory whose authorisation scope covers conditioning work" -> "signed by an Approving Authority whose..."; "the authorising signature" may stay (it names the signature, not the person).

DO NOT change (contrary to the reviewer's fix):
- M7-25 (L1075) and M7-26 (L1076): keep "**Authorised Signatory register**". This matches Part D M13-11 [MUST], Part D L319, appendix.md:128, section_1_foundation.md:211 and research_nabl_17025.md §7.2/P5. Renaming it here alone manufactures a new C-vs-D contradiction.

To close the WF-23 hole properly, add one row to §8.3 (section_2_workflow.md, near L531) so the register's name is legitimised rather than left ambiguous:
| Register of persons authorised to sign | **Authorised Signatory Register** (the accreditation body's own term) | `mst_signatory_scope` | Authorised Signatory Register | Signatory list, Approver list |
and append a note to the "Person authorised to sign" row: "Approving Authority is the role and the screen label; Authorised Signatory Register is the proper name of the register of such persons, retained because it is the accreditation body's term."

Optional completeness item (out of strict scope): Part A has no "Approving Authority" glossary entry - section_1_foundation.md:211 defines only "Authorised signatory", and the role table at L172 calls the person "Lab In-Charge (Scientist...) - Approving and Signing Authority". Adding an "Approving Authority" glossary entry cross-referring to it would prevent the same drift recurring.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
Independently confirmed on the substance. The quote exists verbatim at parts/section_3_functional_core.md:1013. Part B §8.3 (section_2_workflow.md:531) settles the concept "Person authorised to sign" as "**Approving Authority**", gives "Approving Authority" as the mandated on-screen label, and lists "Signatory (acceptable in speech)" in the do-not-call column; WF-23 (section_2_workflow.md:558) makes that a [MUST] with an acceptance check ("Open any five screens... the words match the table exactly"). Part C contains ZERO occurrences of "Approving Authority" and 11 of "authorised signator*". Decisively, Part D §M21.3 (section_4_functional_support.md:1356) defines the role code **AA = Approving Authority (Unit In-Charge)** and grants "Report - authorise and issue" to AA alone, so Part C's screens tables hand the signing screens to a role name that does not exist in the permission matrix. The reviewer also under-reported the problem: section_3_functional_core.md:1164 uses the bare word "Signatory (issue)", which is the term §8.3 explicitly forbids.

HOWEVER, the proposed fix is wrong on the register. "Authorised Signatory Register" is NOT a Part C-only deviation - it is the settled name across the whole document and is mandated as a [MUST] in Part D M13-11 ("The system shall hold an **Authorised Signatory Register** distinct from the general matrix"), used in Part D's register table at section_4_functional_support.md:319 (which correctly pairs register name "Authorised Signatory Register" with owner role "Approving Authority" - Part D distinguishes the two concepts properly), in appendix.md:128 (OPEN-Q-D7), in Part A's glossary at section_1_foundation.md:211, and in research_nabl_17025.md §7.2/P5 where it is the accreditation body's own term of art. §8.3 has no row for the register, so WF-23 does not reach the register's name. Renaming it to "Approving Authority register" in Part C M7-25/M7-26 as the fix instructs would create a fresh Part C vs Part D contradiction against a MUST requirement and would discard the NABL-facing term. The fix's caveat about lower-case quoting does not cover this, because M13-11 uses the Title-Case proper name.

So: the finding stands as a genuine, build-affecting role-label inconsistency (Major is correct - it hits both screen labels and permission-matrix role alignment, against an explicit MUST), but the fix must be narrowed to role labels only and must leave the register name alone.

====================================================================================================
FINDING 5/25  [Major] [contradiction]  (raised by consistency reviewer)
LOCATION: Part C M3-03 and M3-04 (section_3_functional_core.md L373–L374), M3-22, M3 rule 6, against Part B §8.3 (L517), §10.1 sub-sample row and OPEN-Q5 default
EXACT TEXT IN THE DOCUMENT:
| M3-03 | Where a sample type is flagged "has sub-units", the sample record additionally records the count and identity of its sub-units (bales, books, skeins, bobbins, cones, pieces) so that a lot of many bales is one sample with many sub-units, not many samples. | [MUST] |
PROBLEM:
Part C invents a third physical level called a "sub-unit", with its own numbers ("M3-04 | Sample numbers, sub-unit numbers and allocation numbers…"), its own labels ("M3-22 | Sub-unit labels are supported") and its own rule ("6. **Sub-units are not samples.**"). Section 2 has exactly two levels below the Sample and calls the lower one a Sub-sample: §8.3 settles "Physical piece consumed by a test | **Sub-sample** | `txn_subsample`", §10.1 defines only "Sub-sample number | parent sample number + `-P<2>`", and OPEN-Q5's recommended default says in terms that these same objects are sub-samples: "the **grading or settlement unit** is the Sample, and the physical bales, books, cones or skeins inside it are Sub-samples." The developer is being told to build a fourth entity and a number series that does not exist in the numbering scheme.
REQUIRED FIX:
Retire the word "sub-unit" for physical material across all of Part C, not just M3 and M4, and close the definitional and numbering gaps in the same edit. Note that "sub-unit" must remain reserved for its existing meaning — a CSTRI branch establishment, per `mst_customer_category` in section_5_nfr_plan.md L430 and L655 — so this is a rename, not a synonym swap.

1. Widen the target definition first, or the rename creates a fresh mismatch. In Part B §8.3 (section_2_workflow.md L517) change the concept cell from "Physical piece consumed by a test" to "Physical piece below the Sample — an as-received package (bale, book, cone, skein) or a prepared specimen"; add "Sub-unit" to that row's "Do not call it" column. Make the same change to the `txn_subsample` description in section_5_nfr_plan.md L481, and record the as-received/prepared distinction as values of the existing `prep_type` column (bale, book, cone, skein, bobbin, sizing_skein, panel, strip, swatch, aliquot) rather than as a new entity — which is what M4-10 already implies.

2. Rename all 14 occurrences in section_3_functional_core.md: L75 (M1-26 sample-type flag, "is this an entity with sub-samples (bales, books, skeins)" and its acceptance check), L373 (M3-03), L374 (M3-04), L395 (M3-15), L396 (M3-16), L402 (M3-22), L489-491 (registration field table: "Number of sub-samples", "Sub-sample type", "Sub-sample serial numbers"), L539 (M3 rule 6), L593 (M4-10, drop the word "sub-unit" from the tree and write "as-received package (bale, book)"), L660 (acceptance-weighing field table), L761 (field-visibility matrix), L1185 (M8-07 report content).

3. M3-03 becomes: "Where a sample type is flagged \"has sub-samples\", the sample record additionally records the count and identity of its sub-samples (bales, books, skeins, bobbins, cones, pieces) as rows in `txn_subsample` with the appropriate `prep_type`, numbered as the parent sample number + `-P<2>` per §10.1, so that a lot of many bales is one sample with many sub-samples, not many samples." Acceptance check: "Register a lot of 20 bales as one sample with 20 sub-sample rows of prep_type bale."

4. M3-04 becomes: "Sample numbers, sub-sample numbers and allocation numbers are all allocated by the server from configured series and are never re-used."

5. Delete M3-22 outright and add a cross-reference from M3 to M4-14, rather than renaming it. Renaming it produces two requirements for one deliverable at conflicting priorities — M3-22 [SHOULD] against M4-14 [MUST] (L597). If M3-22 is kept for locality, it must be re-priced to [MUST] and marked "see M4-14" so only one of them is normative.

6. M3 rule 6 becomes: "**Sub-samples are not samples.** A lot of 20 bales is one sample with 20 sub-samples of type bale." Keep the rest of the paragraph unchanged.

7. Reconcile the data model, which the reviewer did not reach: drop or demote `no_of_bales INT` and `no_of_books INT` from `txn_sample` (section_5_nfr_plan.md L480) to derived counts over `txn_subsample`, or state explicitly that they are denormalised caches — otherwise the count exists in two places and can disagree.

8. Widen the suffix. Once as-received packages share the `-P<nn>` series with prepared specimens, one raw-silk lot can need 22-30 bale rows plus 10 bobbins, 20 seriplane panels, 40 sizing skeins and 6 conditioned-mass skeins — over 99, which overflows §10.1's `-P<2>`. Change §10.1 L733 to `-P<3>` and `txn_subsample.suffix` accordingly, or the rename breaks the numbering scheme it is being aligned to.

9. Add a **Sub-sample** cross-reference and no **Sub-unit** entry to the Part A glossary, and if "sub-unit" is retained in its organisational sense, define that sense there explicitly so the two can never be confused again.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
Verified end to end; the finding holds, though one of its framings is overstated.

1. QUOTE IS VERBATIM. `parts/section_3_functional_core.md:373` matches the quoted M3-03 exactly, including the acceptance check "Register a lot of 20 bales as one sample with 20 sub-unit rows." M3-04 (L374), M3-22 (L402) and M3 rule 6 (L539) are all quoted accurately.

2. THE TERM IS PART C ONLY. "sub-unit" appears 14 times in `section_3_functional_core.md` — M1-26 (L75), M3-03/M3-04 (L373-374), M3-15 (L395), M3-16 (L396), M3-22 (L402), the registration field table (L489-491: "Number of sub-units", "Sub-unit type", "Sub-unit serial numbers"), M3 rule 6 (L539), M4-10 (L593), the acceptance-weighing field table (L660), the field-visibility matrix (L761) and M8-07 (L1185). It appears ZERO times in `section_2_workflow.md`. I searched sub-sample, subsample, specimen, partition, aliquot, sub-unit and subunit.

3. PART B REALLY DOES SETTLE THE OPPOSITE, AT MUST LEVEL. §8.3 L517 fixes one entity below the Sample: "Physical piece consumed by a test | **Sub-sample** | `txn_subsample`". §8.4 L549 orders a skein or panel called a "sample" to be renamed **Sub-sample**. §10.1 L733 defines only "Sub-sample number | parent sample number + `-P<2>`". OPEN-Q5 L563 is verbatim as quoted and classes bales, books, cones and skeins as Sub-samples. Decisively, **WF-23 [MUST]** at L558 states: "The words in the table in 8.3 are used identically in the user interface, in the database table and column names, in printed documents and in all project correspondence." So this is a breach of a MUST-priority requirement, not a style preference.

4. THE WORD IS ALREADY TAKEN, WITH A DIFFERENT MEANING. This is the strongest point and the reviewer missed it. In this same specification "sub-unit" means a CSTRI branch establishment: `section_5_nfr_plan.md:430` lists `sub-unit` as a value of `mst_customer_category` alongside Reeler, twister, exporter and student; L655 refers to "the zero-charge advisory order type for in-house research and sub-unit samples". The research briefs use it only that way (`research_silk_domain.md` L36, L273, L284, L318 — "CSTRI's sub-units are grouped as STSC…"). Part C therefore overloads one word to mean both a bale of silk and a sister laboratory, in a document whose §8.4 exists precisely to kill that kind of overload.

5. THE GLOSSARY CONFIRMS IT IS UNDEFINED. `section_1_foundation.md:198` promises "Every term used anywhere in this document is defined here in one plain sentence." It defines **Sub-sample** (L295), **Bale**, **Book**, **Skein**, **Bobbin**, and prefers "sub-sample" over "Aliquot" — there is no **Sub-unit** entry.

6. THE DATA MODEL PROVES THE RENAME IS LOAD-BEARING, NOT COSMETIC. `section_5_nfr_plan.md:480` gives `txn_sample` scalar columns `no_of_units INT`, `no_of_bales INT`, `no_of_books INT` and there is no sub-unit child table anywhere. But M3-03 demands "20 sub-unit rows", M3-04 demands server-allotted sub-unit numbers from a configured series, and M3-22 demands 20 bale labels each with a distinct suffix. Scalar counts cannot satisfy any of those. The only table that can is `txn_subsample` (L481: `parent_sample_id`, `suffix VARCHAR(8)`, `prep_type VARCHAR(20)`). So the developer is genuinely stuck.

7. WHERE THE REVIEWER OVERSTATES. M4-10 (L593) does reconcile the two vocabularies: it defines an "arbitrary-depth **sub-sample tree**: parent sample → sub-unit (bale, book) → drawn sample (skein) → prepared specimen…", with "Every node … formed from the parent's number plus a suffix". So sub-units are a LEVEL inside the sub-sample tree, not a fourth entity, and the reviewer's "being told to build a fourth entity" is wrong as stated. But M4-10 sits 220 lines after M3-03, is never cross-referenced from M3, and does not repair the two hard consequences: M3-04 still promises a series §10.1 does not define, and M3-22 [SHOULD] still duplicates M4-14 [MUST] (L597, "Sub-sample labels are printable in a smaller format") — the same deliverable at two different priorities. Substance is consistent everywhere (nobody would build 20 samples from 20 bales), so this is Major, not a Blocker.

8. THE PROPOSED FIX IS DIRECTIONALLY RIGHT BUT IMPRECISE ON THREE COUNTS: (a) "throughout M3 and M4" misses M1-26 (L75), M8-07 (L1185), and the three tables at L489-491, L660 and L761; (b) it leaves M3-22/M4-14 duplicating each other at conflicting priorities once both say "sub-sample"; (c) a straight rename collides with the definition it renames into — §8.3 and Part E both define a sub-sample as the piece "consumed by a test", and a bale in a 20-bale lot is not consumed by any test, so the concept cell has to be widened at the same time or a new mismatch is created. Corrected fix supplied.

====================================================================================================
FINDING 6/25  [Major] [contradiction]  (raised by consistency reviewer)
LOCATION: Part C M3-20 (section_3_functional_core.md L400) against Part B WF-32 (section_2_workflow.md L645) and Table A
EXACT TEXT IN THE DOCUMENT:
| M3-20 | A **barcode label** is printed for each physical item at the moment of acceptance, not at registration.
PROBLEM:
Section 2 prints the label one state earlier and says so twice: Table A's **Received** row "Prints the label", and WF-32 [MUST] "The label is printed only on entry to Received" with the acceptance check "Confirm no label prints for a sample in Expected." M3-20 [MUST] moves it to the acceptance decision, which happens after Received. Operationally this matters: §7.2 step 7 requires "The lab's own label is printed and fixed" as part of receipt, before the acceptance decision at step 8, and M5's over-labelling control ("over-label the sample with the laboratory's own code so the customer's mark is covered") depends on the label existing while the sample is still being inspected. Two [MUST] rules disagree on the trigger.
REQUIRED FIX:
Rewrite the whole of M3-20 (parts/section_3_functional_core.md:400) so the trigger matches WF-32 and the label content stays computable at that trigger:

| M3-20 | A **barcode label** is printed for each physical item on entry to state **Received**, as part of the receipt action, and never while the sample is still Expected (per WF-32 and §7.2 step 7). The label carries the laboratory's own sample number, sample type and unit count, received date, test short codes, an opaque barcode payload and a two-dimensional code. The **due date is printed only where it is already known**: because the turnaround clock starts at acceptance (M3-23) and the allocations that carry due dates are created at acceptance (WF-38, M3-24, M3-25), a label printed at receipt leaves the due-date area blank, and the due date appears on any reprint taken after the acceptance decision under M3-21. It must **not** carry the customer's name. | [MUST] | Print a label at receipt; the customer name is absent and the sample number is scannable. Confirm no label prints for a sample in Expected. Accept the sample, reprint, and confirm the due date now appears with the same payload and an incremented reprint counter. |

If the drafters prefer not to complicate the label content, the equally valid alternative is to delete "due date" from M3-20's field list altogether and rely on NFR-84 (section_5_nfr_plan.md:197), which already makes label content and field order configurable. Either way, do not leave "due date" as an unconditional field on a label printed at Received. No edit to Section 2 is needed: WF-32, Table A, the §7.2 step list and the §7.2 diagram already agree with each other.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
Confirmed on all points. The quoted M3-20 text exists verbatim at parts/section_3_functional_core.md:400 (SPEC.md:1645), and every Section 2 counter-statement exists verbatim too: WF-32 [MUST] at section_2_workflow.md:645 ("The label is printed only on entry to Received", acceptance check "Confirm no label prints for a sample in Expected"), Table A's Received row at L578 ("Prints the label") and its Expected row at L577 ("Does **not** print a label"), §7.2 step 7 at L23 ("The lab's own label is printed and fixed") with the acceptance decision at step 8, and the §7.2 ASCII flow at L103 which puts "LABEL PRINTED" inside box 7, one box before "8 ACCEPTANCE DECISION". So two [MUST] rules name different triggers for the same print event. I tested the charitable reading that M3-20's "acceptance" loosely means "taking physical custody": it fails, because M3-23 three rows below explicitly separates the two terms ("the moment of sample acceptance (not request date, not receipt)"), and because "registration" in M3 already means the receipt-entry step (M3-16 records date and time of receipt, mode of receipt, docket number). The dependency argument also holds and is stronger than the reviewer stated: M5's unavoidable-cases table (section_3_functional_core.md:786) requires the clerk "at receipt" to "over-label the sample with the laboratory's own code" and tick a mandatory confirmation, and M4's Receipt Condition and Acceptance screen (section_3_functional_core.md:638) looks a sample up by "scanning the label" while it "must exist and be in state Received" — both impossible if the label only prints at acceptance. Section 2's position is further corroborated outside Section 2 by NFR-01 (section_5_nfr_plan.md:43, timed "from opening the screen to a printed label" during registration), WF-22 (section_2_workflow.md:272) and AC-02 (section_5_nfr_plan.md:826), which queue labels as part of the registration/intake action. M3-20 is the only statement in the whole document placing the print at acceptance, so Section 2 wins, as the reviewer says. Severity Major is right: it is [MUST] vs [MUST] on an implementable trigger, and WF-32's own acceptance check would not catch a build that followed M3-20. The proposed fix is directionally correct but incomplete: it rewrites only the first clause and leaves M3-20 asserting that the label carries the **due date**, which does not exist on entry to Received — M3-23 starts the turnaround clock at acceptance, WF-38 creates the allocations only on entry to Accepted, M3-24 computes each allocation due date from the acceptance clock start, and M3-25 derives the sample due date from its allocations. That due-date field is almost certainly why M3-20 was drafted at acceptance in the first place, so a fix that ignores it swaps one contradiction for another. The corrected fix keeps the reviewer's trigger and also resolves the due date, using the existing reprint mechanism in M3-21. I also dropped the phrase "not at registration in Expected", which is misleading: for a walk-in, registration creates the sample directly in Received (M3-14) and does print the label (NFR-01, WF-22, AC-02); the thing to prohibit is printing while the sample is still Expected.

====================================================================================================
FINDING 7/25  [Major] [contradiction]  (raised by consistency reviewer)
LOCATION: Part C M3 screens table (section_3_functional_core.md L352), M3-13 (L388), M3-14 (L389) and Part A glossary "Contract review" against Part B §7.2 step 5 and WF-1/WF-2
EXACT TEXT IN THE DOCUMENT:
| Contract review and confirm | Unit Incharge / Section Head | The recorded decision to accept the work |
PROBLEM:
One act has three names and two different state verbs. Section 2 names it "**Request Review**" (§7.2 step 5, WF-1, WF-2, and §10.1's note "triggers a fresh Request Review") and its outcome sets the Test Request to state "**Accepted**". Part C calls the screen "Contract review and confirm", the record "a **contract review record**" (M3-13), a separate embedded step "Capability check" (M3-13: "identical logic to M2-06"), and the state change "**confirmed**"/"Confirmation" (M3-13, M3-14, M3-28, M3-30). Section 1's glossary reinforces the third name ("| **Contract review** | The recorded check, before work starts, that the laboratory has the method, the equipment, a competent person and the capacity…"). WF-1's acceptance test refuses receipt against a request "in state *Pending Review*", a state Part C's request state list spells "Pending review" — and Part C's list has no state at all for §7.2's third documented outcome, "**Decline** (with reason)", even though M21.3 grants a "Test request — decline" permission.
REQUIRED FIX:
Do five things, and drop three of the reviewer's items.

(1) NAMING — rename the review act to "Request Review" throughout Part C, including the two field labels the reviewer missed: section_3_functional_core.md L353 screen name → "Request Review and accept"; L388 M3-13 "a **contract review record**" → "a **Request Review** record naming the reviewer, the review date, the capability-check outcome and the review outcome (Accept / Decline / Clarify)" (this matches WF-1's four fields: reviewer, date, capability confirmed, remarks); L463 "Contract reviewed by" → "Reviewed by"; L464 "Contract review remarks" → "Review remarks". Leave M2-07 (L276) "the standard's requirement for a documented contract review" as-is or write "documented contract review (the Request Review)" — that sentence is deliberately citing the ISO clause.

(2) GLOSSARY — do not rename in place as proposed; a glossary is exactly where the standard's term belongs. Keep section_1_foundation.md:231 as a pointer — "**Contract review** | The ISO/IEC 17025 clause 7.1 name for what this document calls the **Request Review**." — and add a new "**Request Review**" entry carrying the current definition. The glossary currently has no Request Review entry at all.

(3) DO NOT blanket-replace "confirmed"/"Confirmation" with "accepted"/"Acceptance" as proposed. "Sample acceptance" is a separate, load-bearing act (Part B §7.2 step 8, sample states Received → Accepted in §9.1, and M3-23 "the turnaround clock starts at the moment of **sample acceptance** (not request date, not receipt)"). An unqualified "Acceptance" in M3-14/M3-28 would blur the two acts the document works hardest to keep apart. Instead state once in M3 that "confirming a Test Request is the transition Pending review → Accepted, i.e. the Accept outcome of the Request Review", and keep "confirm" as the screen verb (or use the qualified phrase "request acceptance"). If the words are changed anyway, the pass must cover every occurrence in M3 — L361, L385, L388, L389, L413, L420, L450, L460, L462, L463 and L534 — not the five rows listed; and M3-33 (L423) contains no "confirm"/"confirmation" at all, so remove it from the list.

(4) STATE LIST (L465) — add the missing outcome states without dropping the existing ones: "Draft / Pending review / Pending customer (clarification requested) / Accepted / Declined / On hold for payment / Cancelled / Closed". Do not collapse "On hold for payment" into a generic "On Hold" as proposed: M3-26 defines a specific Hold-for-Payment state with clock-pause and due-date-shift semantics, and M3-29 already supplies the general reason-coded hold at request, sample and allocation level.

(5) ADD THE MISSING DECLINE CAPABILITY (the part with real build impact) — add a row to the M3 screens table ("Request Review — decline | Unit Incharge / Section Head | Recorded refusal of the work, with reason") and a requirement, e.g. "M3-13a | A Request Review outcome of **Decline** sets the request to state **Declined**, requires a coded reason, creates no Samples and no Test Allocations, and fires the `REQ_DECLINED` notification. | [MUST] | Decline a request; no samples are created and the customer notification is logged with the reason." This is what Part D already assumes (permission at section_4_functional_support.md:1368, [MUST] notification at L1058) and what research_lims_reference.md:104 models.

DROP: the "Pending Review" capitalisation point (casing only), the claim that the embedded "Capability check" is a conflicting third name (WF-1 nests capability inside the Request Review record), and the M3-33 rename.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
Confirmed in substance, though the reviewer's framing is partly a misreading. The quote is verbatim (section_3_functional_core.md:353). Part B (§7.2 step 5 L21, WF-1 L251, WF-31 L644, L731), Part D (permissions L1368 "Test request — review and accept" / "Test request — decline"; notifications L1057-58 REQ_ACCEPTED / REQ_DECLINED) and Part A's role table (section_1_foundation.md:172 "Review and accept or decline a test request") all use Request Review + Accept/Decline. Part C's M3 is the lone outlier with "contract review" + "confirm", so the naming split is real — but on its own it is Minor: it is synonym-level (ISO/IEC 17025 cl. 7.1 genuinely calls it contract review, and the glossary defines it), §8.3's settled-vocabulary table has no entry for the review act so WF-23 does not bite, and Part C's state list at L465 DOES say "Accepted", so there is no head-on state contradiction. Two sub-claims fail outright: "Pending review" vs "Pending Review" is casing only, and the "Capability check" is not a conflicting third name — WF-1 itself puts "capability confirmed" inside the Request Review record, and Part C's field rows L462-464 map one-to-one onto WF-1's four fields (reviewer, date, capability confirmed, remarks), so Part C refines Part B rather than contradicting it. What earns Major is the sub-claim the reviewer buried at the end and got right: Part C's request state list (Draft / Pending review / Pending customer / Accepted / On hold for payment / Cancelled / Closed) has no Declined state, and M3 has no decline screen or requirement at all (grep for decline in Part C returns only M2-09, at enquiry level). Part D therefore grants a "Test request — decline" permission and mandates a [MUST] customer notification REQ_DECLINED with no state and no action anywhere in the module that owns Test Requests — an orphaned [MUST] path, not a cosmetic issue. The research brief's own state machine has it (research_lims_reference.md:104: PENDING_REVIEW → review_decline → DECLINED). The proposed fix, however, is imprecise and partly wrong, so I have corrected it.

====================================================================================================
FINDING 8/25  [Major] [contradiction]  (raised by consistency reviewer)
LOCATION: Part C OPEN-Q1 (section_3_functional_core.md L148) against Part B OPEN-Q14 (section_2_workflow.md L798) and Part D OPEN-Q18 (section_4_functional_support.md L943)
EXACT TEXT IN THE DOCUMENT:
seed it with the Karnataka registration found in the research pack, mark it **unconfirmed**, and require the answer in writing from CSTRI accounts before the first live invoice.
PROBLEM:
The same unresolved question — which GST registration the unit invoices under — is asked three times, and Part C's recommended default directly contradicts the other two. Part B OPEN-Q14 says "— *Recommended default:* do not guess. Obtain a copy of a recent real invoice from the unit before the invoice module is built", and Part D OPEN-Q18 says "obtain a copy of a recent real invoice from the unit before any invoice template is finalised. **Do not guess.**" Part C says to seed the Karnataka registration held by the parent institute. Seeding a Karnataka GSTIN for an Andhra Pradesh unit sets the intra-State/inter-State flag the wrong way for every local customer; "marked unconfirmed" does not stop it computing a tax split.
REQUIRED FIX:
Two changes, because the reviewer's fix puts the control in advisory prose where nothing is testable and leaves the "unconfirmed" mark still undefined.

(a) Replace Part C OPEN-Q1's recommended default (parts/section_3_functional_core.md:148, and the matching row at parts/appendix.md:106) with: "— *Recommended default:* build the tax registration as a configurable master with one row per registration and **seed no registration row**. Do not pre-populate the parent institute's Karnataka registration: the research pack records it as unverified and expressly warns against assuming which registration the unit bills under. Obtain the registration in writing, with a copy of a recent real invoice from the unit, per PLN-02 and PLN-06, before the first invoice is issued. Do not code either answer in. This is the same question as OPEN-Q-B14 and OPEN-Q-D18; answer it once."

(b) Add the missing in-system gate as a numbered, acceptance-checked requirement rather than leaving it in an open-question note -- either as a new M1-62a beside M1-62 (section_3_functional_core.md:146) or as an extension of M17-29 (section_4_functional_support.md:874): "The system shall refuse to issue any tax invoice, bill of supply or receipt voucher while the issuing unit has no linked tax registration, or while its linked registration is marked unconfirmed, naming the missing registration in the refusal message. Acceptance check: with no registration row present, attempt to issue an invoice; refused with a message naming the missing registration. Mark the registration unconfirmed and retry; still refused." This also gives the "unconfirmed" mark the defined behaviour it currently lacks anywhere in the document, and requires a confirmed_on / confirmed_by column on mst_gst_registration (section_5_nfr_plan.md:428), which does not exist today.

Also add a one-line cross-reference to OPEN-Q-B14 and OPEN-Q-D18 in Appendix B so the three registers read as one question with one answer.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
The quote is verbatim (parts/section_3_functional_core.md:148, repeated as OPEN-Q-C1 at parts/appendix.md:106), and the same question genuinely appears three times with no cross-reference: OPEN-Q-B14 (section_2_workflow.md:798), OPEN-Q-C1, OPEN-Q-D18 (section_4_functional_support.md:936). The reviewer overstates it as a direct contradiction: all three defaults actually agree on the substance (configurable master, one row per registration, state code drives the split, never a clerk's tick, answer required in writing before the first live invoice / before the template is frozen), and Part C adds "Do not code either answer in". The reviewer's "sets the intra-State/inter-State flag the wrong way" also mis-describes the design, since M1-62 (section_3_functional_core.md:146) and M17-29 (section_4_functional_support.md:874) both forbid a flag and require the split to be computed from supplier state vs place of supply. But a real defect survives: Part C alone tells the implementer to pre-populate the Karnataka row (state code 29) for an Andhra Pradesh unit, and I searched the whole specification -- nothing anywhere defines any behaviour for the "unconfirmed" mark. mst_gst_registration (section_5_nfr_plan.md:428) has no confirmation column and no gate, and no requirement blocks invoice issue on an unconfirmed registration, so the mark is decorative. The research pack supports the reviewer: research_india_commercial.md:194 says the GSTIN must be re-verified on the official portal, line 267 marks the two-GSTIN problem [ASSUMPTION but structurally critical], and line 276 says "Do not guess." Mitigations exist (PLN-02 and PLN-06 at section_5_nfr_plan.md:626, both [MUST] Phase 0, require a real tax invoice and the registration in writing before build; PLN-04 requires the register to be signed off), but they are process-only, live in Part E far from the seeding instruction, and do not stop the seeded row being used if Phase 0 slips. Severity Major stands: it sits on a [MUST] item whose failure mode is a wrong tax split on every local invoice, on the question the document itself calls the one with the widest effect, with no in-system control.

====================================================================================================
FINDING 9/25  [Major] [contradiction]  (raised by M1-M10 reviewer)
LOCATION: M10-09 (line 1547) against the M10 per-bale field table rows "Mass of one middle cotton band" and "Number of bands per book" (lines 1629–1630)
EXACT TEXT IN THE DOCUMENT:
the mass of the middle cotton bands of that same sample of books multiplied by the number of bands per book
PROBLEM:
The requirement and the field table specify two irreconcilable inputs for tare line 3. The standard's proforma takes an aggregate weighing of the middle cotton bands of the sampled books (M_c5) and multiplies by the bands per book (3M_c5), then divides the whole sampled-book tare by the number of books sampled. The field table instead collects the mass of one band. A developer who implements M10-09's sentence against the field table's input computes band tare for a single book and then attributes it to the whole sample of books, understating the band tare by a factor equal to the number of books sampled (typically five). Understated tare overstates net mass, which overstates oven-dry mass and therefore the conditioned weight — on a bale with 25 books and 3 bands of about 20 g each the error is roughly 1.2 kg of invoiced silk in the seller's favour, on a document two traders settle money with and which M10 itself says the customer will audit line by line.
REQUIRED FIX:
Same substance as proposed, tightened (the reviewer's wording says "replace the two field rows" and then keeps one of them, and it leaves the sampling basis of the aggregate weighing unvalidated).

In the M10 per-bale field table (parts/section_3_functional_core.md line 1628), replace only the single-band row with:

| Mass of the middle cotton bands of the sampled books | Decimal (18,3) kg | Yes | Zero or more | Tare line 3 input: one band weighed from each of the sampled books and weighed together as one aggregate, not a single band. Must be the same books as tare line 2. |

Leave line 1629 ("Number of bands per book", tare line 3 multiplier) and line 1627 ("Number of books sampled for tare", the divisor at tare line 5) unchanged, and add a validation on the divisor row that the same book sample underlies tare lines 2 and 3.

In M10-09 (line 1546), keep the seven-line narrative and append the arithmetic as named stored values so the multiplier chain cannot be misread: "tare_sampled_books = mass_papers_labels_sampled_books + (mass_bands_sampled_books x bands_per_book); tare_one_book = tare_sampled_books / books_sampled; tare_all_books = tare_one_book x books_in_this_bale; total_tare_bale = tare_all_books + shirt_mass + sum(other bale-level components) + excess_lacing_mass; net_mass_bale = gross_mass_bale - total_tare_bale. The divisor books_sampled is applied once, at tare line 5, and applies to tare lines 2 and 3 together."

Extend M10-08's acceptance check with the reviewer's worked example, which I have checked: "Enter shirt 0.900 kg, papers and labels of 5 sampled books 0.250 kg, aggregate mass of the middle bands of those 5 books 0.300 kg, 3 bands per book, 5 books sampled, 25 books in the bale; the system computes tare of the sampled books 1.150 kg, tare of one book 0.230 kg, tare of all books 5.750 kg and total tare 6.650 kg, and a hand calculation matches every line."
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
Verified on all four checks.

1. The quote is real and verbatim. M10-09 at D:\Prashant_WorkSpace\TTL Software\parts\section_3_functional_core.md line 1546 (assembled: D:\Prashant_WorkSpace\TTL Software\SPEC.md line 2791) reads "...the mass of the middle cotton bands of that same sample of books multiplied by the number of bands per book; the tare of that sample of books; the tare of one book, obtained by division; the tare of all the books in the bale, obtained by multiplication; and the total tare of the bale." The two field rows are also verbatim, at section_3_functional_core.md lines 1628–1629 (SPEC.md 2873–2874): "| Mass of one middle cotton band | Decimal (18,3) kg | Yes | Zero or more | Tare line 3 input |" and "| Number of bands per book | Integer | Yes | One or more | Tare line 3 multiplier |". The reviewer's line numbers are off by one against the part file, which is immaterial.

2. The two inputs really are different quantities, and the requirement side is the correct one. The research brief at D:\Prashant_WorkSpace\TTL Software\parts\research_silk_domain.md line 207 gives the standard's Section II structure as "3. Mass of middle cotton bands of 5 books x 3 (3M_c5)", then "4. Tare of 5 books = (M_p5 + 3M_c5)", "5. Tare of one book = (M_p5 + 3M_c5)/5", marked [F, S4] (verified). So tare line 3 is an aggregate weighing across the sampled books, and the divisor at line 5 is applied to a line 4 in which both line 2 and line 3 are 5-book quantities.

3. The mismatch produces the error the reviewer describes, and it is forced by the field table's own notes. Line 2 is explicitly aggregate ("papers and labels of the sampled books"), line 4 is "Tare of the sampled books", and "Number of books sampled for tare" is annotated "The divisor". The only multiplier the table names for line 3 is bands per book. Following the table literally: line 3 = one_band x 3 (one book's bands), line 4 = papers_of_5_books + one_book's_bands, line 5 = line 4 / 5, so the band contribution per book comes out at 0.6 x one_band instead of the correct 3 x one_band — understated by exactly the number of books sampled. There is no second multiplier anywhere that rescues it. The finding is also not merely an arithmetic slip: a single-band weighing is a different bench procedure from the standard's proforma, which M10-29 requires the certificate to reproduce.

4. Not handled elsewhere. I checked every "tare" occurrence in SPEC.md (lines 129, 2762, 2784–2794, 2853, 2862–2882, 2915, 5801) and all "band" occurrences. M10-08, M10-11, M10-25, M10-29 and M10 rules 2, 3 and 8 all restate that the build-up must be itemised, shown and auditable, but none of them defines the line 3 input, so nothing disambiguates the contradiction.

5. The proposed fix is technically correct. Its five formulas reproduce the standard exactly (M_p5 + 3M_c5, /5, xn, + shirt + other components + excess lacing, gross - tare), are consistent with M10-11's net-mass rule and M10-10's lacing exemption, and still leave seven displayed lines so M10-09's acceptance check ("all seven lines appear") is unaffected. I re-did the proposed acceptance numbers: 0.250 + (0.300 x 3) = 1.150; 1.150 / 5 = 0.230; 0.230 x 25 = 5.750; 5.750 + 0.900 = 6.650. All four figures are right.

Where I part company with the reviewer is severity. The authoritative MUST requirement, M10-09, is already correct and matches the verified brief; the defect is confined to one row of a supporting field list, and the document carries two existing mitigations that would very likely catch the numeric consequence before go-live — M10-08's acceptance check ("the computed total tare matches a hand calculation line for line") and M10 rule 8, which requires twenty historical manual worksheets to be reproduced exactly as a regression suite. The reviewer's 20 g band and the 1.2 kg figure are also illustrative rather than sourced; nothing in the briefs states a band mass. That makes this a Major internal inconsistency that must be corrected, not a Blocker.

====================================================================================================
FINDING 10/25  [Major] [buildability]  (raised by M1-M10 reviewer)
LOCATION: M10-17, M10-18, M10-20 (lines 1560–1563); OPEN-Q11 (line 1580); M10-29 (line 1586)
EXACT TEXT IN THE DOCUMENT:
The dry-to-constant-mass rule is implemented as **configuration, not code**: an initial drying period, then a shorter period, and a convergence threshold expressed as a percentage of the previous weighing, with continued weighings at the shorter interval until the loss between successive weighings falls at or below the threshold.
PROBLEM:
Every numeric constant in M10 is declared to be configuration and then never given a seed value, so the configuration cannot be created. The oven temperature is nowhere stated (140 °C), the initial and subsequent drying periods are nowhere stated (15 minutes then 5 minutes), the convergence threshold is nowhere stated (0.25 per cent), the set-agreement tolerance in M10-20 is only ever "the configured tolerance" (0.5 per cent), the balance capacities and least counts that M10-07 requires to be printed are nowhere stated (platform 100 kg / 0.1 kg, skein 1 kg / 0.1 g, in-oven 0.1 g), and the "material" gap in M10-14 has no threshold. M10 also never names the standard once, while M10-29 requires "a bracketed method attribution line naming the standard and its part" — the developer cannot produce the line and the scientist cannot check any of the values against the source. OPEN-Q11 asks the scientist to confirm values that the document never states, so the question cannot be answered as posed.
REQUIRED FIX:
Do not use the reviewer's table as drafted — it invents two values, duplicates two that are already seeded, and mis-homes a third.

1. Add a seed table in M10 immediately after M10-17, headed "Seeded method parameters for IS 15090 (Part 3), held per method version under M14-06, effective date 01-Apr-2026, every row marked unconfirmed pending OPEN-Q11":
   - Oven temperature: 140 °C. Do NOT write "± 2". The standard prescribes no tolerance; ± 2 is the standard-atmosphere tolerance (27 ± 2 °C / 65 ± 2 % RH, M21.11) and copying it here would fabricate a requirement. The oven's own control tolerance comes from its calibration certificate.
   - First drying period: 15 minutes.
   - Subsequent drying period: 5 minutes, repeating.
   - Convergence threshold: 0.25 per cent of the previous weighing.
   - Set-agreement tolerance: 0.50 percentage points, absolute difference between m1 and m2.
   - Skein lacing tare exemption (M10-10): 1 metre per skein.
   - Tare sample (M10-09): 5 books, 3 middle cotton bands per book.
   Add a note that the drying periods are per method version because the conditioned-size test (IS 15090 Part 6) uses a 10-minute first dry — a single global drying period would be wrong for Part 6.

2. Do NOT restate the regain rate or the six-skein/two-set-of-three split in the new table. Both are already seeded (M10-23; M10-13 and its acceptance check). Cross-reference them.

3. Do NOT seed balance capacities as configuration — that contradicts M10-07 and the M11 equipment master. Instead add to M10-07 the standard's minimum apparatus specification as a selection-time validation: platform balance of at least 100 kg capacity with least count 0.1 kg; skein balance 1 kg with least count 0.1 g; in-oven balance reading to 0.1 g. The system warns when an instrument selected for conditioning work falls short.

4. For M10-14's "materially", do not invent 30 minutes — it has no basis in the standard (which says "at the same time") and no support in the briefs. Seed `skein_draw_gap_warn_minutes` at 120 so the requirement agrees with its own acceptance check ("a draw two hours after the bale weighing; a warning appears"), or tighten both together. Mark it explicitly as a local operating assumption with no standard basis and add it to OPEN-Q11.

5. For M10-29, do not hard-code the attribution line as literal template text — that breaks M14-12, which requires the edition printed to be the one in force at the date of test. Instead: seed the Method Master with `IS 15090 (Part 3):2002`, and state that M10-29's bracketed line renders the proforma's own wording, "[Conducted in accordance with IS 15090 (Part 3) Raw silk — Grading and methods of tests: Part 3 Determination of conditioned mass]" — note the proforma carries no year inside the bracket (research_silk_domain.md:191) — with the edition supplied from the method version per M10-30.

6. Add every M10 parameter above to the M21.11 configuration and feature-switch register (parts\section_4_functional_support.md:1578), which currently jumps from M1 to M11 and lists no conditioning parameter at all.

7. Restate OPEN-Q11 as: "Confirm or correct each seeded value in the M10 parameter table against a real Dharmavaram conditioning worksheet." Fold OPEN-Q9's request for a scanned certificate into the same ask.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
VERIFIED, but the headline is overstated and the fix is imprecise.

Quote is genuine: `D:\Prashant_WorkSpace\TTL Software\parts\section_3_functional_core.md:1559` (SPEC.md:2804), verbatim.

CONFIRMED GAP. I grepped every authored part (section_1..5, front_matter, appendix) for the values and their synonyms. Absent everywhere:
- Oven temperature: `140` appears only in `research_silk_domain.md` (lines 217, 384, 391, 437) and, in SPEC.md, only as a citation index number (appendix.md:344). Never as a drying temperature.
- First/subsequent drying periods (15 min / 5 min): nowhere in any authored section.
- Convergence threshold (0.25 %): `0.25` returns nothing in section_3; nothing anywhere outside the briefs.
- Set-agreement tolerance (0.5 %): M10-20 says only "the configured tolerance". The nearest thing is M6-23's illustrative "0.7 percent against a 0.5 percent rule" (section_3:896) — a generic example, not a seed for M10.
- IS 15090 Part 3: `Part 3` appears in NO authored section. Only Part 5 / Pt XI are named (section_3:347, 784, 1327, 3366). So M10-29's required "bracketed method attribution line naming the standard and its part" has no source in the document, and OPEN-Q11 asks the scientist to confirm "the standard's convergence threshold and its set agreement tolerance" without ever stating them — the question genuinely cannot be answered as posed.
- The document's only configuration-defaults register, "M21.11 Configuration and feature switches introduced by this document" (`parts\section_4_functional_support.md:1578`), runs from M11 to M21 and contains no conditioning parameter at all. So the values are not seeded in M10 and not seeded in the register either. That is the strongest form of the finding and the reviewer missed it.

Research briefs support the values at the highest confidence: `research_silk_domain.md:219` "platform balance 100 kg capacity, least count 0.1 kg; skein balance 1 kg capacity, least count 0.1 g; conditioning oven at 140 °C with in-chamber balance reading to 0.1 g" **[F, S4]**; line 397 the 0.5 % two-set rule **[F, S4]**; line 398 "Dry 15 min ... another 5 min ... ≤ 0.25% ... 5-min intervals" **[F, S4]**; line 191 the exact proforma attribution line. Nothing is inferred or unverified. The spec dropped verified research.

The reviewer also under-reported: M10-09's "a stated sample of books" (5 books) and "bands per book" (3), and M10-10's "the length per skein that the standard permits" (1 metre) are equally unseeded.

WHERE THE REVIEWER IS WRONG:
1. "Every numeric constant in M10 is declared to be configuration and then never given a seed value" is false. M10-23 explicitly seeds the regain rate "at **eleven percent** of the oven-dry mass". Six skeins in two sets of three is stated three times — the M10 screens table, M10-13's acceptance check, and OPEN-Q11's recommended default. The reviewer's own fix table re-seeds both, which would create a second source of truth for values already fixed.
2. Balance capacities/least counts are not a missing constant. M10-07 states they are "held on the equipment record", i.e. per-instrument master data the lab enters from its own calibration certificates (M11). An SRS cannot seed the capacity of a balance it has never seen. What is legitimately missing is the standard's *minimum* apparatus spec as a validation.
3. "The developer cannot produce the line" overstates M10-29. M10-30 already requires "the method identification with its version" on the certificate, and M14-02/M14-12 make designation + edition a Method Master field printed as at the date of test. The line is data-driven and buildable; the gap is that the document never names the part for the scientist to check.

SEVERITY: Major, not Blocker. Nothing in M10 is unbuildable — the loop structure, the hard block on a non-converged mass, the two-set block, the seven-step formula chain, the data model and the historical test vectors (M10-27) are all fully specified. What is missing is go-live seed data for five parameters, which already has a named owner in OPEN-Q11 and a module that OPEN-Q9 recommends shipping inactive. It delays configuration, not development.

====================================================================================================
FINDING 11/25  [Major] [contradiction]  (raised by M1-M10 reviewer)
LOCATION: M1-13 (line 52) against the M1 Customer create/edit field table (lines 196–228)
EXACT TEXT IN THE DOCUMENT:
The system supports a **walk-in / one-time customer** created with name and mobile number only, marked `is_provisional = Yes`, which can be completed later without changing the customer identifier.
PROBLEM:
The field table marks seventeen fields Mandatory = Yes, including Address line 1, Village/town, District, State, PIN code, Country, Preferred language, Credit terms, Advance required, Is blacklisted, Extra confidentiality requested, Privacy notice version shown, both consent flags and Relationship with laboratory staff declared. A developer cannot satisfy M1-13 and the field table at the same time: either the two-field counter creation is impossible, or the mandatory column is decorative and every field becomes optional. This is the screen used for the walk-in traffic that the research says arrives at the counter with silk in hand, so the wrong choice blocks the front desk on day one.
REQUIRED FIX:
Fix the field table and M1-13 together, and state the completion gate in terms the money module already uses.

1. Add a column "Required at provisional save?" to the M1 field table (parts/section_3_functional_core.md:194) with three values, so the Mandatory column keeps meaning for the full form:
   - **Yes (clerk must enter)** — exactly three fields: Customer name, Primary mobile, Customer category. Add "Individual" and "Other" as acceptable single-click values so the third field costs one tap at the counter.
   - **Default applies** — every Mandatory = Yes field that already declares a default or auto-fill: Concession category (None), Is registered business entity (No), Country (India), Preferred language (English), Credit terms (Advance required), Advance required (derived), Is blacklisted (No), Extra confidentiality requested (No), Privacy notice version shown (auto, current version), Consent — status messages (No), Consent — inclusion in published statistics (No), Relationship with laboratory staff declared (No), Is active (Yes). These are stored at their defaults, not left null.
   - **No** — Address line 1, Address line 2, Village/town, District, State, PIN code, GSTIN, PAN, Trade name, Alternate phone, E-mail, Credit limit, Blacklist reason, Linked host ERP customer identifier, Remarks.

2. Amend M1-13 (parts/section_3_functional_core.md:52) to read: "The system supports a walk-in / one-time customer created from a short form carrying only the fields marked 'Required at provisional save' in the M1 field table (customer name, primary mobile, customer category); all other mandatory fields are stored at their declared defaults. The record is marked `is_provisional = Yes`. A Test Request may be confirmed, a sample registered, tested and reported, and a Money Receipt (M17-16) issued against a provisional customer. A tax invoice, bill of supply or receipt voucher may not be issued until State is present — because the place-of-supply split in M1-62 and M17-29 is computed from it — and, where the document value exceeds `unregistered_recipient_value_threshold` (M17-27), until Address line 1, Village/town, District and PIN code are present. Completing the record clears `is_provisional` and never changes the customer identifier or customer code."

3. Replace the acceptance check with: "Save a customer with name, mobile and category only; sample registration, acceptance, testing and the Money Receipt all succeed. Attempt a tax invoice; it is blocked with the missing fields named. Fill the address and state; the same customer code is retained, `is_provisional` clears, and the invoice issues with the correct intra-state or inter-state split."

4. Consistency follow-through: add `is_provisional BOOLEAN` to the `mst_customer` column list in the database design (parts/section_5_nfr_plan.md:429), where it is currently missing, so the DB section does not contradict the new rule.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
The quoted text is verbatim (parts/section_3_functional_core.md:52; SPEC.md:1297) and a real internal inconsistency exists with the M1 Customer create/edit field table (parts/section_3_functional_core.md:192-228; SPEC.md:1437-1473). The field table marks Address line 1, Village/town, District, State, PIN code and Customer category as Mandatory = Yes with "Not blank" validation and no default, so the table as written cannot be satisfied by M1-13's "name and mobile number only" save, and M1-10's acceptance check ("Create a customer with every field populated") sits on the same screen. The document flags that a short form exists (field-table row "Is provisional (walk-in) | Auto | Set Yes when created with the short form", and M1 rule 2 "What may be filled progressively") but never states the short form's field set, which mandatory fields are relaxed while provisional, or what event forces completion — greps for "short form", "provisional", "walk-in" and "quick create" across all parts turn up nothing further. However the reviewer's framing is inflated on the facts: the column actually holds 21 Yes values, not seventeen, and most fields the reviewer names (Preferred language, Credit terms, Advance required, Is blacklisted, Extra confidentiality, Privacy notice version, both consents, Relationship declared, Country, Concession category, Is registered business entity, Is active) carry declared defaults or auto-fill in the Validation column, so they never block a two-field save. The reviewer also missed Customer category, the one non-defaultable pick-list that genuinely does block, and missed the real downstream pinch point: M17-27 requires the recipient's address and state name/code on a tax invoice above the configured unregistered-recipient threshold, and M1-62/M17-29 derive the CGST-SGST versus IGST split from the place-of-supply state. The proposed fix is directionally right but defective: it omits Customer category (needed by the M17-42 per-category payment-release rule and the M20 monthly return), lists auto/defaulted fields as mandatory-when-provisional where nothing is typed, and its blanket "No for every other field" would leave Concession category, Credit terms, Advance required, Is blacklisted and the consent flags blank, breaking rate resolution (M1 rule 5), the payment hold (M3-27, M17-42) and the consent record (M1-18, which requires the defaults to be recorded as No, not null); its absolute invoice block is stricter than M17-27 needs and, read literally, would stop the counter issuing any tax document for a same-day cash walk-in.

====================================================================================================
FINDING 12/25  [Major] [contradiction]  (raised by M1-M10 reviewer)
LOCATION: M6-19 and its acceptance check (line 887); M6 field table reading rows (lines 968–973) and specimen row (line 958)
EXACT TEXT IN THE DOCUMENT:
The specimens consumed are recorded per activity, linked to the sub-sample tree from M4, so an auditor can see which physical bobbin produced which reading.
PROBLEM:
The requirement records specimens at activity (allocation) level, but its own acceptance check demands per-reading resolution — "Open a reading; the specimen identifier it came from is shown" — and the field table has no specimen column on the reading row, only a multi-select "Specimen / sub-sample used" on the allocation. With activity-level linkage a size test's 40 readings all point at all 10 bobbins, so nobody can tell which bobbin produced a deviant sizing skein, which is exactly what is needed to decide whether one bobbin must be re-wound, to attribute the 2 evenness panels per bobbin, and to defend the maximum-deviation figure taken from the 4 coarsest and 4 finest skeins. The acceptance check therefore does not verify the requirement it sits next to, and the requirement does not deliver the traceability its own sentence claims.
REQUIRED FIX:
Keep the reviewer's direction but make the scoping consistent and carry the change into the data model, otherwise the new requirement contradicts SPEC.md 25.2.

1. Replace M6-19 with: "Where the method's sample-draw rule names a specimen level (sizing skein, seriplane panel, test piece, bobbin, skein), **each reading row carries the identifier of the single specimen it was taken from**, and that identifier is mandatory. The specimen must be one of those recorded against the allocation and must belong to this sample's sub-sample tree from M4. The system validates the specimens used across a parameter's readings against the draw rule's expected shape recorded under M4-11 -- for example 40 sizing-skein readings resolving to 4 skeins from each of 10 bobbins -- and blocks submission where the shape does not match, naming the discrepancy. Where a parameter is a whole-sample judgement with no specimen basis (the visual and tactual ratings in M6-09), no per-reading specimen is required. Acceptance check: open reading 27 of a size test; it names one sizing skein and its ancestors (parent bobbin, parent skein) from the M4 tree, and a per-bobbin summary of the 40 readings is available. Enter 40 readings drawn from 9 bobbins instead of 10; submission is blocked naming the shape mismatch."

2. In the M6 field table, add to the **reading rows** block (after "Reading unit"): "| Specimen used (per row) | Lookup | Yes where the method's draw rule names a specimen level | Must be one of the allocation's specimens and belong to this sample's tree; draw-rule shape validated at submission | Links this one reading to one physical item |". Amend the Notes cell on the existing allocation-level "Specimen / sub-sample used" row from "Links readings to physical material" to "The permitted set for this allocation; the per-row Specimen used field selects from it" so the two rows are not read as redundant.

3. **Data model (SPEC.md section 25.2, and the same table in parts/section_5_nfr_plan.md) -- omitted from the reviewer's fix and required for it to be buildable:** add `subsample_id NULL` to `txn_observation` (nullable to accommodate the M6-09 whole-sample ratings), with an index on `subsample_id` to support the per-bobbin summary and the reverse "which readings came from this specimen" query. Also replace the single `txn_sample_test.subsample_id NULL` with a `txn_sample_test_subsample` link table (`sample_test_id`, `subsample_id`, unique on the pair), because a single nullable FK cannot represent the "Lookup (multi)" field or the many-to-many relationship that M4-12 and M4 rule 6 ("the link between specimen and allocation is many-to-many. A one-specimen-one-test model will fail on the first grading job") explicitly require.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
Independently confirmed at four levels. (1) The quoted text is verbatim at parts/section_3_functional_core.md:887 (SPEC.md:2132), including the acceptance check "Open a reading; the specimen identifier it came from is shown." (2) The contradiction is real in context: the requirement scopes specimen capture to "per activity", while its own acceptance check demands per-reading resolution. (3) The M6 field table places "Specimen / sub-sample used | Lookup (multi) | Yes" in the allocation-level block; the per-row block contains only Reading number, Raw reading value, Reading unit, Reading excluded, Exclusion reason and Break cause. There is no per-row specimen column, so the acceptance check cannot be executed on the specified screen. (4) The gap is not covered under any other name. I searched "specimen", "sub-sample"/"subsample", "bobbin", "draw rule"/"sample-draw", and regex combinations of per-reading x specimen across the whole 152k-word document: every specimen-to-test link is activity-level (M4-12's check is "Link ten bobbins to four allocations; each allocation shows the same ten bobbin identifiers"; M4-11 records the draw rule at sub-sample-creation level). Crucially, and missed by the reviewer, the data model confirms the defect and defeats the proposed fix: in SPEC.md 25.2 the txn_observation table ("Raw readings. Append-only.") has sample_test_id, parameter_id, reading_no, raw_value, uom_id, equipment_id, consumable_lot_id, observed_at, observed_by_personnel_id, entry_mode, source_file_attachment_id, is_excluded, exclusion_reason_id, excluded_by_personnel_id, supersedes_observation_id -- and no subsample_id. "subsample" occurs only twice in the entire data model: txn_subsample itself and txn_sample_test.subsample_id NULL, a single nullable FK that cannot even hold the ten bobbins the allocation-level multi-select promises. The acceptance check is therefore unimplementable against the specified schema, not merely unverified. The domain need is [F]-verified rather than inferred: parts/research_silk_domain.md:383 ("4 sizing skeins of 450 m from every bobbin = 40 sizing skeins. Each sizing skein weighed individually"), :385 ("20 panels from 10 bobbins, 2 panels per bobbin"), :399 (max deviation from the 4 coarsest and 4 finest, [F, S4]), and :428 ([F, S4]) which states the requirement outright: "each level generates its own readings. You need at least: lot -> sample_item -> test_run -> reading ... A flat 'sample has result' schema will fail on day one." Activity-level linkage is exactly the shape the brief warns against. One reviewer rationale is overstated and I discount it: computing maximum deviation from the 4 coarsest and 4 finest needs only sorted readings, not specimen identity. The per-bobbin attribution argument (deviant sizing skein -> which bobbin -> re-wind decision; attributing the 2 evenness panels per bobbin) is sound and carries the finding on its own. Severity Major is appropriate: it is one requirement, one table row and one schema field, not a structural failure of the spec, but it would be built wrong as written. The fix needs correcting because it leaves the schema untouched (creating a new requirement/data-model contradiction) and opens with an unconditional "Every reading row carries..." that clashes with its own conditional-mandatory clause and with M6-09's whole-sample visual and tactual ratings.

====================================================================================================
FINDING 13/25  [Major] [contradiction]  (raised by M1-M10 reviewer)
LOCATION: M3, rules and edge cases item 4 (line 537); M2-06(e) (line 275)
EXACT TEXT IN THE DOCUMENT:
The registration screen compares the quantity received against the sum of the minimum quantities of the requested tests and warns.
PROBLEM:
This contradicts M4-12 and M4 rules item 6, which correctly state that downstream tests share intermediate specimens: the 10 bobbins from the winding test feed size, evenness, tenacity and cohesion, and the same 20 seriplane panels serve evenness, cleanness and neatness. Summing per-test minima on a BIS grading order therefore demands several times the material the work actually consumes, so every grading job raises a false insufficient-quantity warning, the clerk learns to click through it, and the control is dead when a genuine shortfall arrives. The requirement also conflates two different masters: M1-26's per-sample-type "minimum quantity" and M1-29's "minimum chargeable quantity", which are a technical and a commercial figure respectively.
REQUIRED FIX:
Keep the reviewer's rewrite of M3 rules item 4 and the seeded draw rules (all [F]-verified at research_silk_domain.md:381, :382, :303, :107), but change three things.

1. Do NOT rewrite M2-06(e). M2-06 is a five-check, per-requested-test grid whose checks each render their own green/amber/red with a one-line reason and are stored per-check by M2-07; an order-level aggregate figure does not fit inside a per-test check and would re-contradict WF-34's per-test gate. Instead leave (e) per-test, restated to name its source: "is the requested sample type permitted for that test, and is the intended quantity at or above the technical minimum sample quantity on the test's Active method revision (M14-10) — not the minimum chargeable quantity of M1-29". Then add an order-level check following the M2-08 precedent, as M2-08a: "A seventh capability check covers material sufficiency for the request as a whole. The system resolves the sample-draw chains of all requested tests, deduplicates shared chains, sums only across independent chains, and compares the total with the intended or received quantity, flagging amber when short."

2. Reconcile the minimum-quantity masters rather than adding a fifth. Put the sample-draw structure in ONE place — M14-10 already declares "minimum sample quantity and the number of sub-samples or specimens" on the method version, so extend M14-10 with "and the specimen level the draw produces plus the method whose output it draws from, so shared specimen chains are resolvable", and make M1-36's "sample draw rule" a cross-reference to M14-10 instead of a parallel definition. State explicitly in M1-29 that "minimum chargeable quantity" is commercial only and must never drive a sufficiency check.

3. Fix WF-34 too — the reviewer's fix omits it, leaving the contradiction half-resolved. WF-34 currently cites "the minimum quantity recorded on the test catalogue item for that sample type", a field that does not exist in M1-29. Restate it as: "Received to Accepted is permitted only if the deduplicated required-quantity figure (M2-08a) is met, and, per test, the technical minimum sample quantity on that test's Active method revision is satisfied." Keep the reviewer's acceptance check, and add one for WF-34: "Receive 10 skeins against a full BIS grading order; Accept is available. Receive 3 skeins; Accept is unavailable."
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
Confirmed. The quoted text exists verbatim at parts/section_3_functional_core.md:537 (SPEC.md:1782). The contradiction is real and stronger than the reviewer argued: (a) M4-12 (SPEC.md:1840), M4 rule 6 (SPEC.md:1922) and M1-33 establish that downstream grading tests consume shared intermediates; (b) research_silk_domain.md:429 marks this [F, S4] — "a test order for grading is a dependency graph with shared intermediates, not independent line items"; (c) research_silk_domain.md:381-389 show the draws sit at DIFFERENT specimen levels (winding 10 skeins to 10 bobbins; size deviation the same 10 bobbins to 40/80 sizing skeins; evenness 20 panels from those bobbins; cleanness and neatness re-use the same 20 panels; cohesion 10 test pieces from the same bobbins), so summing per-test minima is not merely inflated but dimensionally incoherent — skeins plus bobbins plus panels compared against a received quantity in skeins. The consequence is also worse than the reviewer stated: item 4 is a hard gate, not a soft warning, so a false trigger on every BIS grading order forces either a bogus amendment or a fabricated "customer consultation accepting the risk" record which auto-adds a disclaimer to the report, polluting the compliance record on every grading certificate. I additionally confirmed a second contradiction the reviewer missed: WF-34 (SPEC.md:1089, MUST with acceptance check) gates Received to Accepted on "for every requested test, the received quantity meets or exceeds the minimum quantity" — a per-test maximum, not a sum. So the spec computes material sufficiency twice with two different arithmetics at two different gates. The master conflation is real and involves four fields, not two: M1-26 (sample type minimum quantity), M1-29 (minimum CHARGEABLE quantity, commercial), M14-10 at section_4_functional_support.md:449 (method version records minimum sample quantity and number of sub-samples or specimens), and M17-04/M17-05 (rate-card minimum sample size); WF-34 meanwhile cites "the minimum quantity recorded on the test catalogue item for that sample type", a field that exists nowhere — M1-29 carries applicable sample types and minimum chargeable quantity only. Not handled elsewhere: no numbered M3 requirement covers sufficiency (M3-15 to M3-20 are registration fields), no OPEN-Q defers it, and searches for deduplicate/double-count/required quantity/material required/aggregate minimum return nothing. Severity Major is correct. The fix is right in substance and its seed numbers are all [F]-verified, but two parts are imprecise, so I have corrected it.

====================================================================================================
FINDING 14/25  [Major] [buildability]  (raised by M1-M10 reviewer)
LOCATION: M6-41 (line 924)
EXACT TEXT IN THE DOCUMENT:
Target entry speed for the dominant test: a trained tester must be able to complete the readings for **one lot in under twenty seconds** and a batch of thirty lots in under twelve minutes, excluding the physical weighing.
PROBLEM:
The target was written without reference to the number of readings, and for the dominant test it is physically unattainable. The research is explicit that the Limited test weighs twenty test skeins per lot to produce one lot result, so twenty seconds is one second per four- or five-digit weight including navigation — about five keystrokes a second sustained, against roughly 2.5 to 3.5 for trained numeric-keypad entry. The requirement's failure clause, "the target is met or the screen is redesigned", puts the developer on the hook for human typing speed, and no screen design can discharge it. It is also internally inconsistent: thirty lots at under twenty seconds each is under ten minutes, not twelve. The consequence of an unachievable [MUST] is that a real and important requirement gets waived along with the number.
REQUIRED FIX:
Align M6-41 with the document's existing authoritative numbers instead of inventing new ones, and leave the instrument import alone.

Replace M6-41 with:

"| M6-41 | Entry speed for the dominant test (Limited test, twenty readings per lot). The authoritative target is Section 1 goal 3 and success measure S3: a trained tester completes one lot's twenty readings in **under ninety seconds**, and a batch of thirty lots in **under forty-five minutes**, excluding the physical weighing. Because raw typing time is a property of the tester and not of the software, the obligation on the screen is the **overhead**: keystrokes and waits beyond the digits of the readings themselves must not exceed one keystroke per reading to commit and advance, and no more than three keystrokes to move from the last reading of one lot to the first of the next (per M6-40). | [MUST] | Time a trained tester on a thirty-lot batch and count keystrokes. The overhead limits are met, or the screen is redesigned; raw typing time is not treated as a defect. |"

Notes on scope of the edit:
- Delete the "twelve minutes" clause rather than reconciling it; the batch figure must be derived from the per-lot figure (30 x 90 s = 45 min) so the two cannot drift apart again.
- Do NOT touch M6-47. It stays [LATER], as OPEN-Q-T29 and the Phase 1 non-goal at section_1_foundation.md:344 both require. If import is wanted as a throughput lever, the right change is to add the volume rationale to the Phase 6 investigation OPEN-Q-T29 already schedules, conditional on the export actually existing.
- Optionally add a cross-reference from M6-41 to S3 so a future editor changing one is forced to see the other. Note separately that research_silk_domain.md:167 calls 90 s/sample too slow for 11,000 samples/year, so the 90 s figure itself may deserve review, but that is a distinct question and must be settled in S3 and goal 3, not by a divergent number buried in M6.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
CONFIRMED, but the reviewer found the right defect for partly the wrong reasons and proposed a fix that contradicts the document.

VERIFIED:
1. Quote is verbatim at parts/section_3_functional_core.md:924 (SPEC.md:2169).
2. Reading count is confirmed, not invented. parts/research_silk_domain.md:420 is marked [F, S10] (verified fact): "Twenty samples in each lot are weighed on a scale for finding out test results." -> 20 readings -> 1 lot result. The dominant test is the Limited test (parts/section_1_foundation.md:260: "about 98 per cent of Dharmavaram's samples"), and the spec itself ties 20 readings to one Limited-test lot.
3. The infeasibility holds. M6-08 requires denier to two decimal places, so a reading is ~5 characters ("20.35") plus one keystroke to commit; 20 readings in 20 s is ~5-6 keystrokes/second sustained, against ~2.8-3.3/s for trained 10-key entry (~4.2/s elite). No screen design can discharge it, yet the acceptance check makes the developer liable ("the target is met or the screen is redesigned").

THE REVIEWER MISSED THE DECISIVE EVIDENCE and substituted a weak argument:
- The real, hard contradiction is with the document's own targets for the identical operation, stated TWICE as 90 seconds: parts/section_1_foundation.md:83 (goal 3 criterion) "entry of 20 readings for one lot in under 90 seconds", and parts/section_1_foundation.md:145 (success measure S3) "Staff time to enter one lot's 20 denier readings and compute the result | ... | Target: Under 90 seconds per lot". M6-41 is 4.5x tighter than the spec's own measurable success criterion for the same task. That, not typing physics, is what makes this unarguable.
- The reviewer's claimed "internal inconsistency" (30 x 20 s = 10 min, not 12) is NOT a contradiction. 12 minutes is a looser aggregate bound that is automatically satisfied if each lot is under 20 s; it merely leaves ~2 min of batch-level slack and is redundant, not inconsistent. This sub-claim should be dropped.

THE PROPOSED FIX IS WRONG and would introduce new defects:
- Moving M6-47 to [MUST] contradicts OPEN-Q-T29 (parts/appendix.md:195), which records as an OPEN QUESTION whether the denier software can export at all and directs: "Assume manual entry with a mandatory attached instrument printout in Phase 1; investigate export in Phase 6. Do not attempt instrument interfacing earlier." It also contradicts the Phase 1 non-goal at parts/section_1_foundation.md:344 and the "[MUST when M6-47 is built]" conditional structure of M6-48/M6-49. Asserting "the import is the only lever that meets the annual volume" makes a mandatory requirement out of a capability that may not exist.
- Fix part (b), 30 lots x 20 readings in under 25 minutes, is 50 s/lot, which re-creates the same defect by contradicting S3's 90 s/lot.
- Fix part (c) is already covered by M6-35 and M6-36, which mandate full keyboard operation and grid-confined tab order.
- Only fix part (a)'s idea (bound the overhead, not the typing) is sound; its "five seconds" is unsourced.

====================================================================================================
FINDING 15/25  [Major] [buildability]  (raised by M1-M10 reviewer)
LOCATION: M9-04 (line 1369) against M9-02 (line 1367), M8-38 (line 1231) and the M3 report-grouping recommendation (line 528)
EXACT TEXT IN THE DOCUMENT:
The payload carries, and carries only: a payload version; the issuing unit code; the report number and revision; the Unique Laboratory Report number where one exists; the report issue date; the sample identifiers; a very short sample description; the test or standard code; the fingerprint of the issued file; the fixed verification web address; and the issue timestamp.
PROBLEM:
The payload contents and the printed size are incompatible under the report grouping the document itself recommends. M3 defaults to one consolidated report per Test Request and the research's batch is 10 to 30 lots, so "the sample identifiers" means up to 30 numbers of the form DVM/2627/RS/00411 — about 570 characters before the description, the standard code, the ULR, the URL, the fingerprint and a 64-byte signature. That is roughly 800 encoded bytes, needing a version 25 to 30 QR symbol of 117 to 137 modules; at the 30 mm square M9-02 specifies that is 0.22 to 0.26 mm per module, below what a phone camera reads reliably off paper and far below what survives a photocopy or a fax. The payload also signs the verification address and then repeats it as the URL wrapper in M9-07, spending scarce capacity twice.
REQUIRED FIX:
Two corrections to M9-04, then a bounded new requirement.

(1) Amend M9-04 to: "The payload carries, and carries only: a payload version; the issuing unit code; the report number and revision; the report issue date; the number of samples the report covers together with the first sample number, and the last where the samples are a contiguous block; the first 16 bytes of the fingerprint of the issued file, rendered base64url; and the issue timestamp. It may additionally carry the two optional fields already required elsewhere — the headline result under the M9-06 configuration flag, and the opaque customer token permitted by M9-05 — and nothing else. The Unique Laboratory Report number, the sample description, the test or standard code and the verification address are not carried in the payload: they are printed on the report face, and the online tier returns them per ARC-20. The verification address in particular is already the URL wrapper of M9-07, and M9-37 makes the published address, not the encoded one, the trust anchor." The reviewer's version omits the M9-06 result field and the M9-05 customer token from a "carries only" list, which would put M9-04 in direct conflict with two MUSTs; the wording above closes that.

(2) Add M9-44 [MUST]: "The complete encoded string — the URL wrapper, the header, the payload and the signature — must not exceed 360 bytes, so the symbol is QR version 14 or lower at error-correction level M (73 modules). The build fails if any configuration can produce a longer string: the check must be run with the headline-result flag on and off, with the longest configured unit code, and with the largest sample count the bulk registration of M3-15 permits. The symbol prints at not less than 33 millimetres square, giving at least 0.45 mm per module."

(3) Amend M9-02's acceptance check from 30 millimetres to 33 millimetres so the two requirements do not disagree on the printed size, and extend it to: "Generate codes for a 1-sample and a 40-sample consolidated report, with the M9-06 flag off and on. All four are within the 360-byte cap and print at version 14 or lower; five different phones read each one first time from the printed original."

Why the reviewer's numbers must change. First, a 300-byte cap and "version 12 or lower at level M" are mutually exclusive: version 12 at level M holds 287 bytes in byte mode, and byte mode is forced because base64url contains lower-case characters. Second, the reviewer's own field list, encoded as a JWS with a 64-byte P-256 signature, a normal "kid", and the fragment URL wrapper, measures ~412 bytes — version 15 or 16, about 0.38 mm per module — so their cap is unreachable with their own list. Only after also shortening the host, the key identifier and every JSON key does a tight variant reach ~299 bytes, which is still version 13 (0.435 mm at 30 mm), and enabling the M9-06 result pushes it to ~332 bytes. The 360-byte / version-14 / 33 mm triple above is self-consistent (33 / 73 = 0.452 mm) and leaves room for the optional fields the other requirements demand. Third, use 40 samples rather than 30 as the stress case, because M3-15 permits 40 in one bulk registration. Finally, the reviewer's photocopy criterion should be recorded as a [SHOULD], not folded into a MUST: no requirement in the document currently asks the code to survive reproduction, so making photocopy legibility a pass condition adds a new requirement rather than repairing an inconsistent one.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
Confirmed independently. The quote is verbatim at parts\section_3_functional_core.md:1368 (SPEC.md:2613), and every supporting premise checks out: M9-02 (line 1366) fixes the printed square at 30 mm with the acceptance check "five different phones read it first time"; M8-38 (line 1230) defaults to one consolidated report per Test Request; the M3 recommendation (line 528) endorses that default because the work arrives in daily batches of ten to thirty lots; M3-15 (line 395) actually allows bulk registration of 40 samples of the same type and test set in one action; and the sample-number format is 17 characters (DVM/2627/RS/00411, line 514). I searched for a bound under every name I could think of (byte cap, payload size, symbol version, modules, capacity, error-correction level, "samples covered", "first and last", range) across all of parts\ and found none. The only nearby items are the technology choice in section_5_nfr_plan.md:342 (QR Code ISO/IEC 18004 at level M) and ARC-20 (line 362), which governs the separately published ONLINE record and notably carries a sample description, not the sample identifiers — so the online tier is not the source of the constraint. Measuring the actual encoding (JWS compact, ES256, base64url, as the research brief prescribes at research_india_commercial.md:477) makes the conflict concrete and in fact worse than the reviewer states: M9-04's exact field list with 30 sample numbers, a 64-hex-character digest, the ULR and the signed verification URL is 901 characters of JSON, 1,358 characters of JWS, ~1,386 bytes once wrapped as the M9-07 fragment URL. That is QR version 31 at level M (141 modules), i.e. 0.21 mm per module at 30 mm — not the reviewer's version 25 to 30. More damning, the ONE-sample case measures ~610 bytes, which is version 19 (93 modules, 0.32 mm per module), so M9-04 misses M9-02's acceptance check before the grouping question even arises. The research brief's own size budget (research_india_commercial.md:518, "roughly 300-380 bytes of JSON ... version 11-14 at level M ... about 30 mm square") assumed a SINGULAR sample id, no ULR, and offered "shorten dgs to the first 16 bytes" as a lever; the specification pluralised the field to "the sample identifiers", added the ULR and the revision, and dropped the size budget, so no requirement now carries the constraint that made the 30 mm figure true. The secondary point is also real: M9-04 signs "the fixed verification web address" and M9-07 then repeats it as the URL wrapper, and M9-37 makes the PUBLISHED address the trust anchor anyway, so the ~28 duplicated bytes buy nothing. This is a genuine buildability conflict between two MUSTs, not a misreading. Severity Major is right, not Blocker: the defect is confined to one requirement's field list, M9-02's acceptance check would catch it in test, and the repair is local. The proposed fix, however, is arithmetically wrong twice and creates two new contradictions, so it must be corrected.

====================================================================================================
FINDING 16/25  [Major] [contradiction]  (raised by M1-M10 reviewer)
LOCATION: M10 lot header field table (line 1609) against M10-09's tare line 6 (line 1547) and M3-03 (line 373)
EXACT TEXT IN THE DOCUMENT:
| Number of books per bale | Integer | Yes | One or more | Drives the tare scaling |
PROBLEM:
Number of books is captured once at lot level but consumed per bale: tare line 6 is "the tare of all the books in the bale", scaled by that bale's own book count. Bales in one lot do not contain equal numbers of books — the standard's own preliminary-examination proforma records "No. of books or bundles in a bale" per bale precisely because it varies — so a single lot-level figure mis-states the book tare, and therefore the net, oven-dry and conditioned mass, of every bale that differs from it. On a bale with 22 books priced off a lot figure of 25 the book tare is overstated by three books' worth, roughly 0.7 kg. Separately, M10 re-collects the bale count, the bale serial numbers and the book count that M3-03 already captured as sub-units on the sample, creating two sources of truth for the same physical facts with no rule about which wins.
REQUIRED FIX:
The reviewer's fix is directionally correct but leaves two dangling references and names a source of truth that does not yet hold the datum. Apply this instead:

1. In the M10 *Lot header* table (section_3_functional_core.md:1608), delete "| Number of books per bale | Integer | Yes | One or more | Drives the tare scaling |" and add in its place a derived row: "| Total books in the lot | Derived | Auto | Sum of the per-bale book counts | Prints on the certificate as the lot total |".

2. In the M10 *Per bale - weighing and tare* table, immediately above "Mass of the outer wrapper ('shirt')", insert: "| Number of books in this bale | Integer | Yes | One or more; defaulted from the bale's sub-unit row and editable with a reason | The multiplier for tare line 6; may differ from bale to bale |".

3. Amend M10-08 to make the per-bale basis explicit and testable: "...computes the tare per book from the sampled books, scales it to **that bale's own book count**, adds any bale-level components, and shows every line of the arithmetic. Bales in one lot may contain different numbers of books and the tare is computed independently for each." Acceptance check: "Register a two-bale lot with 25 and 22 books and identical packing-component masses; the two bales show different values for tare line 6 and for total tare, and each matches a hand calculation line for line."

4. Amend M10-29 (line 1585), which currently mandates that the printed lot identification block carry "number of books per bale" - a field that would no longer exist. Change that clause to "...number of bales, the total number of books in the lot, chop where supplied", and add to the Section II wording: "each bale row states that bale's own book count alongside its tare build-up."

5. Amend M10-05's acceptance check so a uniform lot no longer passes for the whole requirement: "Register a lot of four bales of twenty-five, twenty-five, twenty-two and twenty-eight books; the certificate shows four bale rows, each with its own book count, and a lot total of one hundred books."

6. Do NOT state that the book count is "read from the sample's sub-unit rows created at registration under M3-03 and not re-entered here" as written - M3-03 and the M3-16 field table (line 489-491) capture only a single "Number of sub-units" integer plus "Sub-unit serial numbers" marked not mandatory, and hold no per-bale book count at all, so the fix would point M10 at an empty source. Instead, first close that gap: add to the M3-16 field table "| Books or bundles per sub-unit | Integer | Conditional | Required where the sub-unit type is Bale or Carton | The standard's 'No. of books or bundles in a bale'; may differ per bale |", make "Sub-unit serial numbers" mandatory where the sample type is a raw-silk lot, and then add to M10-05: "The bales of the lot, their serial numbers and the number of books in each are defaulted from the sample's sub-unit rows and are not blind-entered; where the weighing-time count differs from the registered count, the difference is recorded with a reason, raises a nonconformity, and blocks the certificate until the Unit Incharge resolves it - consistent with the override discipline in M10-06."
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
Independently confirmed. The quote exists verbatim at parts/section_3_functional_core.md:1608 (SPEC.md:2853) in the M10 *Lot header* block, i.e. one integer for the whole lot. Its consumer is unambiguously per-bale: M10-08 "scales it to the number of books in the bale", M10-09 tare line 6 "the tare of all the books in the bale", and M10-25 runs the whole gross-tare-net-oven-dry-conditioned chain per bale. I searched for a per-bale book count under every plausible name ("number of books", "books per", "books in", "book count", "books or bundles", "sub-unit", "n_books") across all five sections, the appendix and the front matter: the per-bale weighing grid (lines 1622-1639) has no book-count input, and the only other hits are "Number of books sampled for tare" (line 1627 - the divisor, a different quantity) and the group-level colour-group grid. So no per-bale figure exists anywhere, and the lot-level one is structurally the only multiplier available. Variance is corroborated by the document's own research, not merely asserted by the reviewer: research_silk_domain.md:244 gives Bale = "22-30 books, approx 60 kg" marked [F, S6]; line 251 records "No. of books or bundles in a bale" as a per-bale attribute of the Preliminary Examination proforma; line 210 states the standard's formula as n(M_p5 + 3M_c5)/5 with n the bale's own count; and cl.3.3 (line 257) requires the colour-group draw to be "proportional to the total number of books in each group", a rule that would be redundant under uniform book counts. Nothing in M10's 37 requirements or 8 edge cases asserts equal book counts per bale or provides an override. Both relevant acceptance checks (M10-05 "four bales of twenty-five books", M10-08 "matches a hand calculation") pass on a uniform lot, so the defect survives testing and surfaces only in production, on the one document the spec itself calls the commercial settlement figure. Two caveats that do not defeat the finding: the reviewer's magnitude is inflated (the spec's own example at section_2_workflow.md:392 is 1.6 kg total tare on 22 books, so three books is roughly 0.15 kg, not 0.7 kg), and the duplication half of the finding is weaker than the arithmetic half - within M10 the per-bale grid already derives "Bale serial number" as Derived/Auto, so the two-sources-of-truth issue is only against M3-03/M3-16 and is a reconciliation gap rather than a wrong number. Severity Major is right: it is a data-model error that yields an incorrect signed money figure, not a cosmetic contradiction.

====================================================================================================
FINDING 17/25  [Major] [missing-capability]  (raised by M1-M10 reviewer)
LOCATION: M1-49, M1-50, M1-51 (lines 118–120); no seed data anywhere in M1–M10
EXACT TEXT IN THE DOCUMENT:
(c) the size category rule that decides which parameters apply
PROBLEM:
The grade engine is correctly declared to be configuration rather than code, but the configuration's content is nowhere specified, so nobody can seed it and the acceptance checks for M1-50 and M1-51 cannot be written. Missing entirely: the three size categories and their boundaries (Category I up to 2.0 tex / 18 denier, Category II 2.1 to 3.6 tex / 19 to 33 denier, Category III 3.7 tex / 34 denier and above); the rule that maximum deviation joins the major tests only in Category III; the rule that cohesion is evaluated only where the marked size is 33 denier or finer; and the fact that BIS maximum-deviation limits deliberately differ from the ISA values, so the two specification sets are not translations of each other. Without these, a developer will build a grade engine that applies cohesion to coarse silk and treats maximum deviation as auxiliary in all categories, producing wrong grades that look plausible.
REQUIRED FIX:
Adopt the reviewer's annex, with three corrections and one deletion.

Delete fix item (6). SPEC.md:5433 already states that BIS and ISA grade tables are entered separately "because they deliberately differ", and M1-49's acceptance check already creates them as separate records. Restating it in M1 adds nothing.

Add the annex to M1 headed "Raw silk grade computation reference data, IS 15090:2002, marked unconfirmed pending the scientist's grade tables", following the OPEN-Q-C11 pattern (seed the standard's values, mark each unconfirmed, require the scientist's sign-off against a real worksheet before go-live), containing:

(1) Size categories: I up to 2.0 tex (18 denier); II 2.1 to 3.6 tex (19 to 33 denier); III 3.7 tex (34 denier) and above. State that the category is resolved from the size **marked/declared on the bales**, not from the measured size, and add a cross-reference reconciling this with M6-03 ("declared or measured size") and WF-74 — a lot marked 33 denier may measure 34.5 denier and still pass the 7 percent gate, so the two clauses must not be left free to resolve the category differently.

(2) Major characteristics: size deviation, evenness variation I, evenness variation II, cleanness, average neatness, low neatness, plus maximum deviation in Category III only.

(3) Auxiliary characteristics: maximum deviation (Categories I and II only), evenness variation III, winding breaks, tenacity, elongation, cohesion.

(4) Cohesion is evaluated only where the marked size is 33 denier or finer, i.e. Categories I and II.

(5) The one-class cap on class differences applies to evenness variation III, and to maximum deviation only in the categories where maximum deviation is auxiliary (I and II). Do not state the cap for maximum deviation unqualified — in Category III it is a major test and the auxiliary cap cannot apply to it.

(6) Data-model correction, required or the annex creates a fresh contradiction: mst_method_parameter (SPEC.md:5158) carries is_major_characteristic and is_auxiliary_characteristic per method-parameter, which cannot express maximum deviation being major in Category III and auxiliary in Categories I and II. State that the authoritative major/auxiliary classification lives on mst_grade_rule (keyed on spec_set_id, therefore per category), and either drop the two booleans on mst_method_parameter or document them explicitly as non-authoritative report-ordering defaults.

Also correct or qualify SPEC.md:794 so its major/auxiliary list is visibly scoped to Category II and points at the annex, since it is currently the only enumeration in the document and reads as general.

Add to M1-51's acceptance check: "Compute a grade for a 40-denier lot (Category III); cohesion is excluded from the computation and maximum deviation is evaluated as a major test, with no software change. Repeat for a 20-denier lot (Category II); cohesion is included and maximum deviation is evaluated as an auxiliary test with the one-class cap applied."
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
Verified. The quote is verbatim at SPEC.md:1365 (parts/section_3_functional_core.md:120). The omission is real: the boundaries of Category I and Category III appear nowhere in the 152k-word document; Category II's boundary appears exactly once (SPEC.md:794) and only inside a non-normative glossary "*Example:*"; the rule that maximum deviation joins the major tests in Category III appears nowhere; the rule that cohesion is evaluated only at 33 denier or finer appears nowhere. I searched size categor*, Category I/II/III, 33 denier, 3.6/3.7/2.0 tex, cohesion, maximum deviation, grade/grading table, Major test, Auxiliary, provisional grade, class difference, marked size, coarser/finer, specification set, category_code, per category, and the whole Appendix B open-question register.

The gap is not a deliberate deferral. parts/research_silk_domain.md:403-404 carries every one of these as [F, S4] verified fact, including "(plus maximum deviation for Category III)" and "Cohesion is only evaluated if the marked size is <=33 d". The spec dropped verified content it held, and unlike the closely analogous OPEN-Q-C11 (parts/appendix.md:114, which seeds the standard's skein counts and marks them unconfirmed), no OPEN-Q covers it.

The consequence is confirmed, not speculative. SPEC.md:794 is the only enumeration of the major/auxiliary sets a seeder would find, and it lists maximum deviation flatly as auxiliary (correct for its stated Category II, misleading as a general seed). M1-51's acceptance check only reclassifies one parameter between Major and Auxiliary, so it never exercises clause (c); nothing in the document would catch a grade engine that applies cohesion to coarse silk or treats maximum deviation as auxiliary in Category III. Wrong grades on grading certificates that set the commercial value of silk lots, with no failing test, justifies Major.

Two of the reviewer's claims are overstated. Fix item (6) is already handled at SPEC.md:5433, which says grade tables must be entered "per category, with an effective date, separately for the Indian Standard and the international method, because they deliberately differ" (also M1-49's acceptance check creates BIS and ISA as separate records). And the underlying data model is adequate: mst_spec_set carries category_code and mst_grade_rule is keyed on spec_set_id with rule_type, so the rules are expressible; what is missing is the seed content and the acceptance coverage, not the capability. "missing-capability" is therefore a slightly wrong category, but the defect itself holds.

====================================================================================================
FINDING 18/25  [Major] [unverified-fact-as-fact]  (raised by M1-M10 reviewer)
LOCATION: M3-07 acceptance check (line 382)
EXACT TEXT IN THE DOCUMENT:
Select Automatic Reeling Machine; the indigenous-machine concession rate resolves.
PROBLEM:
The acceptance check asserts a pricing behaviour that is both factually wrong and structurally contrary to M1. The published concession is for "Only Indigenous ARM produced silk" at ₹1100 against ₹2000 for "Other than indigenous ARM Silk"; the machine type alone does not establish indigenous production, so imported ARM silk would be priced at the concessional rate and the unit would under-recover ₹900 on every such ISA grading. Structurally, M1-12, M1 rules items 4 and 5, and the M3 header field "Concession category (order level)" all place price class on the order line, deliberately separate from technical attributes; letting the reeling-device field silently drive price defeats the audit trail that M1 rules item 5 requires ("the resolution path taken must be stored on the line").
REQUIRED FIX:
The reviewer's fix is directionally correct but has two defects: it references a "declared origin" field that does not exist anywhere in the specification (creating a dangling reference), and it corrects only the acceptance check while leaving the root assertion intact in three other locations. Use this instead.

(a) Replace the M3-07 acceptance check (parts/section_3_functional_core.md:382) with: "Select Automatic Reeling Machine and leave the order-line concession category as None; the standard ISA grading rate resolves and the stored resolution path names the concession-category-None line. Then set the order-line concession category to Indigenous ARM Silk Unit with a reason; the concessional rate resolves and the resolution path is stored on the line."

(b) Amend M3-07's requirement sentence. Replace "This affects both the effective test recipe and the price." with "This drives the effective test recipe. It is a technical attribute and never resolves a price by itself; price class is carried by the order-line concession category per M1 rules item 5."

(c) Add a new requirement (for example M3-07a) rather than burying the cross-check inside M3-07: "Where the order-line concession category is Indigenous ARM Silk Unit and either the reeling or production device is not Automatic Reeling Machine or the declared silk origin is not Indigenous, the system warns and requires a reason code before the request can be confirmed. The warning does not block. [MUST] Acceptance: set the concession category to Indigenous ARM Silk Unit with device Charkha; confirmation is refused until a reason is recorded, and the reason is stored on the order line."

(d) Add the missing field that (c) depends on. In the M3 sample field table (parts/section_3_functional_core.md, near the "Declared variety" row at line 481) add: "| Declared silk origin | Pick-list | Conditional | Indigenous / Imported / Not stated; required where the order-line concession category is Indigenous ARM Silk Unit | Customer-declared, not verified by the laboratory; asserted by the customer, printed as customer-declared |" and add the same field to the M3-06 declaration list so it inherits the "declared by customer" labelling.

(e) Fix the two remaining places that repeat the wrong assertion, or the contradiction survives the edit: change the sample field table row at line 486 from "Affects price and recipe" to "Affects the test recipe; not a pricing input", and change the mst_reeling_device comment at parts/section_5_nfr_plan.md:435 from "Affects price and test recipe." to "Affects the test recipe. Not a pricing dimension; price resolves on the order line's concession category per M1 rules item 5." Keep the is_arm BOOLEAN column, since it is what makes the M3-07a consistency warning cheap to evaluate.

This set introduces no new contradiction: it aligns M3 with M1-12, M1 rules items 4 and 5, M1-53, M1-54, the M3 header field table at line 445, and mst_rate_card_line at parts/section_5_nfr_plan.md:460, all of which already place price class on the order line.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
Verified on all counts. (1) The quote is verbatim at parts/section_3_functional_core.md:382 (SPEC.md:1627), not paraphrased. (2) The factual claim is confirmed against a [F]-marked (verified) research item, not an inference: parts/research_silk_domain.md:58-59 carries the published rate rows "Only Indigenous ARM produced silk" 1100 and "Other than indigenous ARM Silk" 2000, restated at line 261 tagged [F, S1] and line 314, and corroborated independently by parts/research_india_commercial.md:42 ("ISA priced differently for 'Indian ARM units' versus 'Imported silk'"). The concession turns on origin AND device; the acceptance check silently drops the origin half and asserts the device alone resolves the concessional rate. That overstates the source. (3) The structural contradiction is real and I confirmed it in seven independent places, not one. M1 rules item 5 (line 236) enumerates the complete rate-resolution input set - (a) test + the order line's concession category + order date, (b) test + concession "None", (c) fallback reference-laboratory rate, (d) unpriceable - and the reeling device appears nowhere in it. M1 rules item 4 (line 235): "Rate resolution reads the concession category on the *order line*." The M3 header field table (line 445): "Concession category (order level) ... Drives price resolution." M1-53 (line 132) defines rate card lines as carrying test + concession category, and M1-54 (line 133) forbids two active lines for the same test + concession category + overlapping dates - which makes test+concession-category the entire key and leaves no device dimension available. mst_rate_card_line (parts/section_5_nfr_plan.md:460) confirms this in the data model: no reeling_device_id column. So there is no specified mechanism by which the device could resolve a price, and the acceptance check as written is both unimplementable and contrary to M1. (4) It is not addressed elsewhere. Grep for "indigenous" across every section file returns only M1-12's pick-list value and this acceptance check. Critically, no declared-origin attribute exists anywhere: M3-06 (line 380) and the M3 sample field table (lines 478-495) enumerate declared size, variety, composition, twist, colour, weight, quantity, lot number, mark, chop and claimed grade - there is no indigenous/imported field. So the reviewer is right that the machine type cannot establish indigenous production in this design, and the 900-rupee under-recovery per imported-ARM ISA grading is a genuine consequence. (5) The audit-trail point also checks out: M1 rules item 5 requires "The resolution path taken must be stored on the line", which a device-driven price would bypass. (6) The reviewer's chosen direction of resolution is the correct one - seven places put price class on the order line versus three that say the device affects price - so the acceptance check, not M1, is the defect. Severity Major is right, not Blocker: M1 rule 5 is unambiguous enough that a careful implementer would follow it, and M1-12 already supplies the correct "Indigenous ARM Silk Unit" value, so a working system is reachable. But the fix as proposed is imprecise on two counts, so I have supplied a corrected version.

====================================================================================================
FINDING 19/25  [Major] [over-engineering]  (raised by M1-M10 reviewer)
LOCATION: M4-24 (line 617); M4-23 (line 616); M4-22 (line 615)
EXACT TEXT IN THE DOCUMENT:
**Scan stations** exist at, at minimum: receipt counter, store-in, issue to tester, bench / result entry, verification desk, report desk, retention store, and disposal or return.
PROBLEM:
Eight scan stations with eighteen mandatory custody event types and M4-23's requirement that every handover carry the receiver's own confirmation is a chain-of-custody design for a multi-room laboratory with a store keeper. This unit is three people in a shared room, doing 30 lots a day of a ₹50 test on material that never leaves the room. At eight stations that is roughly 240 scans and 30 separate receiver confirmations a day, on top of the 20-second-per-lot entry target — the two requirements are competing for the same clerk. The likely outcome is that scanning is abandoned wholesale and the custody log, which is the module's real compliance product, ends up empty.
REQUIRED FIX:
Leave M4-24 and its eight stations exactly as written. They cost one scan dispatcher plus an eight-row station-to-screen configuration table, sys_scan_event.station_code already carries them, Phase 1 scope (section_5_nfr_plan.md:641) already names them, and M4-20/M4-28/M4-29 [MUST] already require store-in and retention-store behaviour in Phase 1. Fix the grain of the custody event instead, at M4-23 and WF-50, modelled on M7-36's bulk-verification safeguards.

Add M4-23a [MUST]: "For tests flagged batch-custody in the test catalogue, one confirmation action by the receiving tester writes the issue-to-tester custody event for every sample on a worksheet, and one action by the returning tester writes the return-to-store event for every sample on it. One custody row is still written per sample, preserving the per-sample trail required by M4-27, M20-11 and NFR-106; each row carries the worksheet number as the authorising batch reference and the same receiver confirmation identifier. The batch action runs the M4-25 allocation and competency check against every allocation on the worksheet and is refused in full, naming the offending sample, if any one fails. Per-sample custody events with individual receiver acknowledgement remain mandatory for grading lots, conditioned-mass lots (M10), rejected samples, samples under complaint or dispute, and samples transferred to another unit or to a subcontractor; any such sample is excluded from the batch action and listed for individual handling, as in M7-36(c). Acceptance check: issue a 30-lot Limited-test worksheet containing one grading lot; one tester confirmation covers the 29 Limited-test lots, each of which has its own custody row bearing the worksheet number, and the grading lot is held for an individual per-sample confirmation."

Add M4-23b [MUST]: "Where the from-party and the to-party of a handover resolve to the same staff member — routine in a three-person unit where the receipt clerk also tests — the event is recorded once with both parties named and no second confirmation is demanded, since a person cannot acknowledge receipt from themselves. The record states that the parties coincided, so the overlap is visible to an auditor rather than disguised as an independent acknowledgement, consistent with the treatment of role overlap in M5-12."

Amend WF-50 to add: "Where the test is flagged batch-custody, the issue-to-bench and return-from-bench events for all samples on one worksheet may be written by a single confirmation action per M4-23a. The per-sample trail printed by M4-27 must still show both hops for every sample."

Add to the M4 rules and edge cases list, beside existing note 5: "A custody log that records thirty tester-confirmed handovers a day that did not physically occur is worse evidence than an honest batch record, because it is falsified. Match the custody grain to the grain at which the material actually moves — the worksheet — exactly as M7-36 matches the verification grain to the batch."

Also add worksheet_id (nullable) to txn_custody_event at section_5_nfr_plan.md:495, with a note that it is the batch authorisation reference and does not change the append-only rule of M4-21 or the entity_type/entity_id grain.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
The quote is verbatim (parts/section_3_functional_core.md:617) and the scale facts are all in the document (₹50 test, 98% of 11,294 samples/year in 10-30 lot batches, 20-second target in M6-41, three-person unit acknowledged in M5-12). A real defect survives, but not the one the reviewer describes, and not at M4-24.

WHAT DOES NOT HOLD. M4-24 requires that eight scan stations EXIST — a scan dispatcher plus a station-to-screen mapping, backed by sys_scan_event.station_code already in the schema (section_5_nfr_plan.md:533). Its acceptance check is a navigation test ("scan at the bench station; the result-entry screen opens"). Nothing in the document gates any state transition on a scan, so the predicted failure ("scanning abandoned, custody log empty") does not follow — the log is populated by screen actions regardless (section_3_functional_core.md:661: "Storage location assigned ... Writes a custody event"; AC-01 confirms the received event comes from registration). The verification-desk and report-desk stations are navigation shortcuts in the source brief (research_lims_reference.md:417-418, "Pulls up submitted results" / "Pulls the reportable set") — the sample never travels there, so they are not custody hops. M4-22's eighteen types are a controlled vocabulary, not eighteen events per sample; its acceptance check is "Perform each event type once", and several are mutually exclusive (Accepted/Rejected; Handed to customer/Despatched/Disposed/Transferred x2). M4-23's receiver acknowledgement is scoped "where a physical handover occurred", excluding Weighed, Moved, Condition recorded, Sub-divided and Consumed. The 240-scans-and-30-confirmations arithmetic is inflated roughly four-fold. Hardware plan budgets two scanners, not eight (section_5_nfr_plan.md:413).

WHAT DOES HOLD, on a requirement the reviewer did not cite. WF-50 [MUST] (parts/section_2_workflow.md:663) mandates a custody event for every physical movement per sample, with the acceptance check spelling out "including issue to and return from the bench". Combined with M4-23's receiver acknowledgement, that is 30 issue-to-tester confirmations plus 30 return-to-store events daily, in a shared room where the confirming tester is frequently the person who received the lot at the counter — a person confirming a handover from themselves, for material that never left the room. Every other layer of the dominant workflow has a batch grain (M5-13 worksheet, M6-40 worksheet-mode grid, M7-36 bulk verification); the custody layer alone insists on per-sample grain. I searched batch, bulk, worksheet, group, "one action" and "many" against every custody requirement in sections 2, 3 and 4 and found no batch-grain provision. The document already contains the exact pattern needed — M7-36's per-test catalogue flag with mandatory safeguards and automatic exclusion of anything unusual, defended at line 1143 ("Eleven thousand samples a year of one routine test cannot be verified one screen at a time") — which is why this reads as an omission rather than a deliberate choice. Severity stands at Major: it drives a schema decision (a worksheet reference on txn_custody_event) that is cheap now and expensive later, and the realistic outcome is 30 fabricated tester-confirmed handovers a day, which is a worse audit position than an honest batch record.

THE PROPOSED FIX IS WRONG IN THREE PLACES. (1) section_5_nfr_plan.md:641 places "chain of custody and scan stations" inside Phase 1 scope; retagging four stations [LATER] contradicts that line unedited, and section_1_foundation.md:5 defines [LATER] as "must not consume phase-1 effort". (2) Store-in cannot be deferred — M4-20 [MUST] requires a current storage location at all times with full history, and the M4 acceptance-screen field table makes it mandatory. (3) Retention store cannot be deferred — M4-28/M4-29 [MUST] are Phase 1. Further, "one custody event per worksheet" breaks M4-27, M20-11 and NFR-106, all of which require a complete per-sample trail, and contradicts the txn_custody_event grain at section_5_nfr_plan.md:495, which keys each row to a single entity_type/entity_id. Deleting the bench station is also the wrong economy: the source brief identifies it as the one station where error reduction actually happens.

====================================================================================================
FINDING 20/25  [Major] [missing-capability]  (raised by gap-coverage reviewer)
LOCATION: Part C — M8 requirements, M8-11, line 2434 (with M8-08 line 2431, M8-22 line 2446, M13-08 activity SAMPLE)
EXACT TEXT IN THE DOCUMENT:
| M8-11 | Where the laboratory performed the sampling, the report references the **sampling plan and sampling method** used. | [MUST] | A laboratory-sampled job prints the sampling reference; a customer-submitted one prints nothing. |
PROBLEM:
Laboratory sampling is referenced in four places — M8-08 prints the date of sampling, M8-11 prints the sampling plan and method, M8-22 prints the not-responsible-for-sampling statement, M13-08 enforces authorisation for the activity "sampling" — but no module anywhere creates, stores or gates a sampling plan or a sampling record. The gap analysis lists this explicitly as missing under Point 3 ("If lab staff ever draw the sample themselves (from a bale, a godown, a customer's premises), 17025 cl. 7.3 requires a sampling plan and record, and the report must say so"), and it is real work for this unit, which draws sub-samples from bales and sits beside the Government Cocoon Market. M8-11 is a [MUST] with nothing to reference: the developer has no table, no fields and no acceptance path, and the document type "Sampling plan" is absent from M14.4's document-type list.
REQUIRED FIX:
Four coordinated edits.

(1) Add to M4, at the END of the requirements table (after the existing M4-34, SPEC.md line 1877) — note the ID, since M4-33 is already taken by the cancellation requirement:

| M4-35 | Where laboratory staff draw the sample themselves rather than receiving it, the system shall hold a **Sampling Record** against the sample: the sampling plan reference, the date and time of sampling, the place of sampling (customer premises, godown, market yard, another CSB unit), the person who sampled and the M13 authorisation relied on, the population sampled and its identification (bale or lot marks, lot size, number of packages presented), the sampling method and its version from M14, the number and size of increments taken, the equipment used where relevant, the temperature and humidity at sampling where the method requires them, and any departure from the plan with the authorisation for it. A sample whose mode of receipt is "drawn by the laboratory" cannot reach Accepted until its Sampling Record is complete. The record is a technical record: append-only, audited and retained under M21. | [MUST] | Register a sample with mode of receipt "drawn by the laboratory"; acceptance is refused while the Sampling Record is incomplete; once complete, M8-08 prints its date of sampling and M8-11 prints its plan and method reference. |

(2) Make the record reachable. Add **"Drawn by the laboratory"** to the mode-of-receipt list in M3-16 (line 1641) AND to the "Mode of receipt" pick-list in the M3 field table (line 1737, currently "Hand / Courier / Post / Inter-unit / Portal despatch"). Without this the M4-35 acceptance check cannot be executed. Also add a "Sampled by the laboratory" event to the M4-22 custody event types (that list is stated "at minimum", so this is a clarification rather than a change).

(3) Give M8 its data source. Add two rows to the M8 report-compile field table (lines 2524-2566), matching the pattern every other printed element follows: "Date of sampling | Derived | Auto | From the Sampling Record (M4-35) | Printed only where the laboratory sampled" and "Sampling plan and method reference | Derived | Auto | From the Sampling Record (M4-35) | Printed only where the laboratory sampled". Add **"Sampling plan"** to the Document type pick-list in the M14 field table (line 3439), which currently runs "Quality manual / Procedure / Standard operating procedure / Work instruction / Format / External standard / Policy / Other".

(4) Phase it consistently with §26.5 and §26.6 rather than dropping it flat into Phase 1. In the §26.3 Phase 1 "Included:" list, add: "the Sampling Record for samples the laboratory draws itself, with the sampling-plan reference held as a text reference in this phase". Then note in §26.5 Phase 3 that the M13 authorisation check on the SAMPLE activity binds from this phase, and in §26.6 Phase 4 that the sampling-plan reference becomes a controlled-document link once document control exists. This keeps M8-08 and M8-11 satisfiable when reports first print in Phase 1 without asserting a Phase 1 dependency on M13 (Phase 3) and M14 (Phase 4).

Alternative, if the unit confirms it never samples: do NOT leave the current state. Either build the above, or explicitly add lab sampling to §6.2 / §3.2, requalify M8-08 and M8-11 as "[MUST] where the laboratory performs sampling; the laboratory does not currently do so, and the fields are inactive", and strike SAMPLE from M13.4 and "sampling" from M13-08's enforced list. What is not defensible is the present position: enforced activity, mandatory printed fields, no record.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
Independently confirmed on all four legs.

1. QUOTE IS VERBATIM. M8-11 exists exactly as quoted at D:\Prashant_WorkSpace\TTL Software\SPEC.md line 2434 (source: parts\section_3_functional_core.md line 1189). The three companion citations are also exact: M8-08 (line 2431, "date of sampling where the laboratory drew the sample"), M8-22 (line 2445, "not responsible for sampling"), and M13-08 (line 3261) which ends its enforced-activity list with "; sampling", backed by activity code SAMPLE — "Draw the sample where the laboratory does the sampling" — in the M13.4 table at line 3313.

2. THE CAPABILITY IS ABSENT UNDER EVERY NAME I TRIED. The word "sampling" occurs only SIX times in the whole 152k-word SPEC.md, and all six are accounted for: the four cited above, plus the M4 sub-heading "Sub-sampling and specimen preparation" (line 1834) and one out-of-scope-unrelated hit. Zero matches for "sampling record", "sampling event", "sampling details", "sampling log", "increment", "godown", "who sampled", "drawn by the laboratory", "lab-sampled". "Sampling plan" appears exactly once in the entire document — inside M8-11 itself, the requirement that consumes it. The nearest thing, M4-11, records "the method's sample-draw rule that governed the draw", but that is sub-division of an already-received sample inside the lab, a different act from drawing a sample from a population off-site.

3. THE PROBLEM IS REAL IN CONTEXT, AND THE DOCUMENT'S OWN CONVENTIONS PROVE IT. I read all of M4 (lines 1794-1930): requirements run M4-01 to M4-34 and never touch lab sampling. I read the M8 report-compile field table (lines 2524-2566): every other printed element of M8 has an explicit row with a source — "Location of testing per line | Derived | Auto | From the allocation", "Deviations from method | ... | From approved deviation records", "Environmental conditions | ... | From the result records". Date of sampling and sampling-plan reference are the ONLY mandatory M8 elements with no field-table row and no upstream source anywhere. The M3 mode-of-receipt pick-list (M3-16 line 1641, field table line 1737) is closed at "Hand / Courier / Post / Inter-unit / Portal despatch" — a lab-drawn sample cannot even be registered. So the developer literally cannot satisfy a [MUST], and M13-08's "sampling" guard can never fire.

4. NOT HANDLED ELSEWHERE, AND NOT DELIBERATELY EXCLUDED. I checked both exclusion lists: §3.2 Non-goals N1-N13 (lines 200-215) and §6.2 "Out of scope for phase 1" (lines 423-438). Neither mentions sampling. There is no OPEN-Q on it either (grep of all OPEN-Q lines). So the document has not decided to drop it — it has decided (via M13.4's SAMPLE activity and M13-08's enforcement) that the lab does sample, then failed to build the record.

5. THE BRIEFS BACK IT TWICE. parts\research_gap_analysis.md line 128 is quoted accurately by the reviewer. Independently, parts\research_nabl_17025.md line 264 spells out the required content: "Sampling data (7.3.3), if the lab ever samples | Method reference, date and time, sample identity/amount/name, personnel, equipment used, environmental/transport conditions, location diagram, deviations". And line 156 of that brief is where M8-11's wording came from, annotated "Conditional field; usually N/A for customer-submitted silk samples" — the author knew it was conditional and still made it an unqualified [MUST] with no data source.

SEVERITY: Major stands. Three [MUST] report elements and one enforced authorisation activity resting on a table that does not exist, against a named ISO 17025 clause, in a document that otherwise pins every requirement to a source and an acceptance check.

FIX: right in substance, wrong in three mechanical details — hence correctedFix. (a) The proposed ID M4-33 COLLIDES: M4-33 (customer cancels after sample arrives) and M4-34 (unclaimed samples) already exist at SPEC.md lines 1876-1877. The new requirement must be M4-35. (b) The acceptance check depends on a mode of receipt "drawn by the laboratory" that the fix never creates — M3-16 and the M3 field table row at line 1737 must both be amended, or the check is unrunnable. (c) Putting the Sampling Record wholesale into Phase 1 (§26.3) contradicts the phasing: the fix's own text depends on M14's controlled-document register, delivered in Phase 4 (§26.6, "document control with revisions and read-acknowledgement"), and on the M13 authorisation matrix, delivered in Phase 3 (§26.5, "the competency authorisation matrix enforced as a guard"). The Phase 1 entry must therefore be worded so the plan reference degrades to a text reference until Phase 4 and the M13 gate binds from Phase 3. The M14.4 addition is sound: the document-type list at line 3439 has no "Sampling plan" and adding it creates no conflict.

====================================================================================================
FINDING 21/25  [Minor] [contradiction]  (raised by consistency reviewer)
LOCATION: Part C M5-19 (section_3_functional_core.md L737) and Part D M21-16 (section_4_functional_support.md L1462) against Part B §8.3 (L510) and §10.1 Customer code row (L728)
EXACT TEXT IN THE DOCUMENT:
| M5-19 | Each sample carries a **stable pseudonym** of the form `CLIENT-nnnn`, derived once and never changed,
PROBLEM:
Two problems in one identifier. First, "Client" is on Section 2's forbidden list: §8.3 row 1 gives the word as "**Customer**" and lists "Client, Party, Firm, Sender" under "Do **not** call it" — yet `CLIENT-nnnn` is the string printed on every tester's screen, worksheet and label, which is the most visible text in the system. Second, it is an undeclared second identifier for the customer: §10.1 already defines "**Customer code** | `C-<5>` | `C-00417` | Never resets | … **Not** the Tester | Never printed on a tester's worksheet or sample label", so the blinded view needs a pseudonym derived from that series, not a new 4-digit one (M21-16's example `CLIENT-4417` and M5-19's "the 4417 lot" imply 4 digits against the customer code's 5).
REQUIRED FIX:
Rename the token only; change no design. Replace `CLIENT` with `CUST` in all five places, keeping the existing 4-digit form and the existing independent-token design:
1. `parts\section_3_functional_core.md` L737 (M5-19): "of the form `CUST-nnnn`" and "…discuss \"the 4417 lot\"…" (unchanged).
2. `parts\section_3_functional_core.md` L748 (M5 blinding table): "Customer pseudonym (`CUST-nnnn`)".
3. `parts\section_3_functional_core.md` L951 (M6 field table): `CUST-nnnn`.
4. `parts\section_4_functional_support.md` L1462 (M21-16): "for example `CUST-4417`".
5. `parts\section_3_functional_core.md` L796 (M5-25) — missed by the reviewer: "**customer** identity is masked by default in the testing interface".

Do NOT tie the pseudonym to the Customer code or widen it to five digits: `section_5_nfr_plan.md` L429 already stores it as an independent customer attribute (`pseudonym VARCHAR(20)`, `blind_token CHAR(8)` with a unique index), and independence from `C-<5>` is the point of a pseudonym.

Optionally, to close the "where is this identifier declared" gap properly, add one sentence to M5-19 rather than a new derivation rule: "The pseudonym is allotted per customer from its own independent sequence, is unrelated to and not derivable from the Customer code `C-<5>`, and is stored on the customer master (see the customer table in Section 5)." If §10.1 is meant to be exhaustive for system-issued identifiers, add a row there for it with reset rule "never resets" and visibility "everyone, including the Tester"; otherwise no change to Section 2 is needed, since §8.3 and WF-23 are already correct and win by definition.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
Half of this finding is confirmed; the other half is wrong, and the severity is inflated.

CONFIRMED (prong 1 — the forbidden word). All four cited passages exist verbatim:
- `D:\Prashant_WorkSpace\TTL Software\parts\section_3_functional_core.md` L737 (M5-19, `CLIENT-nnnn`), L748 (M5 blinding table row "Customer pseudonym (`CLIENT-nnnn`)"), L951 (M6 field table, `CLIENT-nnnn`)
- `D:\Prashant_WorkSpace\TTL Software\parts\section_4_functional_support.md` L1462 (M21-16, `CLIENT-4417`)
- `parts\section_2_workflow.md` L510 lists "Client, Party, Firm, Sender" under "Do **not** call it" for **Customer**, and L558 WF-23 [MUST] requires those words be "used identically in the user interface, in the database table and column names, in printed documents and in all project correspondence", with acceptance check "Open any five screens and any five tables; the words match the table exactly."
So the spec's own MUST is violated by the token it prints on every blinded screen, worksheet and label. I checked all eight other occurrences of "client*" in the assembled spec (SPEC_body.md L2146, L2178, L2240, L4816, L5035, L5262) — every one is the legitimate client/server sense and not in scope of §8.3. The reviewer also MISSED a fifth breach: `section_3_functional_core.md` L796 (M5-25) reads "**client** identity is masked by default in the testing interface".

DOES NOT STAND (prong 2 — "undeclared second identifier", digit mismatch, must derive from `C-<5>`). This is a misreading:
- The pseudonym is declared, twice. `section_4_functional_support.md` L1702 lists "blinding pseudonym" as a **Customer LIMS-only master attribute** (LIMS is master, not pushed to ERP), and the physical data model in `section_5_nfr_plan.md` L429 gives the customer table columns `blind_token CHAR(8)`, `pseudonym VARCHAR(20)` with a **unique index on `blind_token`**. It is a stored, uniquely-constrained customer attribute, not a stray string.
- Nothing in the document says the pseudonym derives from the Customer code, so there is no contradiction to resolve. Deriving it from `C-<5>` is a design change, and arguably a worse one: an independent stored token is the standard pseudonymisation design precisely because it has no arithmetic relationship to a viewable identifier.
- The "4 digits vs 5" point collapses: M5-19's `nnnn` and M21-16's `CLIENT-4417` are consistent **with each other**; they differ only from a series they were never claimed to derive from. §10.1's Customer code row is about a different identifier with a different audience.
- The fix's clause "the real Customer code `C-<5>` is never shown to a blinded role" is already stated three times: §10.1 ("Front Desk, Accounts, Section Head, Unit Incharge. **Not** the Tester ... Never printed on a tester's worksheet or sample label"), M5-18, and M21-15.

FIX IS WRONG AS PROPOSED. `CUST-nnnnn` widened to 5 digits and "derived once from the customer code by a one-way function under a server-side secret" invents a cryptographic mechanism absent from the document, contradicts `pseudonym VARCHAR(20)` / `blind_token CHAR(8)` being independent stored columns, and specifies a keyed hash with a 5-digit output space (~100k) that would collide. It also leaves the M5-25 and M6-field-table occurrences unfixed.

SEVERITY. Major is inflated. Both readings produce identical system behaviour; no developer would build the wrong thing. The defect is a one-word token rename in five places that would fail WF-23's acceptance check. Minor.

====================================================================================================
FINDING 22/25  [Minor] [contradiction]  (raised by consistency reviewer)
LOCATION: Part C M3-01 (section_3_functional_core.md L371) against Part C M10 field-table heading (L1595) and field "Conditioning job number" (L1601), and Part A §4.1/OPEN-Q7
EXACT TEXT IN THE DOCUMENT:
| M3-01 | The system maintains three distinct record types with a strict parent-child relationship: one **Test Request** (the contract), many **Samples** (one per physical item or lot received), and many **Test Allocations** (one per sample-and-test pair). The word "job" is not used anywhere in the interface. | [MUST] |
PROBLEM:
M3-01 makes a testable [MUST] promise that no interface uses the word "job", and the same Part C then names a screen "M10 field table — Conditioning job entry screen" with a field "Conditioning job number", and M10-05/M10-15 speak of "A conditioning job" and "before the job can proceed". Part A uses it too ("record customer telephone and counter conversations against the job"; OPEN-Q7 "record the overlap on the job"). Section 2 §8.4 permits exactly one concession — "the screen may print `Allocation No.` with the subtitle `(Job No.)` for one release only" — which does not cover a conditioning screen. M3-01's acceptance check will fail against M10's own screen.
REQUIRED FIX:
Use the vocabulary the document has already settled instead of new coinages.

1. parts/section_3_functional_core.md L1595: rename the heading to "### M10 field table — Conditioning Certificate entry screen". L1601: rename the field to "Conditioning Certificate number" and replace the Validation cell "From its own series" with "From the gap-free `DVM/CM/<FY>/<4>` series (Part B §8.4)". This matches §8.3 L530 ("Label on screen: Conditioning Certificate") and the numbering table at §8.4 L738, and closes the currently unnamed series. Do NOT use "Conditioning run" (not in the settled vocabulary) or "Conditioning worksheet number" (collides with the existing **Worksheet** entity `txn_worksheet` / `DVM/WS/2026-27/<5>`, whose "Gaps permitted" rule is the opposite of the conditioning series' gap-free rule under WF-99).

2. parts/section_3_functional_core.md L1537 (M10-05): "A conditioning job is registered against a **lot**" → "A conditioning record is registered against a **lot**". L1557 (M10-15): "before the job can proceed" → "before drawing can proceed".

3. parts/section_3_functional_core.md L371 (M3-01): qualify the promise rather than leaving it absolute, because §8.3 L522 and §8.4 L545 already permit `(Job No.)` on screen. Replace the last sentence with: "No screen names any of these three records a \"job\"; the word's only permitted appearance in the interface is the transitional `(Job No.)` subtitle on the Allocation number allowed by Part B §8.4 for one release." Then extend the acceptance check, which currently tests only the 1/5/15 counts and does not test the vocabulary promise at all: append "; a full-text scan of screen labels and printed templates returns no occurrence of \"job\" other than the permitted `(Job No.)` subtitle."

4. parts/section_1_foundation.md L171: "against the job" → "against the Test Request or complaint", matching M19-14 (parts/section_4_functional_support.md L1139), which is the requirement that actually implements this ("a communication log entry against the order or complaint").

5. parts/section_1_foundation.md L186 (OPEN-Q7), both occurrences: "on the job" → "on the allocation", matching M5-12 (L720) and M7-05 (L1030) verbatim. Apply the identical edit to the duplicated OPEN-Q-A7 entry in parts/appendix.md L81, which the reviewer's fix omits and which would otherwise drift out of step with Part A.

6. Reconcile Part A's glossary entry at parts/section_1_foundation.md L258, which overreaches by asserting "job" is "A word deliberately **not used** in this document" — falsified by the document's own explanatory prose (section_1 L11; section_3 L238, L677, L679, L789, L830, L993, L1189, L1192) and by the unrelated infrastructure sense of the word in Parts D/E ("scheduled job", "queued job", `sys_outbound_job`, "on-the-job training"). Reword to: "A word deliberately not used as the name of any record type in this system, because in the original note it meant four different things; replaced by **Test Request**, **Sample**, **Allocation** and **Worksheet**. (The unrelated computing sense — a scheduled or queued background job — is unaffected.)" Verbatim quotations of the client's original note in parts/appendix.md L15-L55 are source material and must stay as written.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
Independently confirmed. The quoted M3-01 text exists verbatim at parts/section_3_functional_core.md L371 (SPEC.md L1616), including the absolute [MUST] clause "The word \"job\" is not used anywhere in the interface." The same section then specifies, at L1595, "### M10 field table — Conditioning job entry screen" and at L1601 the field label "| Conditioning job number | Text | Auto | From its own series |". Every other field table in Part C is named after a screen ("M1 field table — Customer create/edit screen", "M5 field table — Allocate / Reallocate screen", …) and the Field column holds display labels ("Mark of the lot", "Chop", "Declared weight by customer"), so this is unambiguously interface text, not internal prose. M10-05 (L1537) and M10-15 (L1557) also use "job" in requirement prose. Part A §4.1 (parts/section_1_foundation.md L171) has "record customer telephone and counter conversations against the job" and OPEN-Q7 (L186) has "on the job" twice. I searched for the term under other names and found no exemption, no rationale, and no scoping qualifier anywhere near M3-01 or in M10 that would carve out conditioning work; "conditioning job" appears nowhere in the research briefs, so it is not a required domain term.

Two corrections to the reviewer's reasoning, which is why the fix and severity need amending. First, the reviewer's headline claim that "M3-01's acceptance check will fail against M10's own screen" is not true: M3-01's acceptance check reads "Register five samples with three tests each; the system shows 1 request, 5 samples and 15 allocations" and does not test the word "job" at all. The contradiction is in the requirement text; the promise is in fact untested. Second, the reviewer's premise that "Section 2 §8.4 wins; M3-01's promise is the correct one and must be made true rather than weakened" is self-defeating. §8.4 (parts/section_2_workflow.md L545) expressly permits the screen to print `Allocation No.` with the subtitle `(Job No.)` for one release, and §8.3's "Label on screen" column for Allocation is literally "Allocation (Job No.)" (L522). So M3-01's absolute wording is already falsified by Part B independently of M10, and cannot be made true without deleting a concession the reviewer says should win. M3-01 has to be qualified as well as M10 renamed.

The proposed rename is also wrong on the document's own terms. §8.3 is headed "Settled vocabulary — use exactly these words", and it already settles this record: L530 gives screen label "Conditioning Certificate" for `txn_report` type `CONDITIONING`, and the numbering table at L738 already defines "Conditioning Certificate number" `DVM/CM/2026-27/<4>` as its own gap-free series (reaffirmed by WF-99, L773). "Conditioning run" is a new coinage outside the settled vocabulary, and "Conditioning worksheet number" actively collides with the existing **Worksheet** entity (`txn_worksheet`, `DVM/WS/2026-27/<5>`, "Gaps permitted", L735) whose series rule is the opposite of the conditioning series — so that fix would introduce a fresh contradiction with Part B. The reviewer also missed a third occurrence of the OPEN-Q7 wording, in parts/appendix.md L81 (OPEN-Q-A7), which would drift if only Part A is edited, and did not notice the sharper related contradiction in Part A's own glossary at L258.

Severity is inflated. The defect is real and must be fixed before baselining a [MUST], but its whole consequence is label and requirement wording: no data model, behaviour, numbering, or NABL/17025 compliance effect, and it is repaired by renaming two labels and qualifying one sentence with words the document has already settled.

====================================================================================================
FINDING 23/25  [Minor] [contradiction]  (raised by M1-M10 reviewer)
LOCATION: M5-19 and its acceptance check (line 737) against M5 rules and edge cases item 9 (line 831) and the M5 blinding table row "Sample family identifier" (line 776)
EXACT TEXT IN THE DOCUMENT:
Two samples from the same customer show the same pseudonym; a third from a different customer shows a different one.
PROBLEM:
M5-19 mandates the exact leak that M5 rules item 9 and the blinding table forbid. Rule 9 says a stable, visible family identifier "lets a tester correlate lots to a customer over time" and the table hides it for that reason — but CLIENT-nnnn is a stable, visible, per-customer identifier, which is a strictly stronger correlation channel than a family id. One approved unblinding under M5-22, one counter encounter, or one unmaskable woven mark permanently resolves CLIENT-0417 to a named firm, and thereafter every lot that firm ever sends is identified to the tester, together with the ability to see that this customer's previous lots were graded down. The document also cannot decide what the pseudonym is for: the stated purpose is that staff can discuss "the 4417 lot", which is a per-sample need, not a per-customer one.
REQUIRED FIX:
Do not invent LOT-nnnn and do not edit M5-19 alone. Choose one of two coherent repairs.

Repair A (recommended; keeps the research-backed design and is the smaller edit). Amend rule 9 at SPEC.md:2076 so it no longer rests on a rationale the spec deliberately contradicts: "**Hide the sample family identifier so the tester cannot find which earlier sample this one repeats.** The flag 'repeat exists' is safe; the identifier would let the tester locate the earlier sample and anchor on its result. Note that the stable customer pseudonym (M5-19) does deliberately allow a customer's lots to be correlated over time — that is intended, for quality-pattern visibility (M21-16). The family identifier is hidden for the anti-anchoring reason, not to prevent correlation." Then close the residual the document currently omits: extend M5-25's honest wording, and the impartiality control-effectiveness report of M5-24/M21-23, to state that the stable pseudonym is a persistent re-identification key, so that any single leak (an approved unblinding under M5-22, a counter encounter, an "identity not maskable" sample) resolves that customer's whole past and future history for that tester; and add a [SHOULD]: "The pseudonym may be configured to be re-derived per financial year, or replaced by a per-sample label, for sections where cross-lot correlation by testers is not wanted." Also strengthen the repeat handling already in the unavoidable-cases row (allocate a repeat to a different tester) from "should" to a requirement where staffing permits, since the pseudonym weakens the hidden-earlier-result control.

Repair B (only if the laboratory genuinely wants no per-customer correlation by testers). Then every dependent place must change together, and no new identifier is needed because the tester already sees the sample number (SPEC.md:1992) and the derived allocation number (SPEC.md:1176): (i) M5-19 — remove the per-customer pseudonym from blinded views, state that the visible sample/allocation number serves the "discuss the 4417 lot" need, and keep a stable customer pseudonym for Section Head and above; (ii) M21-16 (SPEC.md:4387) — rewrite identically, and drop "so that patterns remain visible for quality purposes" or reassign that purpose to unblinded roles; (iii) change, not duplicate, the blinding table row at SPEC.md:1993 to "Stable customer pseudonym | **Hidden** | Visible | Visible | Visible | Visible | Visible"; (iv) SPEC.md:2196 M5 field table; (v) M20-20 (SPEC.md:4125) — masked register prints the sample number, not the pseudonym; (vi) confirm M21-72 (SPEC.md:4594) still works, since its retention pseudonymisation depends on the per-customer token continuing to exist; (vii) mst_customer at SPEC.md:5145 keeps pseudonym/blind_token but they become Section-Head-and-above fields. In either repair, replace the reviewer's unimplementable acceptance check with: "As a tester, open two samples from the same customer: no field in either response identifies them as belonging to the same customer, and no stable customer code appears. As a Section Head, both show the same customer pseudonym."
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
VERIFIED TEXT. All three anchors exist verbatim. SPEC.md:1982 (= parts/section_3_functional_core.md:737) carries M5-19 and the acceptance check "Two samples from the same customer show the same pseudonym; a third from a different customer shows a different one." SPEC.md:2021 (part line 776) is the row "Sample family identifier | **Hidden (the flag is shown, the identifier is not)** | Visible | ...". SPEC.md:2076 (part line 831) is rule 9: "Do not let the sample family identifier leak the customer... The identifier itself, if visible and stable, lets a tester correlate lots to a customer over time." Nothing was invented or paraphrased.

SEARCHED FOR A RECONCILIATION ELSEWHERE (pseudonym, blind_token, family, rotate, repeat). None exists, and the search made the tension sharper rather than resolving it: SPEC.md:4387 M21-16 restates M5-19 with a second rationale — "so that patterns remain visible for quality purposes" — with the same acceptance check. So the document states, as a MUST in one section, that a tester correlating a customer's lots over time is a desired quality feature, and states in the neighbouring rules list that the same capability is the harm justifying the concealment of a weaker field. The pseudonym is per-customer, not per-sample (mst_customer.pseudonym / blind_token at SPEC.md:5145), so it is a strictly broader correlation channel than the family identifier it is meant to be protected from. Hiding the family identifier therefore buys very little: a tester can already group a customer's history by CLIENT-0417.

THE SUBSTANTIVE PART IS ALSO REAL. Two concrete consequences the document never addresses: (a) M5-24/M21-23 require recording "identity not maskable" samples (woven selvedge, printed bale cloth) and M5-22 permits approved unblinding — in either case the tester sees the firm and CLIENT-0417 on the same screen once, and a never-changing per-customer code makes that one-off leak a permanent key to every past and future lot of that firm; (b) the table row "The earlier related sample's results | Hidden" exists to stop anchoring ("Knowing 'this failed last time at 22.4 denier' biases the reading"), yet a stable pseudonym re-links a tester to a result they produced themselves. The reviewer is also right that M5-19's own stated purpose does not justify per-customer stability: the tester already sees the sample number (SPEC.md:1992) and the allocation number derived from it, "Everyone, including the Tester" (SPEC.md:1176), so "discuss the 4417 lot" is already satisfied.

WHERE THE REVIEWER OVERREACHES. (1) Severity. No requirement is unimplementable or ambiguous; a builder knows exactly what to build, and the family-identifier row keeps an independent valid purpose (not pinpointing which earlier sample this repeats). The stable pseudonym is not a slip — parts/research_gap_analysis.md:510 recommends it explicitly ("makes cross-job patterns visible for QC without disclosure"), and the spec's own deviation from the brief (which put the "linked-sample family token" in Tier 0, always visible) is what created the clash. The document also already refuses to claim blinding is absolute (M5-25, a MUST) and enumerates residual channels. What is actually defective is an incoherent rationale in a prose rules list plus one missing residual-risk disclosure — Minor, not Major. (2) "M5-19 mandates the exact leak that rule 9 forbids" is too strong: rule 9 is about leaking who the customer is, which the pseudonym by design does not do. The real clash is between rule 9's rationale and M21-16's rationale.

THE PROPOSED FIX IS WRONG AND WOULD CREATE NEW CONTRADICTIONS. It edits only M5-19 and leaves M21-16 (SPEC.md:4387) mandating the stable per-customer pseudonym with the identical acceptance check — converting a rationale tension into a flat M5-vs-M21 contradiction. It says "add the corresponding row to the blinding table" when a row already reads "Customer pseudonym (`CLIENT-nnnn`) | Visible" for the tester (SPEC.md:1993); adding a second, opposite row contradicts the same table. It ignores the M5 field table (SPEC.md:2196), M20-01/M20-20 (SPEC.md:4106/4125, which specify the pseudonym in place of the customer name on the inward register for masked viewers), M21-72 (SPEC.md:4594, retention pseudonymisation via "the opaque customer token already used elsewhere"), and mst_customer (SPEC.md:5145). Its LOT-nnnn is a redundant third identifier for something already visible. And its acceptance check — "no field visible to a tester is common to both" — can never pass, since two samples from one customer legitimately share sample type, declared size, method, section and the repeat flag.

====================================================================================================
FINDING 24/25  [Minor] [contradiction]  (raised by M1-M10 reviewer)
LOCATION: M5-12 and its acceptance check (line 720) against M7-04 and M7-05 (lines 1029–1030)
EXACT TEXT IN THE DOCUMENT:
Allocate and later verify as the same person; the overlap and its justification are recorded and appear on a monthly report.
PROBLEM:
M5-12's acceptance check requires the system to let one person verify their own work with a justification note, while M7-04 says "By default the verifier must not be the person who entered the result. The default is enforced" and M7-05 permits it only under a dated, named-method exception. The M5-12 check cannot pass on a default installation, and M5-12's own wording ("It does not silently permit it") reads as permitted-with-a-record rather than blocked. A developer implementing M5 will build a warn-and-record path; a developer implementing M7 will build a block. In a three-person unit this is the code path that decides whether work can flow at all on a day when one person is on tour.
REQUIRED FIX:
Amend M5-12 to (changes: foreseen-overlap framing, explicit statement that permission is decided elsewhere, authorisation case retained, no "solely"):

"Where the system can foresee at allocation time that the same person is likely to end up performing and later verifying or authorising the same work (a real situation in a three-person unit) — because no other person holding the required 'technical review' or signatory authorisation for that method is available — it warns at allocation, records the foreseen overlap on the allocation, and counts it on a monthly report, so the Unit Incharge can decide in advance whether to raise a small-laboratory exception under M7-05, use the M13-10 override for an authorisation overlap, or hold the work. Allocation itself is not blocked. Whether the overlap is actually permitted at the later verification or authorisation step is governed by M7-04, M7-05 and M13-10, not by this requirement, which only ensures the overlap is never silent."

Amend the acceptance check to:

"Allocate a cohesion test where the proposed tester is the only holder of the technical-review authorisation; the warning appears, the foreseen overlap is recorded on the allocation, and the monthly report counts it. Then attempt verification by that same person with no M7-05 exception in force; verification is blocked per M7-04. Enable the M7-05 exception for that method and repeat; verification succeeds and the occurrence is recorded on the allocation with its justification and counted."

Do not use the word "solely", and do not narrow M5-12 to verification only — M13-10 governs performer != authoriser and WF-12 defines a per-section single-analyst mode, so an exclusive pointer to M7-04/M7-05 would introduce a fresh contradiction. Separately worth raising as its own finding (out of scope here): the document offers three different mechanisms for the same overlap — WF-12's per-section "single-analyst mode", M7-05's dated per-method exception, and M13-10's per-occurrence Approving Authority override that per SPEC.md:4479 raises a nonconformity. These should be reconciled to one.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
The quoted text is genuine (parts/section_3_functional_core.md:720, SPEC.md:1965), and a real defect exists: M5-12's acceptance check "Allocate and later verify as the same person" cannot be executed on a default installation, because M7-04 (line 1029) enforces the block by default and M7-05 (line 1030) correctly states its own precondition ("Enable the exception for one method for one month") while M5-12's check states none. M5-12 also never points to where the permission decision actually lives, so a tester running the M5-12 check verbatim will fail a [MUST] requirement.

However the reviewer overstates it as a contradiction of Major severity. M5-12 is a *recording* requirement, not a permission grant — it demands the same record M7-05 already demands ("recorded on the allocation with a justification... reported monthly with a count"), and it sits directly beside M5-11, which is an allocation-time warn-and-confirm. Allocation is not verification, so warn-and-record at allocation does not conflict with a block at the verification action. Block-by-default is stated five independent times in the document, not two: M7-04 (SPEC:2274), M13-10 (SPEC:3263, "refused by default; after override..."), WF-12 (SPEC:704, "Attempt self-verification with the rule on. Refused"), WF-76 (SPEC:1127), and regression test #4 (SPEC:4479, "Refused unless overridden, and the override raises a nonconformity"). Against that weight, the claimed risk of a developer building a permissive path from M5-12 is low, and no functional behaviour is genuinely in doubt. This is a testability and cross-reference defect in one acceptance check, fixable in one line — Minor, not Major.

The proposed fix is wrong as written. "whether self-verification is later permitted is governed solely by M7-04 and M7-05" creates a new contradiction with M13-10, which independently governs performer != authoriser via an Approving Authority override with mandatory justification, and with WF-12's per-section single-analyst mode. The fix also silently drops M5-12's "or authorising" scope, which M7-04 and M7-05 do not cover at all (only M13-10 does). Finally, its trigger condition — "no verifier other than the proposed tester will be authorised for that method" — would not fire in the tour/leave scenario the reviewer's own rationale cites, since the absent colleague still holds the authorisation.

====================================================================================================
FINDING 25/25  [Minor] [unverified-fact-as-fact]  (raised by M1-M10 reviewer)
LOCATION: M1-44 (line 108); M6-09 (line 872)
EXACT TEXT IN THE DOCUMENT:
Neatness in particular must be an enumerated picker with the values 100, 90, 80, 70, 60, 50, 30, 10 — because the method judges panels against a fixed set of official standard photographs and no other value exists.
PROBLEM:
The claim "no other value exists" is stated as fact and is not supported. The eight-photograph set is the BIS/IS set, but the ISA method that this unit also sells carries a rounding convention — values from 100 to 50 to the nearest 5 per cent, below 50 to the nearest 10 per cent — which makes 95, 85, 75 and 65 legitimate reportable neatness assessments. The research also records that BIS deliberately diverges from ISA on some grading values. Hard-coding one list at Parameter-master level, as M1-44 requires, will block a valid ISA neatness assessment on the roughly 69 ISA gradings a year, and the tester will round to the nearest permitted value, silently altering a graded result.
REQUIRED FIX:
Do not restructure the data model - M14-06 already holds parameter definitions per method version and M1-43 already carries permitted enumerated values, so the reviewer's sentence would create a contradiction. Instead:

(a) M1-44 (parts\section_3_functional_core.md:108): delete "and no other value exists". Replace the justification with: "...because the method judges panels against a fixed set of official standard photographs. The permitted value list is held on the per-method-version parameter definition of M14-06, not as a global property of the parameter, because the same characteristic may be judged against different official value sets under different methods. The eight values above are the IS 15090 Part 9 / BIS set and are seeded as unconfirmed; the ISA set is not yet established (see OPEN-Q below)." Change the acceptance check to: "Under the BIS method revision 85 is not selectable for neatness; the picker offers only the values configured for that method revision, and the same parameter code can carry a different value list under another method revision."

(b) M6-09 (same file, line 872): change "Neatness offers only 100, 90, 80, 70, 60, 50, 30, 10" to "Neatness offers only the values configured for the method revision in force - under the BIS method revision, 100, 90, 80, 70, 60, 50, 30, 10." Adjust the acceptance check to name the BIS method revision rather than asserting eight values absolutely.

(c) Add a cross-reference in M1-43 stating that where M14-06 defines a per-method-version value list, that list governs, so the Parameter master value list is a default only.

(d) Fix the conflation at parts\section_2_workflow.md:347 (SPEC.md:789): "Average neatness (%)" is a computed value under M6-06 (mean of the panel ratings, then the method's rounding rule), not a value chosen from the photograph set. Only the individual panel rating is picked from the photograph set. Leave M14-08 unchanged.

(e) Add OPEN-Q: "Confirm against a real ISA grading worksheet (i) the permitted neatness value set under the ISA method, and (ii) whether the FAO rounding convention recorded in the research (100-50% to the nearest 5%, below 50% to the nearest 10%) applies to the individual panel assessment or only to the reported average and low neatness. Recommended default until answered: seed the BIS eight-value list for the BIS method revision, and hold the ISA neatness value list as unconfirmed rather than assuming it matches BIS." Do not seed a specific 15-value ISA list as the reviewer proposes - that list follows only from the unverified reading of the convention's scope.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
VERIFIED, but narrower than claimed.

1. Quote is verbatim and locations are right. M1-44 at D:\Prashant_WorkSpace\TTL Software\parts\section_3_functional_core.md:108 (SPEC.md:1353) and M6-09 at the same file line 872 (SPEC.md:2117), both including "and no other value exists".

2. The overstatement is real and is contradicted by the spec's own source. Research finding #29 at D:\Prashant_WorkSpace\TTL Software\parts\research_silk_domain.md:456 is marked [F, S4, S5] and reads: "The neatness set is discrete: 100, 90, 80, 70, 60, 50, 30, 10. So the neatness input control is an enumerated picker, not a free number — and FAO adds the rounding convention (100-50% to the nearest 5%, below 50% to the nearest 10%)." The spec carried the first half forward and silently dropped the second, then hardened what remained into an absolute ("no other value exists") used as the justification for a [MUST]. That is exactly unverified-fact-as-fact.

3. The supporting facts check out. ISA grading is genuinely sold at this unit (research_silk_domain.md:58-59, two ISA price variants; the Dharmavaram booking portal list at line 86 includes "Raw silk testing & Grading - ISA x2"), and the "roughly 69 a year" figure is correctly attributed to Dharmavaram's own FY 2021-22 mix, not to a comparator unit ("All test (ISA) | 69 | 0.6%", research_silk_domain.md:159, of 11,294 samples). BIS/ISA divergence is also recorded at research_silk_domain.md:431 ("BIS deliberately diverges from ISA" on maximum-deviation values).

WHY SEVERITY DROPS TO MINOR - two of the reviewer's three load-bearing claims are already handled:

4. The structural capability the reviewer says is missing already exists. M14-06 (SPEC.md:3370) holds "parameter definitions per method version" with data type "list of allowed values"; M1-45 links parameters to a method revision; M1-36 stores rounding rule and significant figures per method revision; M1-49/M1-50 already require "a BIS grading set and an ISA grading set as separate records". So the fix's core sentence ("belongs to the method revision, not to the parameter") is not a new capability - it is already Module 14's model, and as written it would newly contradict M1-43, which puts "permitted enumerated values where applicable" on the Parameter master. The fix is imprecise: it rewrites M1-44 without reconciling M1-43 and M14-06.

5. The claimed mechanism is itself an inference presented as settled. The research says FAO "adds the rounding convention" but never says whether it governs the per-panel photograph comparison or the reported result. The spec frames the picker as panel ratings (SPEC.md:848 "Neatness, cleanness, evenness panel ratings"), while M6-06 (SPEC.md:2117 area, line 2114) makes "average neatness, low neatness as the mean of the worst one-fifth of panels" computed per method. If the convention governs the reported average rather than the panel rating, the picker restriction is defensible and the rounding rule already has a home in M1-36. The reviewer asserts the stronger reading, and a specific 15-value ISA seed list, as fact.

6. One adjacent defect I found does reinforce the overstatement and should be swept in: SPEC.md:789 (parts\section_2_workflow.md:347) says "Average neatness (%, chosen from the standard set 100, 90, 80, 70, 60, 50, 30, 10)" - but M6-06 makes average neatness a computed mean of twenty panels, which will not land on one of eight values under BIS either. So the false premise has already produced a conflation inside the document.

7. M14-08 (SPEC.md:3372) repeats the eight values but frames them as an example of a configurable value list and makes no absolute claim; it needs no change.

Net: the unsupported absolute claim is confirmed and the two [MUST] acceptance checks would reject correct ISA behaviour, so this is worth fixing - but it is a wording and acceptance-check defect on 0.6% of volume, not the data-model gap the reviewer describes.

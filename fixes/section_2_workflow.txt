====================================================================================================
FINDING 1/14  [Major] [contradiction]  (raised by consistency reviewer)
LOCATION: Part A §4.1 role table (L169–L182) against Part B §7.2, §9.1, §9.2, §10.1 and Part D M21.3 (L1356)
EXACT TEXT IN THE DOCUMENT:
| **Lab In-Charge (Scientist, e.g. Scientist-D) — Approving and Signing Authority** |
PROBLEM:
Section 1's role table is the document's only definition of roles, and it does not contain most of the actors the rest of the document uses. Section 2 assigns work to **Unit Incharge**, **Section Head**, **Verifier**, **Report Writer**, **Sample Receipt Clerk** and **Approving Authority** (§7.2 steps 5, 11, 16, 17, 18; Tables A and B; §10.1 visibility column); Part C adds **Authorised Signatory** and **Equipment Custodian**. None of these seven appears in the Section 1 table. Worse, Part D's permission matrix redefines the relationships the opposite way round from Section 1: "**LI** Lab In-Charge / Section Head · **AA** Approving Authority (Unit In-Charge)" makes Lab In-Charge and Section Head one role and separates them from the Approving Authority, whereas Section 1 makes Lab In-Charge *be* the "Approving and Signing Authority". Section 2's WF-41 requires them to be distinct ("Attempt to clear a payment hold as Section Head. Refused… Override as Unit Incharge"). The developer has no canonical role list and cannot build the permission matrix, and an assessor reading the quality manual would find two incompatible authority structures. The spelling also varies: "Unit Incharge" (Part B), "Unit In-Charge" (Part D), "Lab In-Charge" (Parts A, C, D).
REQUIRED FIX:
Do not mass-rename; make four targeted edits and let the existing WF-23 mechanism govern them.

1. Part A §4.1 (parts\section_1_foundation.md L172): change the row heading to "**Approving Authority (Scientist, e.g. Scientist-D; the Unit Incharge at Dharmavaram)**". Use "Approving Authority", NOT "Unit Incharge", because Part B §8.3 already settles that label for "person authorised to sign" under WF-23 [MUST]. Add three new rows to the same table for the roles genuinely absent — **Verifier**, **Section Head**, **Report Writer** (drawing their duties from Part B §7.2 steps 11/16/17 and §9.1/§9.2) — and add one line to the §4.1 preamble recording the synonyms rather than new rows: "Front Desk / Receipt Clerk is called **Sample Receipt Clerk** in Part B; Store Keeper is called **Store Keeper / Equipment Custodian** in Part D; the person holding the Approving Authority role is referred to by post as the **Unit Incharge**."

2. Add two rows to Part B §8.4 "Words we will stop using": "**Lab In-Charge**" and "**Unit In-Charge**" → "Say **Approving Authority** for the signing role or **Section Head** for the sectional supervisor — state which is meant." This puts the rename under the existing WF-23 [MUST] instead of an ad-hoc sweep, and picks up the 7 Part D + 3 appendix occurrences of the hyphenated spelling.

3. Part D M21.3: decide per context which column each "Lab In-Charge" instance is, then re-mark. Because Part D's uses are overwhelmingly the unit's technical head, the safest resolution is to merge the LI column into AA and keep a separate **SH Section Head** column carrying only the permissions Part B actually grants a Section Head (§7.2 step 11 allocation; §9.1/§9.2 hold/release, conditioning gate, reassignment, retract-to-In-Test). If the LI column is instead relabelled SH, then its "Payment hold — override" mark MUST change from "A" to "–", or the matrix contradicts WF-41's acceptance check; the same audit is needed for LI's "A" marks on "Method version — activate", "Nonconformity / corrective action — close", "Concession / waiver (to limit)", "Equipment block — override" and "Expired batch — override".

4. Resolve the third conflict, which the finding does not mention: Part D's legend asserts AA = Unit In-Charge (one role), but Part B §7.2 step 22 ("Unit Incharge + Approving Authority") and §9.2's "Verified" and "Under Verification" rows list Unit Incharge and Approving Authority as two distinct actors. State once, in Part A §4.1, whether these are one role or two, and make Part B §9.2 and §7.2 step 22 consistent with the answer. Fold this into OPEN-Q-A6, which already asks who may sign.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
QUOTE VERIFIED. Exact verbatim at D:\Prashant_WorkSpace\TTL Software\parts\section_1_foundation.md L172 (= SPEC.md L253). Part D legend verified verbatim at parts\section_4_functional_support.md L1356 (= SPEC.md L4281): "**LI** Lab In-Charge / Section Head · **AA** Approving Authority (Unit In-Charge)".

CONTRADICTION IS REAL AND CONFIRMED. Part A §4.1 makes "Lab In-Charge" the signer ("authorise and sign the certificate"). Part D's M21.3 matrix row "Report — authorise and issue" marks LI as "–" and only AA as "A"; likewise "Result — retract after verification" is AA-only. Part D also uses the two as separate parties throughout (M11-20 "only by the Lab In-Charge or Approving Authority"; M12-11 "overridable only by the Lab In-Charge... flagged for the Approving Authority"; M13 Authorisation Matrix "Lab In-Charge, Approving Authority"). So the identical label "Lab In-Charge" is the top signing authority in Part A and a subordinate non-signing role in Part D — inverted, on the system's most regulated control.

Part B usages verified: §7.2 step 5 "Unit Incharge or Section Head", step 11 "Section Head", step 16 "Verifier", step 17 "Report Writer", step 18 "Approving Authority", steps 7-8 "Sample Receipt Clerk", step 22 "Unit Incharge + Approving Authority"; §9.1/§9.2 state tables (L580-609) and §10.1 series table (L728-750) use Section Head / Unit Incharge / Verifier / Report Writer. WF-41 (L654) verified verbatim, and it does require Section Head ≠ Unit Incharge. Spelling split verified by count: "Unit Incharge" 3/44/69/0/80 across Parts A-E, "Unit In-Charge" only in Part D (7) and appendix (3), "Lab In-Charge" only in Part A (3) and Part D (95). A third conflict the reviewer missed: Part D says AA = Unit In-Charge (one role), but Part B §7.2 step 22 and §9.2's "Verified"/"Under Verification" rows list them as two separate actors.

BUT THE REVIEWER OVERSTATES THE OMISSION HALF, AND SEVERAL CLAIMS ARE WRONG. (a) "Section 1's role table is the document's only definition of roles" is false: Part B §8.3 "Settled vocabulary — use exactly these words" (L505-539) is a normative table enforced by WF-23 [MUST], and it settles "Person authorised to sign = **Approving Authority**"; Part D M21.3 is a complete 10-role x ~70-permission matrix that M21-13 requires to be printable for the quality manual and M21-11 makes configurable data. So "the developer has no canonical role list and cannot build the permission matrix" is not correct — a developer would seed from M21.3. (b) Of the seven allegedly absent actors, only four are genuinely missing from §4.1 (Verifier, Section Head, Report Writer, Approving-Authority-as-distinct-role). "Sample Receipt Clerk" is the §4.1 row "Front Desk / Receipt Clerk"; "Equipment Custodian" is Part D's own gloss on §4.1's "Store Keeper" ("SK Store Keeper / Equipment Custodian"); "Authorised signatory" is defined in Part A §5 glossary (L211). (c) "Part C adds Authorised Signatory and Equipment Custodian" is half wrong: Equipment Custodian appears only in Part D (14 occurrences in section_4, zero in section_3). (d) §4.1's preamble and the Technical Manager row ("In a small unit this is usually the same person as the Lab In-Charge; the roles are kept separate so the record shows which capacity the person was acting in"), plus OPEN-Q-A6 (which asks exactly who may sign and defaults to "the Unit Incharge as the only signatory at go-live"), already handle the one-person-many-roles problem — they do not, however, reconcile the two role labels.

SEVERITY: Major, not Blocker. Nothing is unbuildable — M21.3 is a complete normative matrix, §8.3/WF-23 settle the signer's name, and OPEN-Q-A6 flags the signatory question — but a reader of Part A and a reader of Part D get opposite answers to "may the Lab In-Charge sign a report", and an assessor handed both would see two authority structures. Must be fixed before the document is used as a quality-manual input.

FIX IS PARTLY WRONG AND WOULD INTRODUCE NEW CONTRADICTIONS. (i) It is self-inconsistent: it says "exactly the ten roles" then lists fifteen. (ii) Standardising on "Unit Incharge" as the approving-authority label directly violates Part B §8.3 + WF-23 [MUST], which settle that concept's name as "Approving Authority" and require that word be used identically in UI, database and printed documents; renaming the matrix column from AA to "UI" makes it worse, not better. (iii) Mapping Part D's 95 "Lab In-Charge" instances to "Section Head" is substantively wrong for most of them — they are the unit's technical head (equipment condemnation, batch quarantine release, purchase-indent approval, staff and competence records, access review, unblinding approval) in a three-person unit that has no sections. (iv) Applied literally it breaks WF-41: the LI row carries "Payment hold — override = A", and relabelling LI to Section Head makes the matrix grant exactly what WF-41's acceptance check requires to be refused.

====================================================================================================
FINDING 2/14  [Major] [contradiction]  (raised by consistency reviewer)
LOCATION: Part A §5 Glossary rows "Job" (L258) and "Sample-test" (L284) against Part B §8.3 and §8.4
EXACT TEXT IN THE DOCUMENT:
| **Job** | A word deliberately **not used** in this document, because in the original note it meant four different things; replaced by **order**, **sample** and **sample-test**. |
PROBLEM:
The glossary names a replacement set that Section 2 does not use. §8.4 replaces "Job" with four things — "**Test Request**, **Sample**, **Allocation** or **Worksheet** — whichever is meant" — and §8.3 settles the assignable work unit as "**Allocation**" on table `txn_sample_test`, label "Allocation (Job No.)". The glossary instead invents "**Sample-test**" and gives it its own entry ("| **Sample-test** | One test on one sample: the smallest unit of work that can be given to a person, tracked, timed and priced. |"), which is a second customer-facing name for the same concept — exactly the failure mode §8.3 exists to prevent. The glossary also omits **Allocation**, **Authorisation**, **Sender**, **Reading** and **Request Line** altogether while claiming "Every term used anywhere in this document is defined here in one plain sentence."
REQUIRED FIX:
Keep the reviewer's Job-row rewrite exactly as proposed (it reproduces §8.4's four-word set faithfully): "| **Job** | A word deliberately **not used** in this document, because in the original note it meant four different things; say **Test Request**, **Sample**, **Allocation** or **Worksheet**, whichever is meant. |"

Then correct three things in the rest of the fix.

(1) Placement. The glossary preamble at L200 states "Terms are in alphabetical order", so do not replace the Sample-test row in place with an "Allocation" row. Delete L284 outright and insert the new row in the A-block, between "Aliquot" (L206) and "Amendment (of a report)" (L207).

(2) Wording. Do not copy the screen label into the glossary — §8.4 permits "Allocation No. / (Job No.)" only "for one release only", so restating it in §5 creates a second normative statement about labels that will drift. Keep the house one-sentence style and cross-reference instead:
"| **Allocation** | One test on one sample: the smallest unit of work that can be assigned to a person, tracked, timed and priced; the settled name for what the original note called a "job", held on `txn_sample_test` — see the settled-vocabulary table in 8.3. |"

(3) Sourcing for the added rows. "Using the §8.1 definitions verbatim" is not executable: §8.1 has no standalone **Authorisation** entry (the concept appears only in the §8.3 row "Signing and issuing the document"), and every §8.1 entry is a multi-paragraph block with *Example* and *It is NOT* prose that cannot go verbatim into a one-plain-sentence table. Source each from §8.3's Concept column, condensed to one sentence, each in alphabetical position:
- **Authorisation** — the recorded act of signing and issuing a report, distinct from Verification, which is the check on the numbers.
- **Reading** — one raw value as first read from an instrument or judged by eye, before any calculation ("observation" is acceptable in speech only).
- **Request Line** — one requested test, at one agreed price, for a stated number of samples, within a Test Request.
- **Sender** — the person or agency that physically handed over or despatched this particular consignment, recorded on the Sample and never the Customer.
- **Specification Set** — a named, dated set of limits or grade bands against which results are judged.

Additionally, while in the same table, align the headword at L273 "| **Order (Test Request Form, TRF)** |" to the settled word: make it "| **Test Request (TRF)** |" in the T-block, with a pointer row "| **Order** | See **Test Request**. |" — §8.3 settles "Test Request" for this entity, so leaving "Order" as the primary headword reproduces the same defect one row over.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
Independently confirmed on all counts. The two quoted glossary rows exist verbatim at parts/section_1_foundation.md L258 and L284, and the completeness claim at L200 is quoted accurately. The contradiction is real and is forbidden by the document's own [MUST]: section_2_workflow.md §8.3 (L506) settles the concept "One test on one sample — the assignable work unit" as **Allocation** (table `txn_sample_test`, label "Allocation (Job No.)", "Do not call it: Job, Test, Task, Work order"), §8.4 (L541) says replace "Job" with "**Test Request**, **Sample**, **Allocation** or **Worksheet**", and WF-23 (L558) makes the §8.3 words mandatory "in all project correspondence" with an acceptance check. The §5 glossary instead instructs the reader to say "order, sample and sample-test" (three words, not §8.4's four) and gives "Sample-test" its own definitional row whose wording mirrors §8.3's Allocation definition — a second customer-facing name for the same entity. I searched for the concept under other names before accepting: "sample-test" occurs ZERO times anywhere else in the authored parts (only 2 hits, both in this glossary, plus one incidental hit in research_lims_reference.md), while "allocation" occurs 100x in section_2, 76x in section_3, 13x in section_5 and 6x in section_4. So the glossary is the lone dissenter, not the document's real vocabulary. The omission half also verified: a grep over the glossary block (L198-312, 106 rows) finds no entry for Allocation, Authorisation, Reading, Request Line, Sender or Specification Set — six of §8.3's settled canonical words — against the L200 claim that every term is defined. No precedence clause anywhere in the six parts rescues §5. One genuine mitigation, which the reviewer already noted: the database table name `txn_sample_test` matches the glossary's coinage, so no wrong schema would be built; the damage is confined to terminology and labels. That keeps this at the lower end of Major rather than a Blocker, but Major stands because the document elevates vocabulary consistency to a testable [MUST] and the glossary is the first place a reader looks. The fix direction ("Section 2 wins under WF-23") is right, but the fix as written is imprecise in three ways, corrected below.

====================================================================================================
FINDING 3/14  [Major] [contradiction]  (raised by consistency reviewer)
LOCATION: Part D M12 throughout — M12-03 (section_4_functional_support.md L192), M12.4 heading (L240), field "Batch internal code" (L244), M12.5 heading — against Part B §8.3 (L536)
EXACT TEXT IN THE DOCUMENT:
| M12-03 | [MUST] | For a batch-tracked item, **the batch (lot) is the unit of control, not the item**. Stock quantity, expiry, certificate and usage are all held at batch level. |
PROBLEM:
Section 2 §8.3 settles this concept as "**Lot**", table `mst_consumable_lot`, screen label "Lot", and lists "Batch" as acceptable in speech only — not in the system. Part D M12 uses "Batch" as the primary word in the requirement text, the field-table heading ("M12.4 Field table — Batch (Lot) Record"), the field name ("Batch internal code"), the state-table heading ("M12.5 Batch states") and the permission matrix rows ("Batch — receive", "Batch — accept / reject", "Batch — issue to test", "Expired batch — override"). It also invents a "Batch internal code" with no format where §10.1 already defines "Consumable lot identifier | `DVM/LOT/2026-27/<4>`". WF-23 [MUST] is violated in the module that owns the entity, so the database columns and the screens will both carry the wrong word.
REQUIRED FIX:
Rename to **"Consumable Lot"**, not bare "Lot". §8.4 prescribes "**Lot** (reagent) — always qualified" because bare "lot" collides with the customer's commercial lot and the IS 15090 grading lot; §10.1 and the M5/M6/M7 field tables already use the qualified form ("Consumable lot identifier", "| Consumable lot | Lookup |"). So the matrix rows should read "Consumable lot — receive / accept or reject / issue to test" and "Expired consumable lot — override", and M12.5 should be "Consumable lot states". Bare "Lot" is fine inside M12 prose once the module heading has established the context.

Apply in parts/section_4_functional_support.md across the whole M12 block (L163-300), plus these spots the reviewer omitted:
- L172 M12.1 screen list: "Batch (Lot) Register" → "Consumable Lot Register"; "Batch Impact Analysis" → "Lot Impact Analysis"; Item Master purpose "whether batch-tracked" → "whether lot-tracked".
- L191 M12-02 and L230 M12.3: "**Batch tracked (Yes/No)**" → "**Lot tracked (Yes/No)**".
- L192 M12-03, L246, L290: "batch-tracked item" → "lot-tracked item".
- L244 M12.4 first row: "Batch internal code" → "Lot identifier", Notes: "From the `DVM/LOT/<FY>/<4>` series per §10.1. Distinct from the supplier's number in the next row." Keep L246 "Supplier's / manufacturer's batch or lot number" exactly as-is.
- M12-12 heading "**BATCH IMPACT ANALYSIS.**" → "**LOT IMPACT ANALYSIS.**".
- M12-31 "print batch labels carrying the batch code as a barcode" → "print lot labels carrying the lot identifier as a barcode".
- M12.6 rule 2: the sub-batch suffix example `B-2026-014-A`, `-B` invents a third code format and must become a suffix on the §10.1 series, e.g. `DVM/LOT/2026-27/0156-A`, `-B`.

Do NOT run the replace outside M12/M21.3: "batch" is used in a legitimately different sense elsewhere (§8.3 "Batch run sheet | **Worksheet**", NFR-09 "Batch registration", AC-02 "one batch action", front_matter "batch entry", "eight certificates in one batch"). Also worth a separate look, out of scope here: the appendix `mst_consumable_lot` schema (SPEC.md L5168) carries only `lot_no` with unique key `consumable_id, lot_no` and has no column for the internal `DVM/LOT/...` identifier that §10.1 mandates — so once M12.4 names the field "Lot identifier", the schema needs a matching column.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
Independently confirmed on every point.

1. QUOTE IS REAL. `D:\Prashant_WorkSpace\TTL Software\parts\section_4_functional_support.md` L192 (SPEC.md L3117) contains M12-03 verbatim as quoted.

2. THE CANONICAL TERM IS "LOT", AND §8.3 IS NORMATIVE. section_2_workflow.md L536 (SPEC.md L978): `| Batch of a reagent | **Lot** | mst_consumable_lot | Lot | Batch (acceptable in speech), Stock, Item |` — the last column is headed "Do **not** call it". WF-23 [MUST] (section_2_workflow.md L560) requires the §8.3 words be used "identically in the user interface, in the database table and column names, in printed documents and in all project correspondence", with acceptance check "Open any five screens and any five tables; the words match the table exactly."

3. M12 IS THE SOLE OUTLIER IN THE WHOLE DOCUMENT. I counted 79 occurrences of "batch" vs 4 of "lot" in the M12 region (L163-300), and inspected every one: all refer to the reagent-lot entity except "Supplier's / manufacturer's batch or lot number" (L246), which is legitimately the supplier's own label. Everywhere else in the spec uses "lot":
   - parts/appendix.md: ZERO occurrences of "Batch". Data model (SPEC.md L5168) is `mst_consumable_lot`, described as "Lot or batch — **the unit of control, not the item**" — the same sentence as M12-03 but with "Lot" leading.
   - §10.1 (SPEC.md L1190): "**Consumable lot identifier** | `DVM/LOT/2026-27/<4>`".
   - section_2_workflow.md: §8.2 entity list "Consumable Lot" (L415), WF-14 (L264) "consumable or reference-material lot", WF-68 (L677) "consumable lot".
   - section_3_functional_core.md: M5-05 (L713) "lot"; M5 field table (L813) "| Consumable lot | Lookup |"; M6 field table (L961) "| Consumable / reference material lot | Lookup |"; M6-15 (L883) "lot identifier is mandatory"; M7-12(f) and M7 field table (L1115) "Consumable lot"; M1-75/M1-77 "expired consumable lot" / "consumable lot later found unsuitable".
   - section_5_nfr_plan.md: DB-20 `consumable_lot_id`, DB-31, AC-19, NFR-106 — all "lot". Its only "batch" uses (NFR-09, AC-02) are the unrelated bulk-action sense.
   So M5/M6/M7 field tables already label the picker "Consumable lot" while M12.4 labels the same entity's own record "Batch" — the two screens the tester and store keeper use side by side would carry different words. This is a live WF-23 violation, not a stylistic quibble.

4. NOT HANDLED ELSEWHERE. M12 never cites §8.3, §10.1 or WF-23, and contains no carve-out. The parentheticals in "M12.1 Batch (Lot) Register" and "M12.4 Field table — Batch (Lot) Record" show the author knew they were the same entity but kept "Batch" as the primary word — the opposite of what §8.3 settles. §8.4 independently flags this exact hazard ("'Lot' used loosely ... the customer's commercial lot AND the grading unit under IS 15090 AND a reagent batch"), so the collision was foreseen and resolved against M12's usage.

5. THE "BATCH INTERNAL CODE" GAP IS ALSO REAL and is the substantive (non-lexical) part. M12.4 L244: `| Batch internal code | System-generated | Yes | Unique in the laboratory |` — no format, no cross-reference — while §10.1 already defines `DVM/LOT/2026-27/<4>` for exactly this identifier and explains why it exists separately from the supplier's number. A builder reading M12 alone would mint a second, undefined code series.

6. M21.3 MATRIX CONFIRMED. SPEC.md L4332-4335 are "Batch — receive / accept / reject / issue to test" and "Expired batch — override", sitting directly under "Consumable item master" and alongside canonical "Equipment ..." rows.

The reviewer's fix is directionally right, and the "keep batch only for the supplier's own label" carve-out is correct and matches §10.1. Severity Major stands: WF-23 is [MUST], the breach is in the module that owns the entity, and it drags in an undefined second identifier series. I am refining the fix because a literal "Batch"→"Lot" replace lands on the bare word that §8.4 forbids, and because the reviewer missed several spots.

====================================================================================================
FINDING 4/14  [Major] [contradiction]  (raised by consistency reviewer)
LOCATION: Part D OPEN-Q22 (section_4_functional_support.md L945) and M17.5 against Part B WF-5 (section_2_workflow.md L255) and Part B OPEN-Q1 (L274)
EXACT TEXT IN THE DOCUMENT:
— *Recommended default:* set *Required before report release* for all commercial categories, *Not required* for internal research, other CSB units and state government departments, and make it configurable per category.
PROBLEM:
The payment-hold switch is keyed to two different dimensions. Section 2 WF-5 [MUST] says "A configuration switch **`payment_before_report_release`** exists per unit and per **order type**", and Part B OPEN-Q1's default keys it to order types ("the switch on for Commercial orders and off for Internal R&D, Inter-unit and Statutory orders"), where the four order types are fixed by WF-19. Part D OPEN-Q22 keys it to **customer category** and introduces a value not in WF-19's list ("state government departments"). Customer category and order type are different masters in M1, so the developer must choose which one the switch hangs off, and the two produce different behaviour for a state government department placing a commercial order.
REQUIRED FIX:
Fix the requirements, not the open question. Make one rule with an explicit evaluation order, and merge the duplicate open questions.

1. Amend WF-5 (section_2_workflow.md L255) to own the whole rule and name both dimensions:
"[MUST] A configuration rule **`payment_release_rule`** exists per unit, taking one of three values — *Not required*; *Required before testing begins*; *Required before report release*. The rule is set per **order type** (WF-19), with an optional override per **customer category** (M1-11). Evaluation order: the order-type value is read first; where it is *Not required* the order is never held; otherwise the customer-category override for that order's customer applies, falling back to the order-type value. *Required before report release* blocks the move to *Issued*; *Required before testing begins* raises the Hold-for-Payment state of M3-26. Either may be overridden by a named officer with a reason (WF-41, M17-43)."

2. Amend M17-42 (section_4_functional_support.md L892) to defer instead of redefining: "[MUST] The system shall implement the `payment_release_rule` of WF-5, exposing the per-order-type values and the per-customer-category overrides on one configuration screen, with the three settings Not required / Required before testing begins / Required before report release. Default: *Required before report release* for Commercial, *Not required* for Internal R&D (advisory, zero charge), Inter-unit referral and Statutory / scheme." Do not restate the keying independently.

3. Update the config register (section_4 L1602) to `payment_release_rule` per unit, per order type, with per-customer-category override | Commercial = Required before report release; others Not required | WF-5, M17-42.

4. Replace OPEN-Q22 (section_4 L944) with a pointer, not a second default: "**OPEN-Q22:** See Part B OPEN-Q1 — the same question. Answer it once." Extend OPEN-Q1's recommended default (section_2 L274) to answer the customer-class half it currently drops: "…with the order-type value on for Commercial and off for Internal R&D, Inter-unit referral and Statutory / scheme, and per-customer-category overrides seeded to *Not required* for Government Department, CSB Internal Unit and CSB Internal R&D so that a government department placing a Commercial order is not held."

5. Scope the per-customer layer explicitly so it does not become a fourth rule: add a note to the M1 field table (section_3 L216) that `Advance required` / `credit_terms` set the **per-order Payment terms default** consumed by M3-26, and are subordinate to `payment_release_rule`; state which wins when they disagree.

Separately (out of scope for this finding, worth its own): the customer-category enumeration differs three ways — M1-11, M17-03 and `mst_customer_category` (section_5 L430) list different values.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
CONFIRMED, but the reviewer anchored it on the wrong line and two supporting claims are wrong.

Quotes verified verbatim. `D:\Prashant_WorkSpace\TTL Software\parts\section_2_workflow.md` L255 (WF-5), L269 (WF-19), L274 (OPEN-Q1); `D:\Prashant_WorkSpace\TTL Software\parts\section_4_functional_support.md` L944 (OPEN-Q22).

The real collision is [MUST] vs [MUST], not a recommended default. Two requirements independently define the report-release payment gate on different dimensions with different value sets:
- WF-5 [MUST]: switch `payment_before_report_release` "per unit and per order type" — binary on/off.
- M17-42 [MUST] (section_4 L892): rule "release the report only after payment" with "three settings per customer category: Not required; Required before testing begins; Required before report release."

I grepped every reference to WF-5 and to M17-42 across all authored parts: neither cites the other, and there is no precedence rule in front_matter.md or appendix.md. The configuration register (section_4 L1602) commits to only one of them — `payment_release_rule` per customer category | Required before report release | M17-42 — so a developer reading the register implements the customer-category form and silently violates WF-5. The reviewer's failure case is genuine: a Government Department customer placing a Commercial order gets a hold under WF-5/OPEN-Q1 and no hold under M17-42/OPEN-Q22. WF-5's binary switch also cannot express M17-42's third setting ("Required before testing begins"), which is exactly what OPEN-Q1's own question asks about ("before testing, before report release, or neither").

Two reviewer claims are wrong and should not be repeated:
1. "Customer category and order type are different masters in M1" — false. Order type is not in M1 and has no master anywhere; `grep -i "order.type"` across section_3 and section_4 returns zero hits. It exists only as WF-19's prose enumeration plus a bare `txn_order.order_type VARCHAR(20)` column (section_5_nfr_plan.md L477). Customer category is a real master (M1-11, `mst_customer_category` L430).
2. OPEN-Q22 does not "introduce a value not in WF-19's list" in any invented sense — "state government department", "another CSB unit" and "internal research (zero charge)" are the exact values of M17-03's own customer-category enumeration (section_4 L838). OPEN-Q22 is internally consistent with its module; only the dimension choice conflicts.

Also, OPEN-Q1 and OPEN-Q22 are the same open question asked twice with two different defaults — OPEN-Q1 even asks the customer-class half ("advance from traders, none from government departments") and then answers only the order-type half.

The proposed fix is wrong. It edits only OPEN-Q22 and asserts "there is no separate per-customer-category switch", which leaves M17-42 [MUST] contradicting the new OPEN-Q22 text four lines below it, contradicts the config register at L1602, and deletes the "Required before testing begins" setting. It also collides with a third keying the reviewer never found: M1-10 plus the M1 field table (section_3 L215-216) put `credit_terms` / `Advance required` on the customer with the note "Drives the payment hold in M3", M3-26 [MUST] triggers Hold-for-Payment from "the customer has an advance-payment requirement, or where credit terms demand it", the M3 order field table carries per-order "Payment terms" defaulted from the customer, and `mst_customer.advance_required BOOLEAN` exists in the schema (section_5 L429). Finally, the fix invents an unsupported mapping — WF-19 defines Statutory / scheme with no reference to fee exemption, and nothing in the document says government work is Commercial when it is not fee-exempt.

Severity Major is right: two [MUST]s gating report issue, with the config register committing to one of them.

====================================================================================================
FINDING 5/14  [Major] [buildability]  (raised by technical-half reviewer)
LOCATION: section_2_workflow.md WF-74 (line 683), §9.2 state table (lines 599-612), §9.1 Testing Complete (line 585), WF-42 (line 655)
EXACT TEXT IN THE DOCUMENT:
the system discards every other allocation on that sample, creates repeat allocations for all of them including conditioned size, and prevents the certificate being issued from the discarded run
PROBLEM:
Two defects. First, WF-74 must move sibling allocations to **Discarded** from whatever state they are in — Pending, Allocated, Awaiting Conditioning, In Test, Result Entered — but the §9.2 transition table lists Discarded as an allowed next state only from **Verified**. Since DB-15 executes transitions from `sys_state_transition_rule` and refuses moves with no rule row, WF-74's [MUST] behaviour is illegal against the declared state machine. Second, Discarded is absent from the terminal set in the Sample states table ("Every allocation has reached Verified, Cancelled or Aborted") and from WF-42 ("Reported, Cancelled or Aborted"), so a sample with one discarded allocation can never reach Testing Complete or Reported and is deadlocked forever — and Reported is the state that sets the retention date and triggers the invoice. Third, the Discarded row promises to record "the discard reason and the triggering allocation" and `txn_sample_test` has no `discard_reason_id` and no `discarded_by_sample_test_id`.
REQUIRED FIX:
Three edits, two of them broader than proposed. (a) §9.2: add "Discarded" to the Allowed-next-states column of Pending, Allocated, Awaiting Conditioning, In Test, Result Entered **and Under Verification** (the reviewer omitted the last), with mover "System (WF-74) or Unit Incharge", and add the matching sys_state_transition_rule seed rows. Also fix the Discarded row's own "Who can move it out" column, which says "Unit Incharge" while the next-state cell says terminal. (b) Rather than the clumsy "Discarded-with-a-completed-repeat" wording, define the derivation over current allocations and cover Invalidated too: §9.1 Testing Complete — "Every **current** allocation has reached Verified, Cancelled or Aborted. An allocation in Discarded or Invalidated is not current once its linked repeat allocation exists (`superseded_by_id` set); the repeat is then the current allocation for that test."; WF-42 — "**Reported** is entered automatically when every **current** allocation on the sample is Reported, Cancelled or Aborted, where Discarded and Invalidated allocations whose repeat exists are excluded. It cannot be set by hand." Add an acceptance check: a sample whose four allocations were discarded under WF-74 and whose four repeats are all Reported reaches Reported, sets the retention date and raises the invoice. (c) §25.2 `txn_sample_test`: add `discarded_due_to_sample_test_id BIGINT NULL` (self-FK) — this is the only genuinely missing column. `discard_reason_id` is optional: the reason is already captured in `sys_state_transition.reason_id`; add it only for symmetry with the existing denormalised `abort_reason_id`, and if added, document `validity VARCHAR(15)` at the same time since it is currently listed with no defined value set.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
Independently confirmed on the primary two counts. (1) The quote is verbatim at section_2_workflow.md:683. (2) In §9.2 (lines 599-612) "Discarded" appears in the Allowed-next-states column of one row only — Verified (line 605); Pending, Allocated, Awaiting Conditioning, In Test, Result Entered AND Under Verification all omit it. DB-15 (section_5_nfr_plan.md:557) makes the table binding ("Allowed moves live in sys_state_transition_rule as data … checks the rule exists"), so WF-74's [MUST] and its acceptance check ("All sibling allocations move to Discarded") are unexecutable. The reviewer's list of rows to patch is itself incomplete — it omits Under Verification. (3) The deadlock is real and wider than claimed: "Testing Complete" is defined exactly once (line 585, "Verified, Cancelled or Aborted") and WF-42 exactly once (line 655, "Reported, Cancelled or Aborted"); Discarded is terminal and in neither set, and per §9.1 Reported is what sets the retention date and triggers the invoice. Invalidated (line 608) has the identical hole, so a Discarded-only fix leaves the same bug for the post-issue invalidation path. I searched for the capability under other names before accepting the omission: superseded_by_id, validity VARCHAR(15) (section_5_nfr_plan.md:482, matching research_silk_domain.md:430's recommended valid/discarded/repeat status), "supersed*", "open allocations", "every allocation" — nothing anywhere states that discarded or superseded allocations are excluded from the derived sample state. The report side IS handled (section_3_functional_core.md:1290 "Only Verified allocations selectable"), so WF-74's certificate clause is satisfiable; only the sample-state derivation deadlocks. Where the finding overreaches: the missing discard_reason_id is largely a non-issue — sys_state_transition carries reason_id and remarks (line 526) and DB-15 mandates a row per transition with a reason where required, so the reason is already captured; only the triggering-allocation link is genuinely unrecordable (retest_of_id and superseded_by_id both point at the repeat, not the trigger). Severity downgraded from Blocker to Major: the intended behaviour is unambiguous elsewhere (§9.3 line 629 "certificate issued from the repeat run only"; section_3_functional_core.md:990 principle 4), so this is a mechanical repair to one table column and two derivation lists, not a requirement that cannot be decided or built.

====================================================================================================
FINDING 6/14  [Major] [missing-capability]  (raised by technical-half reviewer)
LOCATION: section_5_nfr_plan.md txn_payment / txn_payment_reference (line 493), AC-32 (line 881), §10.1 Money Receipt (section_2_workflow.md line 745)
EXACT TEXT IN THE DOCUMENT:
`unit_id`, `receipt_no`, `received_on DATE`, `customer_id`, `amount DECIMAL(14,2)`, `mode VARCHAR(20)`, `status VARCHAR(20)`, `credited_on DATE`, `bank_statement_line_id`
PROBLEM:
Nothing makes payment capture idempotent. A re-imported bank statement, a duplicated portal callback, or a clerk clicking Save twice creates a second `txn_payment` row, and because the Money Receipt series is gap-free and statutory each duplicate burns a receipt number that then has to be voided, and `txn_payment_allocation` will over-settle the invoice until DB-37's trigger happens to catch it. `txn_payment_reference.ref_value` is indexed but not unique, `bank_statement_line_id` has no unique constraint, and there is no idempotency key — even though `sys_notification` has one, so the pattern was clearly known to the author of §25.4 and not applied here. AC-32 tests the happy path of matching and never tests re-importing the same statement.
REQUIRED FIX:
Put the dedup where the duplicate actually originates — the import — and leave the payment-to-line relationship many-to-one.

1. Add a table to section 25.2, immediately before `txn_payment`, because `bank_statement_line_id` currently references nothing that exists:
`txn_bank_statement_line` | A line of an imported bank statement or payment-portal report, and the import that produced it. | `unit_id`, `import_batch_id`, `bank_account_id`, `value_date DATE`, `credited_on DATE`, `amount DECIMAL(14,2)`, `bank_ref VARCHAR(60)`, `narration TEXT`, `payer_remark TEXT`, `line_no INT`, `match_state VARCHAR(20)`, `source VARCHAR(20)` | `id` | as named | `unit_id, bank_account_id, value_date, bank_ref, amount, line_no` | `match_state`; `payer_remark`
plus `txn_import_batch` | One import of a statement or portal report. | `unit_id`, `source VARCHAR(20)`, `file_attachment_id`, `file_sha256 CHAR(64)`, `imported_by_user_id`, `imported_at`, `lines_read INT`, `lines_new INT`, `lines_already_present INT` | `id` | as named | `unit_id, file_sha256` | `imported_at`

2. Add to `txn_payment`: `idempotency_key VARCHAR(120) NULL`, `UNIQUE (unit_id, idempotency_key)`. Do NOT make `bank_statement_line_id` unique — M17-24 and M17-25 both contemplate several instruments deposited on one slip arriving as a single bank credit line, so several payments may legitimately share one line. Instead add a trigger check that the payments referencing one statement line sum to no more than that line's amount, which is the constraint that actually matters.

3. Add to `txn_payment_reference` a partial unique index on `(unit_id, ref_type, ref_value)` restricted to the machine-generated reference types (`UTR_NO`, `BHARATKOSH_TXN_REF`, `PG_TXN_ID`, `UPI_RRN`, `SB_COLLECT_REF`, `INB_REF_NO`). These are genuinely one-per-transaction; leave `DD_NO`, `CHEQUE_NO` and the rest non-unique, since those repeat across banks and accounts. `captured_from` already distinguishes MANUAL from PORTAL_CALLBACK and BANK_MIS_IMPORT, so this needs no new column.

4. Add to section 25.6 (numbered DB-43, not DB-46, and update the traceability line at section_5_nfr_plan.md:643 from "DB-01 to DB-42" to "DB-01 to DB-43"):
| DB-43 | A payment captured from an external source — a bank statement line, a portal callback or a gateway response — carries an idempotency key formed from the source system, that system's own transaction identifier and the amount. Re-presenting the same key returns the existing payment and its receipt number, if any, and consumes no new number. Re-importing a statement recognises lines already held and creates no second statement line. Machine-generated payment references are unique per unit and reference type. | `UNIQUE (unit_id, idempotency_key)` on `txn_payment`; the natural unique key on `txn_bank_statement_line`; a partial unique index on the machine-generated `ref_type` values; and a service-layer upsert that returns the existing row rather than raising. | [MUST] |

5. Add an acceptance criterion in section 27.6 (AC-49, not AC-50, since the criteria end at AC-48):
| **AC-49** | A bank statement already imported once, with one of its credits already matched to an order | The same file is imported again, and a portal callback for one of those payments is delivered twice | No second statement line and no second payment are created; no money receipt number is consumed; the invoice's paid amount and the customer ledger balance are unchanged; the import batch summary shows the count of lines already present alongside the count of new lines, and the repeated callback is recorded in the integration log of M22-11 with the reason "already processed" | DB-43, M17-19, M22-06 |

6. Amend M22-06 so that inbound is covered symmetrically with M22-02: add "An inbound message shall be recognised by its source identifier and applied at most once; a repeat delivery shall be logged and discarded, not re-applied."

Note for the author: the counter double-save case is separate from the import case and is not fixed by an import-side key. If it is to be closed, it belongs as a short-window duplicate warning on the payment form comparing customer, amount, mode and date — the same shape as the existing duplicate-sample warning in M3 rule 5 — rather than as a hard constraint, because M17.7 rule 2 deliberately permits a customer to pay twice.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
Verified. The quoted field list exists verbatim at parts/section_5_nfr_plan.md:493, and its unique key is only `unit_id, receipt_no`; `ref_value` and `bank_statement_line_id` carry no uniqueness. I searched for the capability under idempot*, duplicate, de-duplicat*, re-import/reimport/already processed/already imported, replay, exactly once, optimistic/row version/resubmit, and import batch/staging/import log — nothing covers duplicate payment capture. The pattern is demonstrably known to the author and applied elsewhere: sys_notification.idempotency_key with a UNIQUE constraint (line 532), M22-02 (scoped explicitly to *outbound* messages), M3 rule 5's duplicate-sample warning, NFR-34's "all 10 arrive once". M22-06, the inbound counterpart, has no dedup rule at all, and M22.1 explicitly routes money inbound ("ERP -> LIMS for money received through bank or portal channels ... On recording, and on statement import"). AC-32 is happy-path only. So the omission is real and it is a real gap in a document this thorough.

Two parts of the reviewer's harm chain are wrong, though. (a) DB-37 is `CHECK (paid_amount <= total_amount)` plus a summing trigger — it blocks over-settlement rather than "happening to catch it". (b) The Money Receipt burn does not apply to the import or callback path: M17-16 scopes the receipt to "money tendered at the laboratory" and the M17.6 field table marks "Money receipt number ... Conditional | Where the laboratory issued its own receipt", so a re-imported statement line consumes no number. Only the counter double-save case burns one.

The residual harm is still Major: M17-26 forbids deleting a payment, so a duplicate can only be reversed and stands forever; meanwhile it sits as an unallocated advance inflating the M17-21 running account, which can wrongly clear the M17-42 payment hold, and it appears as a phantom receipt on the statutory M17-24 Daily Collection Register and the M17-25 collection-versus-remittance reconciliation.

The proposed fix needs correction on three counts. First, `UNIQUE(bank_statement_line_id)` contradicts the document: demand drafts are deposited in batches (M17-24 "reconciliation of collection against deposit", M17-25 ageing of "undeposited drafts", and the research brief's DD field set "date deposited, GAR 1 / bank deposit slip reference, date credited"), so two separately-tendered drafts on one deposit slip yield one bank credit line and two payment rows — the constraint would block a legitimate case. Second, no `txn_bank_statement_line` table exists anywhere in section 25, so `bank_statement_line_id` is a dangling reference and import-level dedup has nowhere to live; AC-50's "visible on the import log" also cites an artefact the specification never defines (no match for "import log", "import batch", or "staging table" anywhere). Third, the identifiers are wrong: section 25.6 ends at DB-42 and the traceability line 643 states "DB-01 to DB-42", and the acceptance criteria end at AC-48, so the new items are DB-43 and AC-49 and line 643 must be updated.

====================================================================================================
FINDING 7/14  [Major] [contradiction]  (raised by technical-half reviewer)
LOCATION: section_2_workflow.md §8.3 (lines 519, 533), §8.1 (line 327), §10.1 (lines 732-745), WF-4 (line 254), WF-14 (line 264) vs WF-70 (line 679); section_5_nfr_plan.md mst_method (line 438), txn_payment (line 493), NFR-103 (line 241), NFR-109 (line 254)
EXACT TEXT IN THE DOCUMENT:
| Documented procedure and its revision | **Method Version** | `mst_test_method` | Method / Version |
PROBLEM:
WF-23 makes the §8.3 vocabulary table a [MUST] for database table names, and §25 breaks it twice: §8.3 mandates `mst_test_method` and §25.1 defines `mst_method`; §8.3 mandates `txn_receipt` for money received and §25.2 defines `txn_payment` / `txn_payment_reference`. §8.1 also calls the entity "Order Line" where §8.3 mandates "Request Line". The number examples are systematically inconsistent with the format that defines them: §8.1 shows the sample as `DVM/2627/RS/01188`, the allocation as `DVM/2627/RS/01188-T01`, the report as `DVM/TR/2627/00380` and the money receipt as `DVM/MR/2627/00902`, while §10.1 defines those four series with the full financial year (`DVM/2026-27/<TYPE>/<5>`, `DVM/<TYPE>/2026-27/<5>`, `DVM/MR/2026-27/<5>`) and reserves the compressed `2627` form for 16-character GST documents only. And three cross-references point at the wrong requirement: WF-4 cites "WF-58", which does not exist; NFR-103 says "This is test AC-30", which is the invoice-cancellation test, not the direct-database-change test (AC-42); NFR-109 says "This is test AC-32", which is the payment-reconciliation test, not the add-a-new-test test (AC-38). Finally WF-14 and WF-70 are two [MUST] rules for the same guard with different content — WF-14 requires unexpired consumable lots and tests "the observation date", WF-70 omits lots and tests "the test date".
REQUIRED FIX:
Apply only these edits.

1. Method table name — pick ONE direction. Simplest: change §8.3 (section_2_workflow.md:519) "Database table" cell from `mst_test_method` to `mst_method`, since §25 is internally consistent across four table names. If instead §25 is renamed (which better matches the research pack and WF-23), rename `mst_method` -> `mst_test_method` and `mst_method_equipment`/`_consumable`/`_parameter` -> `mst_test_method_equipment`/`_consumable`/`_parameter` at section_5_nfr_plan.md:438, 439, 440, 442. Do not do both.

2. Receipt table — rename in §25 to satisfy WF-23 (the settled word is "Receipt"): `txn_payment` -> `txn_receipt`, `txn_payment_reference` -> `txn_receipt_reference` (line 493), AND `txn_payment_allocation` -> `txn_receipt_allocation` with its `payment_id` column -> `receipt_id` (line 494), AND the same rename inside DB-37 (line 586, "a trigger summing `txn_payment_allocation`"). The reviewer's fix misses lines 494 and 586.

3. §8.1 "Order Line" — change the heading at section_2_workflow.md:320 to "**Request Line**" AND the prose at line 322 ("The order line is what was agreed and priced" -> "The request line is what was agreed and priced").

4. Financial-year examples — expand `2627` to `2026-27` at section_2_workflow.md:327 (`DVM/2026-27/RS/01188`), :357 (`DVM/2026-27/RS/01188-T01`), :382 (`DVM/TR/2026-27/00380`), :387 (both occurrences: `DVM/TR/2026-27/00380/R1` superseding `DVM/TR/2026-27/00380`), :402 (`DVM/MR/2026-27/00902`), :427 (`DVM/TR/2026-27/00380`), and section_3_functional_core.md:514 (`DVM/2026-27/RS/00411` … `DVM/2026-27/RS/00415`). Leave section_2_workflow.md:397 (`DVM/I/2627/00417`) and section_4_functional_support.md:873 unchanged — those are the 16-character GST documents where the compressed form is correct.

5. Drop the WF-4 edit entirely. WF-4 already reads "(see WF-37, WF-84)"; "WF-58" does not appear anywhere in the specification.

6. Cross-references: change NFR-103's "This is test AC-30" to "This is test AC-42" (section_5_nfr_plan.md:241) and NFR-109's "This is test AC-32" to "This is test AC-38" (line 254). Both are confirmed by the reverse traceability column on AC-42 and AC-38.

7. Do NOT delete WF-70. §9.4 is the normative allocation-transition table and §7.6 is an intentional overview layer (WF-10/WF-71 and WF-12/WF-76 overlap the same way). Instead align WF-70 (section_2_workflow.md:679) to the stronger wording: "A Test Allocation cannot enter Result Entered unless every mandatory parameter has at least the required number of readings for that method version, every instrument recorded against those readings was within calibration validity on the observation date of each reading, and every consumable or reference-material lot recorded was unexpired on that observation date." Then leave WF-14 as the overview restatement and append "(detailed in WF-70)" to it so the two can no longer drift.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
Five of the eight sub-claims are independently confirmed and are genuine internal contradictions, not misreadings.

CONFIRMED: (a) The quote exists verbatim at parts/section_2_workflow.md:519. §8.3's "Database table" column mandates `mst_test_method`, while §25.1 (section_5_nfr_plan.md:438-442) defines `mst_method`, `mst_method_equipment`, `mst_method_consumable`, `mst_method_parameter`. WF-23 (line 552) is a [MUST] requiring the §8.3 words to be used "in the database table and column names", with an acceptance check ("open any five tables; the words match the table exactly") that this schema would fail. Aggravating: §25's preamble says "Naming follows the research pack", and parts/research_lims_reference.md:225 uses `mst_test_method`. (b) §8.3:533 mandates `txn_receipt` for "Money received"; §25.2:493 defines `txn_payment`/`txn_payment_reference` for the same entity (identical columns `receipt_no`, `received_on`, `mode`, `head_of_account`); research brief T19 also uses `txn_receipt`. (c) The `2627` examples do contradict §10.1: line 721 confines the compressed FY to "where character length is constrained", and §10.1 defines the sample, allocation, report and money-receipt series with the full `2026-27`. The problem is BROADER than the reviewer stated — also section_2_workflow.md:387 (twice), :427, and section_3_functional_core.md:514 (`DVM/2627/RS/00411 … 00415`). Line 397 (`DVM/I/2627/00417`) is correct per the 16-character invoice rule and must be left alone. (d) NFR-103 cites AC-30 (invoice cancellation) when AC-42 (line 896) is the direct-database-change test and explicitly back-references "DB-04, NFR-103". (e) NFR-109 cites AC-32 (payment reconciliation) when AC-38 (line 892) is the add-a-new-test test and explicitly back-references "NFR-109".

REFUTED: The claim that WF-4 cites a non-existent "WF-58" is fabricated. WF-4 reads "(see WF-37, WF-84)" — verified at section_2_workflow.md:254 and SPEC.md:696. The string "WF-58" appears nowhere in the specification or the parts, only in review_findings.txt. WF-37 exists (Received -> Rejected) and is a defensible reference to a terminal state.

OVERSTATED: "Order Line" is a single glossary heading in §8.1, a section that deliberately supplies slash-aliases ("Sender / Agent", "Sub-sample / Specimen", "Test Request (TRF) / Order"); minor, and the fix misses the prose at line 322. WF-14 vs WF-70 is not a contradiction — WF-14 is strictly stronger than WF-70, and §7.6 is an explicit overview layer that restates §9.4 detail rules elsewhere too (WF-10/WF-71, WF-12/WF-76 are the same pattern); WF-68 separately mandates the consumable lot. The real defect there is only that WF-70's wording is weaker ("test date" vs "observation date", lots omitted).

The fix is also imprecise: the `txn_payment` rename must carry `txn_payment_allocation` (line 494), its `payment_id` column and DB-37 (line 586), which the reviewer omits; and deleting WF-70 would strip the guard out of §9.4's normative allocation-transition table, which is the wrong direction.

Severity: Major is defensible but borderline — no requirement's intended behaviour is ambiguous, every conflict is resolvable by identifying the normative section, and the breadth (8+ instances across three sections, two inside [MUST] rows, two breaking test traceability) is what carries it. Leaving it at Major.

====================================================================================================
FINDING 8/14  [Major] [contradiction]  (raised by technical-half reviewer)
LOCATION: section_5_nfr_plan.md AC-25 (line 869), AC-01 (line 825), AC-02 (line 826) vs section_2_workflow.md WF-32 (line 645), §9.1 Received (line 578), WF-38 (line 651), WF-99 (line 773)
EXACT TEXT IN THE DOCUMENT:
The signature verifies but the page reports **"the document you uploaded does not match this QR code"**, naming the expected fingerprint
PROBLEM:
Three acceptance criteria are internally wrong and will be signed off as passing behaviour that is either impossible or forbidden. (1) AC-25 postulates a doctored PDF carrying a genuine QR image and then asserts "The signature verifies" — if the PDF content was altered, a PAdES signature over that content must fail; the criterion as written trains the lab to accept a broken signature check as normal. (2) AC-01 requires the printed label to carry "due date, test codes", but WF-32 and the Sample state table print the label on entry to **Received**, while allocations, their due dates and therefore the test codes are only created on entry to Accepted (WF-38) — at label-print time those fields do not exist. (3) AC-02 requires "30 sample numbers ... allotted with no gaps", but WF-99 explicitly permits gaps in the sample series and only invoices, reports, receipts, complaints and nonconformities are gap-free.
REQUIRED FIX:
Apply ONLY the label-timing edit. Leave AC-25 and AC-02 exactly as written.

1. parts/section_2_workflow.md WF-32 (line 645) — replace "The label is printed only on entry to Received." with: "No label is printed on entry to Received; the label is printed on entry to Accepted or Accepted with Reservation, once allocations and their due dates exist (M3-20, M3-24)." Update the acceptance check to: "Attempt receipt with the quantity blank. Refused. Confirm no label prints for a sample in Expected or Received, and that the label prints on acceptance carrying the due date and test short codes."

2. parts/section_2_workflow.md §9.1 (line 578), Received row — delete "Prints the label." from the automatic actions.

3. parts/section_2_workflow.md §9.1 (lines 580-581) — add "Prints the sample label, which is now able to carry the due date and test short codes." to the automatic actions of both Accepted and Accepted with Reservation.

4. Collateral edit the reviewer missed: parts/section_3_functional_core.md line 638 (M4 field table, Receipt Condition and Acceptance screen) currently reads "Sample number | Lookup / scan | Yes | Must exist and be in state Received | Populated by scanning the label" — with no label yet at Received, change the note to "Populated by lookup, or by scanning the acknowledgement slip; the sample label does not exist until acceptance." (M4-02 already anticipates this by allowing "a written slip bearing the sample number" in the receipt photograph, so no other change is needed.)

5. Do NOT silently change the turnaround-clock wording while editing this row. There is an adjacent, separate contradiction that needs a laboratory decision rather than a drafting fix: §9.1's Received row says "Starts the turnaround clock, subject to the daily cut-off time" and its Doubt Raised row says "Turnaround clock pauses" (both presuming the clock runs from Received), whereas M3-23 [MUST] says the clock starts at acceptance "not receipt". Raise that as its own finding.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
PARTIALLY STANDS — 1 of 3 sub-claims survives; the headline claim is flatly wrong and its proposed fix would damage the spec.

(1) AC-25 — DOES NOT STAND. The quote exists verbatim (parts/section_5_nfr_plan.md:869), but the reviewer misread which signature is being verified. parts/section_3_functional_core.md:1413 M9-18 [MUST] already mandates exactly this behaviour: "A fifth, distinct state covers the report-substitution attack: 'Signature verified, but the file you uploaded does not match this code.' This is the state that catches a genuine code photographed and pasted onto a forged document." The signature in question is the QR payload's ECDSA signature (M9-01 to M9-03: header/payload/signature, key identifier, EC algorithm), which of course verifies because the scenario stipulates a genuine photographed QR. The uploaded PDF is never signature-checked at all: ARC-21 (section_5_nfr_plan.md:363) says the only interactive control "hashes the visitor's own copy of the PDF in their browser and compares fingerprints", and the M9 field table (section_3:1480) says "The file is never uploaded to the server". Section_3:1493 states the design intent outright — "the file fingerprint check that catches a genuine code pasted onto a doctored document." There is no PAdES verification of the recipient's file anywhere, so "a PAdES signature over that content must fail" describes a check the system does not perform. AC-25 is a correct, precise instance of M9-18.

(2) AC-01 label content vs label timing — STANDS, and is real, but the reviewer misassigns the error. AC-01 is NOT the wrong party: its own When column has the clerk "accepts the sample after the condition checklist, and prints the label", which matches section_3:400 M3-20 [MUST] — "A barcode label is printed for each physical item at the moment of acceptance, not at registration. The label carries ... received date, due date, test short codes ...". M3-23 [MUST] (section_3:408) reinforces this: "The turnaround clock starts at the moment of sample acceptance (not request date, not receipt)", and M3-24 computes the due date from that acceptance clock start. Allocations and due dates exist only from Accepted (WF-38, §9.1 Accepted row "Computes each allocation's due date", §9.2 Pending row). The outliers are WF-32 ("The label is printed only on entry to Received") and the §9.1 Received row ("Prints the label"), which are contradicted by four separate MUST requirements. So a genuine MUST-vs-MUST contradiction exists at the location the reviewer's fix targets, and the fix direction (amend WF-32 and the §9.1 Received row) is right — the reviewer just did not notice that M3-20 already settles the question in favour of the fix.

(3) AC-02 "no gaps" vs WF-99 — DOES NOT STAND. WF-99 says "Gaps are permitted in enquiry, quotation, worksheet, sample, sub-sample and allocation series." A permission is not a mandate; nothing forbids a particular batch from being gap-free. AC-02 tests one atomic batch action, and WF-97 (number allotted at commit, inside the same transaction) plus WF-98 (locked counter row) guarantee 30 consecutive numbers for a committed batch of 30. The reviewer's own replacement wording is self-refuting: it deletes "with no gaps" and substitutes "consecutive within this batch", which is the same requirement in different words. The reviewer also misquotes AC-02 as "30 allocations are created" when it reads "30 tests are created".

Fix assessment: the AC-25 portion of the proposed fix is actively harmful — requiring the page to report "this file's digital signature is not valid" would directly contradict M9-18's mandated single fifth state and would tell a holder of a genuine seal that the seal failed, which M9-17/M9-19 deliberately avoid ("never 'not genuine'", "must never imply..."). The AC-02 portion is a no-op. Only the WF-32/§9.1 portion should be applied, with two collateral edits the reviewer missed.

====================================================================================================
FINDING 9/14  [Minor] [unverified-fact-as-fact]  (raised by fact-integrity reviewer)
LOCATION: Part B §Receipt / Challan worked example (approx. L844); conflicts with Part B §Invoice example (L838), M1-60 and OPEN-Q-C2
EXACT TEXT IN THE DOCUMENT:
*Example:* cash ₹295 received at the counter, money receipt `DVM/MR/2627/00902`
PROBLEM:
The immediately preceding example states "5 Limited Tests at ₹50, taxable value ₹250, plus tax as applicable". ₹295 is ₹250 plus exactly 18 per cent, so this example silently fixes the GST rate at 18 per cent — the one number the rest of the document deliberately refuses to state (M1-60: "No tax rate may appear anywhere in program code"; OPEN-Q-C2: "at the rate the finance wing confirms ... Obtain a written opinion from CSB's tax adviser before go-live"). The research marks 18 per cent for SAC 998346 as [LIKELY, NOT VERIFIED] and records a contradictory source stating 28 per cent, and separately flags an unresolved agricultural-produce exemption question for cocoon work. A Scientist-D reading this glossary will take ₹295 as the arithmetic the system should produce, and a developer will seed 18 per cent from it.
REQUIRED FIX:
Edit SPEC.md:844 (and its source, parts/section_2_workflow.md:402) to drop the tax-inclusive total, leaving no rate derivable and preserving the receipt-versus-invoice contrast and all four payment modes:

"*Example:* the full amount due on invoice `DVM/I/2627/00417` tendered in cash at the counter, money receipt `DVM/MR/2627/00902`; or a Demand Draft; or a bank transaction reference and Unique Transaction Reference; or a government challan reference."

This removes the only tax-inclusive figure in the document, adds no cross-references or meta-claims that the specification does not otherwise support, and keeps the glossary in its plain-language register. Rate and treatment stay where the document already puts them — the Tax master per M1-60/NFR-110, pending OPEN-Q-C2.

Optionally, for consistency, change "takes ₹250 in cash" at SPEC.md:271 to "takes the counter payment in cash" so the narrative walkthrough does not imply the opposite error (zero tax) for the same five-test scenario.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
Independently confirmed on every element. The quote is verbatim at SPEC.md:844 (source: parts/section_2_workflow.md:402). The adjacent Invoice entry at SPEC.md:839 reads "taxable value ₹250, plus tax as applicable", and ₹250 x 1.18 = ₹295 exactly, so the Receipt example does silently pin the rate at 18 per cent. The document's silence on the rate is genuinely deliberate and this is the only leak: grep for "18%" / "18 per cent" across all 152,000 words returns hits only inside URL strings in the sources table, and ₹295 is the SOLE tax-inclusive figure in the whole document (every other rupee amount — ₹50, ₹55, ₹60, ₹67, ₹150, ₹400, ₹2,000, ₹5,000, ₹7,56,789 — is a pre-tax rate-card price, a revenue total, or a statutory threshold). M1-60 (L1389), NFR-110 (L4970-71) and OPEN-Q-C2 (L1395, L5816) are quoted accurately, and the research backing checks out: parts/research_india_commercial.md:227 marks 18% as "[LIKELY, NOT VERIFIED]", L230 flags a contradictory source stating 28%, and L863 lists the cocoon/agricultural-produce exemption as an unresolved question for the tax adviser. I searched for a disclaimer that would neutralise the problem ("illustrative", "indicative only", "worked example", "examples in this document") and found none, so the point is not handled elsewhere.

Two corrections. (1) Severity is inflated. Blocker should mean the spec cannot be built or signed off as-is; this cannot. ₹295 occurs exactly once, in a plain-language glossary illustration. No requirement, data-model field, formula or acceptance criterion derives from it, and the control that actually prevents a hardcoded rate is intact and independently testable — NFR-110 is [MUST] with an acceptance check of "Zero such values in code, confirmed by a search for the known values", which would catch a seeded 18. This is one-line documentation hygiene: Minor.

(2) The proposed fix is wrong in three ways and should not be applied as written. It imports the Invoice-side breakdown ("taxable value ₹250 plus tax") into the Receipt / Challan entry whose stated purpose is the contrast "*It is NOT the Invoice.*", blunting the distinction the glossary exists to draw. Its phrase "for the whole invoice" jars against that same entry's next line, "One receipt may settle several invoices, one invoice may take several receipts, and advances arrive before any invoice exists." Most importantly, the parenthetical "this document states no tax rate anywhere" asserts a document-level policy that does not exist — both M1-60 and NFR-110 scope their prohibition to *program code*, not to the specification prose — so the fix would insert a new unsupported meta-claim, and it injects requirement IDs and an OPEN-Q reference into a Part B glossary that carries none. The correct repair is simply to remove the derivable arithmetic while keeping the entry focused on what it is actually defining ("its mode and its mode-specific references").

Collateral observation the reviewer missed, relevant to the fix: SPEC.md:271 has the Front Desk clerk "takes ₹250 in cash" for the same five-Limited-test scenario, implying zero tax. So the document's two counter-cash figures are mutually inconsistent regardless of what the rate turns out to be; the fix below removes the conflict rather than moving it.

====================================================================================================
FINDING 10/14  [Minor] [contradiction]  (raised by consistency reviewer)
LOCATION: Part B §8.1 "Order Line" (section_2_workflow.md L320) and the §8.2 diagram, against §8.3 (L515)
EXACT TEXT IN THE DOCUMENT:
**Order Line**
One requested test, at one agreed price, for a stated number of samples, within a Test Request.
PROBLEM:
Section 2 contradicts itself on the settled name of one of its own entities. §8.1 heads the entity "Order Line" and the §8.2 cardinality diagram uses "ORDER LINE"; the settled-vocabulary table in §8.3 says the word is "**Request Line**" with the screen label "Request Line" and the forbidden alternatives "Job line, Test". Because §8.3 is the table the UI, the database and every meeting must follow (WF-23), the developer will label the screen "Request Line" while the specification prose he is implementing from says "Order Line", and the two will diverge in review.
REQUIRED FIX:
Rename the prose term to "Request Line" everywhere, leaving all database identifiers alone (§8.3 itself prescribes `txn_order_line`, and parts/section_5_nfr_plan.md:478,482 correctly use `txn_order_line` / `order_line_id` — do NOT change those, or you create a new contradiction with §8.3's own "Database table" column).

1. parts/section_2_workflow.md:320 — change the heading `**Order Line**` to `**Request Line**`. To preserve discoverability for a reader who knows the table name, use the same parenthetical style §8.1 already applies to "Test Request (TRF) / Order": `**Request Line**  *(the line item on a Test Request; table `txn_order_line`)*`.
2. parts/section_2_workflow.md:323 — "The order line is what was agreed and priced" becomes "The request line is what was agreed and priced".
3. parts/section_2_workflow.md:447 — `|1---*  ORDER LINE` becomes `|1---*  REQUEST LINE`.
4. parts/section_2_workflow.md:455 — `|1---*  TEST ALLOCATION  *---1 ORDER LINE` becomes `... *---1 REQUEST LINE`.
5. parts/section_2_workflow.md:257 (WF-7 acceptance check, missed by the reviewer) — "Accept one sample against an order line requesting 4 tests" becomes "... against a request line requesting 4 tests".
6. parts/section_3_functional_core.md:235 — "the concession category on the *order line*" becomes "on the *request line*".
7. parts/section_3_functional_core.md:236 — both uses: "For each order line the system resolves the price" and "the order line's concession category" become "request line" / "request line's".
8. parts/section_4_functional_support.md:840 (M17-05) — "the order line is flagged" becomes "the request line is flagged".
9. parts/section_4_functional_support.md:841 (M17-06) — "store the computed unit price on each order line" becomes "on each request line".

Optionally, add "Order line" to the §8.4 "Words we will stop using" table (say "Request Line" instead) so the drift cannot re-enter, and reassemble SPEC.md from parts/ afterwards.

Separately (out of scope for this finding, worth its own item): §8.1's "**Test Method**" heading and the "TEST METHOD" nodes in the §8.2 diagram diverge from §8.3's settled "**Method Version**", which additionally lists "Test" among the forbidden alternatives.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
Independently confirmed. The quote exists verbatim at parts/section_2_workflow.md:320-323 (SPEC.md:762-765), the §8.1 heading is "**Order Line**", and the §8.2 diagram uses "ORDER LINE" at L447 and L455. §8.3 L515 (SPEC.md:957) settles the word as "**Request Line**" with screen label "Request Line" and forbidden alternatives "Job line, Test". The two definitions describe the identical concept ("One requested test, at one agreed price, for a stated number of samples" vs "One requested test at one price for n samples"), so this is not two legitimately-different things. It is not addressed elsewhere: §8.4 "Words we will stop using" (L543-554) does not list "Order Line", nothing cross-references the two names, and §8's preamble (L286) actually strengthens the finding by requiring these words "and no others". WF-23 (L558) does bind §8.3, so the reviewer's tie-breaker reasoning is sound and the rename direction is correct.

I tested the strongest defense and rejected it: §8.1 headings routinely differ from §8.3's settled word (Test Allocation/Allocation, Observation-Reading/Reading, Sender-Agent/Sender, Test Request (TRF)-Order/Test Request, Specification-Limit Set/Specification Set), which could make this tolerated layering rather than a contradiction. It does not hold, because in every one of those cases the settled word is a substring of the §8.1 heading so the reader can connect them, whereas "Order Line" and "Request Line" share no word and §8.1 never mentions "Request Line" at all. (The one comparable case, §8.1 "Test Method" vs §8.3 "Method Version", is a separate defect the reviewer did not flag.)

Two corrections to the reviewer. First, severity is inflated. The claimed harm is that the developer labels the screen "Request Line" while the prose says "Order Line" and the two diverge in review, but WF-23 resolves exactly that, and §8.3 itself supplies the table name `txn_order_line`, which the §5 data dictionary (parts/section_5_nfr_plan.md:478) already matches. Nothing gets implemented wrongly; both the screen label and the schema name are unambiguously pinned. The defect is spec-internal consistency, not build correctness. Second, the fix is incomplete: it patches 4 of 9 sites and leaves the same contradiction live inside WF-7's acceptance check (parts/section_2_workflow.md:257, same section as the finding) and in MUST requirements at parts/section_3_functional_core.md:235-236 and parts/section_4_functional_support.md:840-841 (M17-05, M17-06). The fix must also leave the DB identifiers `txn_order_line` and `order_line_id` untouched, since §8.3 prescribes them and renaming those would create a new contradiction against the settled-vocabulary table.

====================================================================================================
FINDING 11/14  [Minor] [contradiction]  (raised by consistency reviewer)
LOCATION: Part B §10.1 rows "Report number" (section_2_workflow.md L736) and "Conditioning Certificate number" (L738)
EXACT TEXT IN THE DOCUMENT:
| **Conditioning Certificate number** | `DVM/CM/2026-27/<4>` | `DVM/CM/2026-27/0007` | Financial year | Everyone; printed on the certificate | Its own gap-free series,
PROBLEM:
The same document is given two different number formats in the same table. The Report number row defines `DVM/<TYPE>/2026-27/<5>` and lists `CM` among the report types ("`CM` conditioning (conditioned mass) certificate"), which yields a 5-digit serial; the Conditioning Certificate row defines a 4-digit serial. §8.3 confirms the conditioning certificate is a report type, not a separate entity ("Mass and settlement document | **Conditioning Certificate** (report type) | `txn_report`, type `CONDITIONING`"), so only one series can exist. Part C M10 then adds a third identifier ("Conditioning job number | Text | Auto | From its own series"). Because both series are declared gap-free, whichever the developer implements the other's Series Register report will report every number as Missing.
REQUIRED FIX:
Keep the conditioning certificate in the report-number series as report type `CM` (per §8.3, the Report number row's own type list, and OPEN-Q-B6's "as a report type with its own number series"), and align on the 5-digit serial. Three edits, all in Part B:

1. parts/section_2_workflow.md L738 — delete the standalone "Conditioning Certificate number" row.

2. L736 — append to the Report number row's Notes, worded so it does NOT contradict Part C: "The conditioning (conditioned mass) certificate is report type `CM` in this series — e.g. `DVM/CM/2026-27/00007`. Per the reset rule above it has its own gap-free counter, its own prefix and its own register, separate from the `TR` test-report series (M10-01, M10-33, M10-37); it is not a second series with a different format." Do not write "it has no separate series" — that wording breaks M10-01 and M10-33 [MUST].

3. WF-99 (parts/section_2_workflow.md, SPEC.md L1215) — drop the redundant "conditioning certificate" item from the gap-free list, since "report (per type)" already covers `CM`.

Leave Part C M10-01, M10-33, M10-36 and M10-37 unchanged: per-type sub-series with a separate register already satisfy them, and M10-33's acceptance check passes as `DVM/CM/2026-27/00007` versus `DVM/TR/2026-27/00380`.

Do NOT rename "Conditioning job number" to a worksheet number on the `DVM/WS/<FY>/<5>` series — nothing supports it and the WS series is the gaps-permitted batch run sheet on `txn_worksheet`. Instead resolve M10's dangling "From its own series" by adding one §10.1 row: "| **Conditioning job number** | `DVM/CJ/2026-27/<4>` | `DVM/CJ/2026-27/0031` | Financial year | Front Desk, Tester, Section Head, Unit Incharge | Internal work record for the lot registration. Gaps permitted. Never printed on the certificate; the certificate carries its `CM` report number, allotted at authorisation. |" (`CJ` does not collide with any code already used in §10.1.)

Separately, and worth raising as its own finding rather than folding in here: §8.3 L972 stores the certificate on `txn_report` type `CONDITIONING`, while M10-02 [MUST] requires that "the weight certificate has its own tables and no grade columns". That storage tension is unresolved and should not be cited as the authority for this numbering fix.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
Confirmed verbatim at parts/section_2_workflow.md L736 and L738 (SPEC.md L1178/L1180). The Report number row lists `CM` conditioning (conditioned mass) certificate as a report type under `DVM/<TYPE>/2026-27/<5>` (yielding `DVM/CM/2026-27/00007`), while L738 defines the same `DVM/CM/` prefix with a 4-digit serial (`DVM/CM/2026-27/0007`). Same prefix, same FY reset, both declared gap-free, two incompatible serial widths in one table. WF-99 (L1215) compounds it by listing "report (per type)" and "conditioning certificate" as two separate gap-free series. The decisive evidence — which the reviewer missed — is that CM's siblings `GC`, `PE`, `PS` and `HS` have NO standalone rows in §10.1, even though M10-03 and M10-04 [MUST] require Preliminary Examination and Grading Certificate to be "separately numbered" with "its own number series"; those are satisfied purely by the Report number row's reset rule "separate series per report type". So the L738 row is a leftover duplicate, and a real defect. However, the reviewer's framing and severity overreach on three points. (a) "Only one series can exist" is a misreading: "separate series per report type" already makes the CM counter its own series, so there is no entity-level conflict, only a serial-width conflict. (b) The Part C M10 "Conditioning job number" (L2846) is not a third identifier for the certificate — M10-05 registers a conditioning job against a lot and the certificate is issued per lot at authorisation, so it is a distinct work record; the reviewer's own fix concedes this by renaming it. (c) The claimed failure mode ("the other's Series Register report will report every number as Missing") is wrong — a single counter cannot report itself Missing. Severity should be Minor because WF-96 [MUST] makes "serial width" explicit configuration ("No number format is written into program code"), so 4-vs-5 is seed data rather than code, and M10-33's acceptance check ("Issue a weight certificate and a test report on the same day; their numbers come from different series") is satisfied under either reading. The residual harm is a wrong-width number printed on a money-settlement document plus a redundant WF-99 entry. The proposed fix is also imprecise: the phrase "it has no separate series" collides head-on with M10-01 and M10-33 [MUST] ("its own numbering series", "its own prefix"), and rerouting the conditioning job onto `DVM/WS/<FY>/<5>` is unsupported — §8.3 binds Worksheet to `txn_worksheet` "batch run sheet" and §10.1 marks that series "Gaps permitted", while M10 already has separate worksheet-like artifacts (bale weighing sheet, tare build-up sheet, oven drying register).

====================================================================================================
FINDING 12/14  [Minor] [contradiction]  (raised by consistency reviewer)
LOCATION: Part B §8.1 examples (section_2_workflow.md L327, L357, L382, L402) and Part C M3 worked example (section_3_functional_core.md L511) against §10.1 (L732, L736)
EXACT TEXT IN THE DOCUMENT:
*Example:* 5 skeins of raw silk received on 19 August 2026, marked "SLS-118", declared 20/22 denier, 350 g, registered as `DVM/2627/RS/01188`.
PROBLEM:
The worked examples do not match the format string they illustrate. §10.1 defines the Sample number as `DVM/2026-27/<TYPE>/<5>` with the example `DVM/2026-27/RS/01188`, and §10's preamble restricts the compressed form to length-constrained documents only: "written **`2026-27`** in full and compressed to **`2627`** where character length is constrained" — the constraint applies to GST documents. Yet §8.1 uses `DVM/2627/RS/01188`, `DVM/2627/RS/01188-T01`, `DVM/TR/2627/00380` and `DVM/MR/2627/00902`, and Part C's M3 worked example uses `DVM/2627/RS/00411` … `DVM/2627/RS/00415`. Sample, report and money-receipt numbers are not length-constrained, so the compressed form is wrong in every one of these. The developer will seed the series from whichever example he reads, and the two forms are not interchangeable once history exists.
REQUIRED FIX:
Replace all nine compressed-FY tokens in non-GST examples with the full financial year, matching §10.1 exactly.

In parts/section_2_workflow.md (SPEC.md line in brackets):
- L327 [769] Sample: `DVM/2627/RS/01188` -> `DVM/2026-27/RS/01188`
- L357 [799] Test Allocation: `DVM/2627/RS/01188-T01` -> `DVM/2026-27/RS/01188-T01`
- L382 [824] Report: `DVM/TR/2627/00380` -> `DVM/TR/2026-27/00380`
- L387 [829] Report Revision — BOTH tokens on this line: `DVM/TR/2627/00380/R1` -> `DVM/TR/2026-27/00380/R1` AND "superseding `DVM/TR/2627/00380`" -> "superseding `DVM/TR/2026-27/00380`"
- L402 [844] Receipt / Challan: `DVM/MR/2627/00902` -> `DVM/MR/2026-27/00902`
- L427 [869] Complaint (report reference): `DVM/TR/2627/00380` -> `DVM/TR/2026-27/00380`

In parts/section_3_functional_core.md:
- L514 [SPEC.md 1759] M3 worked example — both tokens: `DVM/2627/RS/00411` … `DVM/2627/RS/00415` -> `DVM/2026-27/RS/00411` … `DVM/2026-27/RS/00415`

Leave unchanged (all legitimately GST / length-constrained): §8.1 L397 invoice `DVM/I/2627/00417`; §10.1 rows for invoice, bill of supply, advance receipt voucher, refund voucher and credit note; and parts/section_4_functional_support.md:873 (M17-28) acceptance check `DVM/2627/{5 digits}`, which is an invoice series. Also leave the unrelated `DVM/F/12` at parts/section_4_functional_support.md:462 alone — that is a Format Register number, not a refund voucher.

Optional hardening: a one-line note under §10 preamble L721 naming the five series that use `<FY4>`, so the compressed form can never be read as a general option.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
Independently confirmed. The quote exists verbatim at parts/section_2_workflow.md:327 (SPEC.md:769). §10.1 (L732-745) defines Sample as `DVM/2026-27/<TYPE>/<5>`, Allocation as parent+`-T<2>` (`DVM/2026-27/RS/01188-T03`), Report as `DVM/<TYPE>/2026-27/<5>`, Money Receipt as `DVM/MR/2026-27/<5>`, and reserves `<FY4>` for the five GST series only. The §10 preamble (L713-721) restricts the 16-character family to "GST documents" and says the FY is written `2026-27` in full and "compressed to `2627` where character length is constrained" — a condition that does not hold for sample, report or money-receipt numbers (§10.1's Proforma row makes the same point explicitly: "Not a tax document, so no length limit"). I enumerated every `DVM/...` token in all authored sections: exactly nine tokens on eight lines use the compressed form for a non-GST series, and every other compressed-FY use is a legitimate GST document. There is no caveat anywhere that examples are non-normative, and no competing constraint (barcode/label width or sample-number field length) that would justify the compressed form — M3-20 and M1-27 cap the type code at three characters but say nothing about total number length. So the contradiction is real and the reviewer's direction of resolution (§10.1 wins) is right.

Two corrections. (a) The Location field is incomplete: it omits L387 (which carries TWO offending tokens) and L427, though the fix narrative does reach them; and the fix as written changes only the first token on L387, leaving "superseding `DVM/TR/2627/00380`" inconsistent. My corrected fix enumerates all nine tokens. (b) Severity is inflated. §10.1 is the only place in the document that states a number *format*; §8.1 and the M3 table give bare instances with no format string, so no one can seed or configure a series from them, and WF-96 makes every series definition pure configuration with "no number format written into program code". The "developer seeds from whichever example he reads" failure mode therefore requires ignoring the one section that actually specifies formats. Real, worth fixing, but no requirement is left ambiguous and no design decision turns on it — Minor.

The fix introduces no new contradiction: every replacement string is byte-identical to an example already printed in §10.1, and the 16-character rule (WF-105, M17-28) touches only the tax series, which stay in the `2627` form. Note also that parts/section_4_functional_support.md:873 (M17-28) uses `DVM/2627/{5 digits}` for an invoice series and is correct as-is — the reviewer rightly leaves it alone.

====================================================================================================
FINDING 13/14  [Minor] [buildability]  (raised by technical-half reviewer)
LOCATION: section_2_workflow.md §10.1 sub-sample number (line 733), WF-8 (line 258); section_5_nfr_plan.md V8 (line 18)
EXACT TEXT IN THE DOCUMENT:
| **Sub-sample number** | parent sample number + `-P<2>` | `DVM/2026-27/RS/01188-P07` | Per parent sample |
PROBLEM:
A two-digit suffix caps a sample at 99 sub-samples. The document's own flagship grading case exceeds that: V8 puts the size-deviation test at 80 sizing skeins, and WF-8's acceptance check requires the same sample to carry the 10 bobbins, the 20 seriplane panels and (per §8.1) the 6 skeins drawn for conditioned mass alongside them — 116 specimens on one sample before any retest. The allocation suffix `-T<2>` has the same cap and WF-8 plus repeat allocations under WF-74 can push a full grading order past it. `txn_subsample.suffix VARCHAR(8)` is wide enough; it is the numbering rule that breaks, and OPEN-Q5 warns that this decision "cannot be changed later without re-registering history".
REQUIRED FIX:
Change only the sub-sample row in §10.1 (parts/section_2_workflow.md:733, mirrored at SPEC.md:1175): format "parent sample number + `-P<3>`", example `DVM/2026-27/RS/01188-P007`, and add to its Notes column: "Three digits. One raw-silk grading sample yields 6 conditioning skeins, 10 bobbins, 40 or 80 sizing skeins and 20 seriplane panels, each of which is a sub-sample in its own right (see §8.1 and M4-14) — over 110 on one sample before any repeat. `txn_subsample.suffix VARCHAR(8)` already accommodates this." Leave the Allocation number row at `-T<2>`: allocations are one per sample x test (WF-7), so a full grading plus WF-74 repeats stays well under 99; optionally add "Two digits suffice: a sample carries one allocation per requested test, and repeats under WF-74 take the next free suffix." Instead of the proposed mst_sample_type column, extend WF-96 (or M1-69) with: "A series whose numbers are derived per parent record is refused if its serial width cannot accommodate the largest specimen or replicate count declared on any active method version applicable to that sample type (`mst_method.reading_count_default`, `mst_method_parameter.reading_count`)." This keeps draw and replicate counts on the method version, where WF-38, M4-11 and M6-03 already put them, and needs no new master field.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
The quote is verbatim (parts/section_2_workflow.md:733, SPEC.md:1175) and the capacity problem is real for the sub-sample suffix. The document's own §8.1 definition (section_2_workflow.md:330-333) makes each individual specimen a Sub-sample — "skein 3 of the 6 drawn for conditioned mass; bobbin 7 of the 10 wound; sizing skein 24 of 40; seriplane panel 12 of 20" — and M4-14 (section_3_functional_core.md:597) requires each specimen label to carry "the suffix" with the acceptance check "Print forty sizing-skein labels; each is distinct". txn_subsample is keyed UNIQUE(parent_sample_id, suffix) (section_5_nfr_plan.md:481), a flat namespace per parent sample, so `-P<2>` truly caps one sample at 99 specimens. V8 (section_5_nfr_plan.md:18) and M6-03 (section_3_functional_core.md:866, "the system asks for 80 readings, not 40") give 80 sizing skeins for coarse silk, and WF-8 plus §8.1 put the 6 conditioning skeins, 10 bobbins and 20 panels on the same sample: 116 specimens, over the cap, and even the 40-skein case (76 plus kilcha and cohesion test pieces) leaves almost no headroom. Nothing elsewhere handles this; M1-69's length guard applies only to tax documents. However, three parts of the finding are wrong or overstated, so Major is inflated: (1) the allocation suffix `-T<2>` is adequate — WF-7 creates one allocation per sample x test, a full grading is roughly 10-15 tests and WF-74 repeats double that, nowhere near 99; the reviewer conflated "order" (many samples) with "sample", which is the suffix's actual scope. (2) OPEN-Q5 (section_2_workflow.md:563) concerns what counts as one sample, not the suffix width, so the "cannot be changed later without re-registering history" quote is misattributed. (3) WF-96 (line 770) and M1-69 (section_3_functional_core.md:171) already make serial width per-series configuration with no number format in code, so the defect is a wrong default value in one table cell, not a design or schema rework. The proposed fix is also partly incorrect: putting "the widest count per sample type" on mst_sample_type contradicts the rest of the specification, where draw and replicate counts belong to the method version (WF-38 line 651, M4-11 section_3:594, M6-03 section_3:866, mst_method.reading_count_default, mst_method_parameter.reading_count), and mst_sample_type (section_5_nfr_plan.md:433) has no such column.

====================================================================================================
FINDING 14/14  [Minor] [missing-capability]  (raised by technical-half reviewer)
LOCATION: section_2_workflow.md §8.1 Weight / Conditioning Certificate (lines 390-393), §8.3 (line 530), OPEN-Q6 (line 565); section_5_nfr_plan.md §25.2 (lines 475-497)
EXACT TEXT IN THE DOCUMENT:
*It is NOT a test report.* It is a mass and settlement document in kilograms, with an arithmetic build-up the customer will audit line by line. Its data shape is completely different from a quality report and it must not be forced into the same tables.
PROBLEM:
Part 2 states in the strongest terms that the conditioning certificate must not live in the report tables, then §8.3 puts it in `txn_report` with `type = CONDITIONING`, and §25 provides no table for its data at all. Gross mass, itemised tare per bale or book, net mass, average moisture, oven-dry mass and conditioned mass (oven-dry plus 11%) have nowhere to be stored: `txn_result` is one row per parameter per allocation and cannot hold a per-bale tare build-up, and `txn_report_test.snapshot_json` freezes printed values but is not a place to compute or audit them. The 11% commercial allowance is stated as a literal in prose with no configuration home, which NFR-110 forbids for exactly this class of value. OPEN-Q6 defers the certificate's scheduling but not its data model, and the lab will read §8.1 and reasonably assume the tables exist.
REQUIRED FIX:
Scope the fix to the one real gap: §25 is missing the M10 tables that M10-02 already mandates. Do NOT apply the reviewer's schema — it contradicts Part 3 in five ways.

DO NOT DO:
- Do not change §8.3. `txn_report` / type `CONDITIONING` is correct and is what §8.1 line 383 prescribes. At most append a clarifying clause "(mass arithmetic in its own tables, M10-02)".
- Do not put `conditioning.allowance_pct` in `sys_setting`. As specified in §25.4, `sys_setting` has columns `scope, scope_id, key, value, data_type, description` and **no effective_from/effective_to**. Placing a dated commercial rate there directly violates M10-23 ("configuration with an effective date"), M10 rule 4 ("a data edit with a version history") and DB-22 ("Reference data that changes over time is dated, never overwritten"). Instead add a small dated master `mst_conditioning_regain` (`silk_type`, `regain_pct DECIMAL(6,3)`, `effective_from DATE`, `effective_to DATE`, `authority_note TEXT`), or add the three dated columns to `sys_setting` and amend DB-22 to match.
- Do not amend NFR-110. Its list is tax and accreditation identifiers; the regain rate is already governed by M10-23 and DB-22.

DO: add to §25.2, at the granularity Part 3 actually requires (all with `unit_id NOT NULL` per DB-41 and the DB-01 audit columns):
- `txn_conditioning_lot` — lot header. `unit_id`, `job_no`, `order_id`, `sample_id`, `certificate_report_id NULL`, `mark_text`, `chop_text`, `no_of_bales INT`, `no_of_books_per_bale INT`, `packing_manner`, `skein_formation`, `reeling_device_id`, `declared_mass_kg DECIMAL(18,6)`, `colour_sorted BOOLEAN`, `avg_moisture_pct DECIMAL(6,3)`, `regain_pct_applied DECIMAL(6,3)`, `regain_effective_from DATE`, `conditioned_mass_lot_kg DECIMAL(12,3)`, `state`. The regain rate is snapshotted here at computation, per DB-21.
- `txn_conditioning_bale` — one row per bale, because M10-22 and M10-25 require gross, tare, net, oven-dry and conditioned mass **per bale** and totalled for the lot; the reviewer's lot-level `net_mass_kg`/`oven_dry_mass_kg`/`conditioned_mass_kg` cannot express this. Columns: `conditioning_lot_id`, `bale_serial_no`, `gross_mass_kg DECIMAL(12,3)`, the seven tare lines as **named stored columns** (`tare_shirt_kg`, `tare_papers_labels_kg`, `books_sampled_for_tare INT`, `band_mass_kg`, `bands_per_book INT`, `tare_sampled_books_kg`, `tare_one_book_kg`, `tare_all_books_kg`, `tare_other_kg`, `excess_lacing_kg`, `lacings_within_limit BOOLEAN`), `total_tare_kg`, `net_mass_kg`, `oven_dry_mass_kg`, `conditioned_mass_kg`, `platform_balance_equipment_id`, `weighed_at`, `weighed_by_personnel_id`. M10-09 requires each tare line to be "a stored value, not a derived display" — a single `tare_mass_kg` per line row does not satisfy this.
- `txn_conditioning_moisture_set` — one row per set (M10-19 computes moisture separately for each of two sets and prints both; the reviewer's single lot-level `moisture_pct` cannot hold this). `conditioning_lot_id`, `set_no SMALLINT`, `pre_dry_mass_g DECIMAL(12,4)`, `oven_dry_mass_g DECIMAL(12,4)`, `moisture_pct DECIMAL(6,3)`, `skein_balance_equipment_id`, `oven_equipment_id`, `drying_temp_c DECIMAL(5,1)`, `convergence_satisfied BOOLEAN`.
- `txn_conditioning_dry_weighing` — the dry-to-constant-mass loop. `moisture_set_id`, `seq_no`, `elapsed_minutes INT`, `mass_g DECIMAL(12,4)`, `loss_from_previous_g DECIMAL(12,4)`.
- `txn_conditioning_skein` — the skein draw. `conditioning_lot_id`, `moisture_set_id`, `source_bale_id`, `source_book_ref`, `colour_group`, `drawn_at`, `drawn_by_personnel_id`, `books_replaced BOOLEAN`.

Also drop the reviewer's `line_type BALE/BOOK/...` + free-text `identifier_text` design: OPEN-Q5's recommended default is that for conditioning the **lot is the Sample and the bales and books inside it are Sub-samples**, so bale and book identity should reference `txn_subsample`, not free text.

Add a §25.6 constraint that a `CONDITIONING`-type `txn_report` cannot reach ISSUED without a linked `txn_conditioning_lot` whose bale count matches `no_of_bales` and whose two moisture sets agree within the configured tolerance (M10 rule 5 makes this a hard block, not a warning).

Note for the parent agent, separately from this finding: §26's phase summary and phase deliverable lists never mention the conditioning certificate module at all — "M10" appears nowhere in section_5_nfr_plan.md. That, not the data model, is the unresolved half of OPEN-Q6.
INDEPENDENT VERIFIER CONFIRMED THIS BECAUSE:
VERIFIED IN NARROW FORM ONLY. Two of the finding's three stated problems fail on the document, its category is wrong, and its fix would introduce new contradictions. One residual defect is real.

1. Quote is genuine — parts/section_2_workflow.md:393, verbatim.

2. The claimed §8.1 vs §8.3 contradiction DOES NOT STAND — reviewer misread. Ten lines above the quote, §8.1 line 383 states the design explicitly: "Test report, grading certificate, conditioned-mass certificate, pre-shipment certificate and HSN certificate are all **report types** — one entity, different templates, different numbering series and different signatory rules." Line 390 itself opens "A report type that states the commercial invoice weight...". So §8.3's `txn_report` / type `CONDITIONING` is precisely what §8.1 prescribes. Line 393's "must not be forced into the same tables" is about the mass data payload, not the document envelope — confirmed by section_3_functional_core.md:1534 (M10-02) whose acceptance check reads "Inspect the stored records; the weight certificate has its own tables and no grade columns." The reviewer conflated envelope with payload.

3. "The 11% allowance is stated as a literal in prose with no configuration home" DOES NOT STAND. section_3_functional_core.md:1570 (M10-23, [MUST]): "The regain rate is held as **configuration with an effective date**, seeded at **eleven percent of the oven-dry mass** ... It is never a number in program code," with the acceptance check "Change the configured regain rate in a test system; the computed conditioned weight changes with no software change, and a previously issued certificate is unaffected." Reinforced by M10 rule 4 (line 1671) and the field-table row "Regain rate applied | Derived | Auto | From configuration, with its effective date". That is a stronger requirement than the reviewer proposes. Separately, NFR-110's enumerated list is tax rates, tax exemptions, service codes, tax registration numbers, statutory deadlines and accreditation numbers — the regain rate is not in that class; the governing rule is DB-22.

4. "The lab will read §8.1 and reasonably assume the tables exist" DOES NOT STAND as characterised. M10 (section_3_functional_core.md:1503-1680) specifies this module in more depth than any other: M10-08 to M10-25, plus a three-part field table (lot header, per-bale weighing and tare with all seven tare lines, skein draw and two-set moisture with the dry-to-constant-mass loop), plus eight rules and a 20-historical-worksheet regression acceptance test. No implementer is left without a data model.

5. WHAT IS REAL: §25 genuinely contains no physical-schema entry for those tables. I read §25.1 through §25.4 in full and grepped SPEC.md for `txn_conditioning`, `conditioning_lot`, `txn_bale`, `txn_mass`, `gross_mass`, `oven_dry`, `net_mass`, `conditioned_mass`, `tare` — zero hits in part 5. §25 carries no "not exhaustive" disclaimer, and it does include later-phase tables (`txn_pt_participation`, `txn_document_control`), so deferral does not explain the omission. Conditioning is the one module Part 3 says cannot use the generic `txn_observation`/`txn_result` shape (M10-02, M10 rule 7), and it is the one module for which §25 offers nothing. That is a genuine Part 3 / Part 5 consistency gap against a [MUST].

6. The proposed fix is materially wrong and must not be applied as written — see correctedFix.
